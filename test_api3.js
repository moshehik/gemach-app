const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const eventDateStr = new Date().toISOString(); const eventDate = new Date(eventDateStr); const startWindow = new Date(eventDate); startWindow.setDate(startWindow.getDate() - 7); const endWindow = new Date(eventDate); endWindow.setDate(endWindow.getDate() + 7); const overlappingOrderItems = await prisma.orderItem.findMany({ where: { order: { status: { notIn: ['מבוטל', 'מחוק'] }, eventDate: { gte: startWindow, lte: endWindow } } }, include: { dressItem: true } }); console.log('Overlapping items:', overlappingOrderItems.length); } main().catch(console.error).finally(() => prisma.$disconnect());
