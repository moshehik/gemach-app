 import prisma from '@/app/lib/prisma'; async function main() { const setting = await prisma.systemSetting.findUnique({ where: { key: 'item_locations' } }); if (!setting) { await prisma.systemSetting.create({ data: { key: 'item_locations', value: '����,�����,����', name: '������ ������', category: '����', type: 'text' } }); console.log('Created setting'); } else { console.log('Setting exists'); } } main().catch(console.error).finally(() => prisma.$disconnect());
