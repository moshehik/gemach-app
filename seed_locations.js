const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const setting = await prisma.systemSetting.findUnique({ where: { key: 'item_locations' } }); if (!setting) { await prisma.systemSetting.create({ data: { key: 'item_locations', value: 'חנות,רזרבה,מחסן', name: 'מיקומי פריטים', category: 'מלאי', type: 'text' } }); console.log('Created setting'); } else { console.log('Setting exists'); } } main().catch(console.error).finally(() => prisma.$disconnect());
