# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## What this is

A Next.js (App Router) management system for a "gemach" (גמ"ח) — a free-loan society that lends out wedding/event dresses. It tracks customers, employees, dress inventory (models + physical items), rental orders, payments/refunds, price lists, work shifts, and internal messaging. UI and data are Hebrew, RTL.

## Commands

Run from `gemach-app/` (this is the actual app root; the parent folder contains unrelated legacy Access-migration scratch files and is not part of the app).

```bash
npm run dev      # next dev --webpack
npm run build     # prisma generate && next build --webpack
npm run start
npm run lint      # eslint
```

There is no automated test suite (no Jest/Vitest configured). Verification is done by running one-off scripts (see below) or manually through the dev server. `eslint.config.mjs` ignores `*.js` at the repo root, `scripts/**`, and `scratch/**` — these are ad-hoc/throwaway scripts, not part of the linted app.

The repo root is cluttered with many one-off `check_*.js`, `fix_*.js`, `test_*.js`, `migrate_*.js`, `scratch_*.js` scripts used historically for data migration/debugging against the DB directly. They aren't part of the app runtime — don't treat them as source of truth for current behavior, and don't add new throwaway scripts there; prefer `scratch/` (gitignored) for anything temporary.

## Architecture

### Dual/offline database mode
[app/lib/prisma.js](app/lib/prisma.js) exports a `Proxy` that routes every Prisma call to either a "prod" or "test" Postgres client based on the `.active-db` file (or `globalForPrisma.activeDbMode`) — toggled via `/app/api/admin/database`. There is also an `IS_OFFLINE_MODE` env flag that swaps in a locally generated `@prisma/local-client` (SQLite, schema is [prisma/schema.local.prisma](prisma/schema.local.prisma) — `prisma/schema-sqlite.prisma` is an unrelated, stale, unused file despite the similar name, don't confuse the two) for working without internet. Always import the shared client from `@/app/lib/prisma` — never instantiate `PrismaClient` directly.

The local client isn't generated automatically by `npm install`/`npm run build` (that would make Vercel builds depend on a `SQLITE_URL` env var they don't need) — run `npm run generate:offline` once on any machine that will actually use offline mode (needs `SQLITE_URL` in `.env`), then `npm run offline:db-push` to create the local SQLite tables. Rows written offline get reconciled back into Postgres by [lib/offlineSync.js](lib/offlineSync.js) — a simple delta sync keyed by each model's `updatedAt`, upserting by `id` (both schemas use the same UUIDs, so no id remapping is needed) with true last-write-wins conflict handling (the cloud row's own `updatedAt` is checked before every upsert; a newer-or-equal cloud row is skipped, not overwritten — see the 2026-09-09 incident below for why this matters) plus a manual `AuditLog` entry per synced row. It runs automatically once per process on server startup whenever `IS_OFFLINE_MODE` is not `'true'` (see `syncOfflineChangesIfAny` in `app/lib/prisma.js`), and can also be triggered on demand from `/admin/database` ("סנכרון נתוני אופליין") without restarting the server. Currently covers `Employee`, `Customer`, `DressModel`, `DressItem`, `Order`, `OrderItem`, `Payment`, `PaymentObligation` (`SYNCED_MODELS` in `lib/offlineSync.js`) — extend that list if other models start being written while offline. **Before re-enabling offline mode on any machine, read the 2026-09-09 incident section below first.**

### Automatic audit logging
The same Prisma client wraps every model's `create`/`update`/`delete` in a query extension that writes an `AuditLog` row (entity type/id, diffed `changesJson`, and the acting `employeeId` pulled from the `auth_token` cookie). `AuditLog`, `PageVisitLog`, and `Shift` are excluded to avoid recursive/noisy logging. Because of this, bulk data fixes are usually better done through raw SQL or scripts than through Prisma writes, to avoid flooding the audit log.

### Data model ([prisma/schema.prisma](prisma/schema.prisma))
Core entities and relations:
- `Customer` → `Order` → `OrderItem` → `DressItem` → `DressModel` (a `DressModel` is a dress design; `DressItem` is a physical, barcoded unit of that design; `OrderItem` links a rented unit to an order and tracks its own rental lifecycle: `isTaken`/`isReturned`/`returnedOk`/dates).
- `Order` → `Payment` / `Refund` / `PaymentObligation` (obligations are the itemized charges derived from `PriceList`/`PriceRule`; payments/refunds settle them).
- `Employee` → `Shift` (punch clock), `Department` (role), `Notification` (internal messaging with per-recipient read/archive state via `NotificationTag`).
- Almost every model has `legacyId` (unique) — a carry-over ID from the original Microsoft Access database this system replaced, used to cross-reference during/after migration. Most models also have soft-delete (`isDeleted`) rather than hard deletes.

### Business logic lives in `lib/`, not in routes
- [lib/pricingEngine.js](lib/pricingEngine.js) — recalculates `PaymentObligation`s for an order; explicitly ports logic from the legacy Access VBA module `שמלות_תשלום_רישום`.
- [lib/inventory.js](lib/inventory.js) — dress item availability/overlap logic, including `addDaysSkippingWeekends` for rental-period spacing between bookings.
- [lib/orderStatus.js](lib/orderStatus.js) — derives an order's Hebrew status string (`הוחזר`, `הושכר חלקי`, `מחוק`, etc.) from its items' taken/returned flags — this is the source of truth for order status, don't duplicate the logic in components.
- [lib/hebrewDate.js](lib/hebrewDate.js) and the `@hebcal/core`/`hebcal` deps — Hebrew calendar conversions, used throughout order/event dates alongside Gregorian dates (`Order.eventDate` + `eventDateHebrew`).
- [lib/ai/gemini.js](lib/ai/gemini.js) — Gemini API integration backing the AI search/chat features (`AIChatSession`, `AISearchBar`, `AIFloatingWidget`); keys come from `GEMINI_API_KEYS` in `.env.local`.

### Routing/structure conventions
- App Router: pages under `app/<feature>/`, matching API routes under `app/api/<feature>/route.js`.
- Path alias `@/*` maps to the repo root (`jsconfig.json`).
- Shared client code lives in `app/lib/` (Next-coupled: `prisma.js`, `nedarim.js`) and `lib/` (framework-agnostic business logic) — check both when looking for a utility.
- Print views are separate routes under `app/print/` (e.g. `app/print/order`, `app/print/alterations`) rendered standalone for printing, not modals.
- Auth is a simple cookie (`auth_token` = employee UUID) gated by the `require_login` `SystemSetting`; see [lib/auth.js](lib/auth.js). `SystemSetting` (key/value rows) drives various runtime toggles — check there before assuming a feature is hardcoded.

### ID display rule (from AGENTS.md)
When referencing entities (Orders, Customers, Dress Models, etc.) in code, UI, or communication with the user, always use the human-readable short ID (`orderId`, `legacyId`, `barcodePrefix`), never the internal UUID `id` — except where a route genuinely requires the UUID (e.g. `/customers/[id]` uses the UUID; `/orders/[id]` uses the numeric `orderId`).

## System emails
All outbound email (password resets, manager-sent messages, order/rental reports, bug
reports, internal-message alerts) goes through a shared Google Apps Script mailer and a
shared HTML template in [lib/emailTemplates.js](lib/emailTemplates.js). See
[EMAILS.md](EMAILS.md) for the full list of send sites, attachment filenames, and how to
add a new templated email.

## Keep-alive (2026-09-01 בוטל)
Task GemachApp-NeonKeepAlive כל 5 דקות (07:55-21:55) בוטל ונמחק לבקשת הבעלים - Neon ישן כדי לחסוך compute. נשאר רק cron יומי 04:00 ב-vercel.json. התכנית החדשה: דגל ב-Vercel יעיר את Neon רק כשיש שינויים. קובץ scripts/neon_keepalive.ps1 מסומן DEPRECATED.

## Backups
Neon's built-in point-in-time restore (7-day window) is the first line of defense but never leaves Neon. [scripts/backup_prod_db.js](scripts/backup_prod_db.js) adds a second, independent layer — a nightly compressed logical SQL dump written to `backups/` (gitignored), scheduled via a Windows Task Scheduler job (`GemachApp-ProdDbBackup`, daily 03:30). See [BACKUPS.md](BACKUPS.md) for what each layer covers, retention/rotation, the restore command, and how to verify the nightly job actually ran.

## Legacy Access data import
The owner still edits the original Microsoft Access file in parallel with the live app, so [scripts/import_from_access.js](scripts/import_from_access.js) exists as a **permanent, reusable** full-reload/upsert tool (never a delete-then-insert) — re-run it whenever the Access file has new data. `node scripts/import_from_access.js` dry-runs (reports counts only); add `--write` to actually write to PROD. Full usage, exact field mappings, and the data-quality guards it applies (Hebrew-date parsing via `lib/hebrewDate.js`, null-date sentinel handling, etc.) are documented in the script's header comment — read that before re-running it or touching the mapping logic. One important known gap: OrderItem/Payment/PaymentObligation are only inserted for orders that are brand-new to the DB in a given run (pre-existing orders' sub-records are left untouched) — see the header for why.

## Audit agent system
`/audit-system` (defined in [.claude/commands/audit-system.md](.claude/commands/audit-system.md)) runs 11 fixed subagents — [.claude/agents/audit-*.md](.claude/agents/) — covering code bugs, code quality, security, orders, employee attendance, inventory, payments/refunds, data integrity, performance, print/UI, and audit-log/backup health, in parallel. Every agent is read-only; the command itself performs the one write in the whole system by parsing each agent's findings into JSON and inserting a single `AuditReport` row via [scripts/insert_audit_report.js](scripts/insert_audit_report.js) (a raw-`PrismaClient` CLI tool, same DB-selection logic as `app/lib/prisma.js` — respects `.active-db`). Reports are viewed, not read as files: `/admin/audit-system` ([app/admin/audit-system/page.js](app/admin/audit-system/page.js) + [AuditReportsPanel.js](app/admin/audit-system/AuditReportsPanel.js)) renders past runs as collapsible cards via `GET /api/admin/audit-reports`. It's a developer tool run from within Claude Code, not an in-app button — the DB-checking agents run read-only one-off Node scripts (same pattern as `scripts/audit_100_orders.js`) against whichever DB is currently active; `audit-backups-history` additionally reads `backups/*.log` and queries Windows Task Scheduler (`schtasks /Query`) to verify the nightly backup/log-cleanup jobs from [BACKUPS.md](BACKUPS.md) actually ran.

## Customer kiosk screen
`app/customer-interface/page.js` is handed to customers unsupervised to browse the dress catalog. The in-app lock (fullscreen + code-gated header buttons + re-lock on Esc/fullscreen-exit + blocked right-click context menu) only covers the browser tab — it can't stop Alt+Tab, the Windows key, or closing the browser window. [scripts/kiosk/launch-kiosk.bat](scripts/kiosk/launch-kiosk.bat) launches the screen in real OS-level `--kiosk` mode for the physical customer-facing machine. See [KIOSK.md](KIOSK.md) for setup and how to exit kiosk mode.

## Performance fixes 2026-08-26 (round A)
Prod `pg_stat_user_tables` showed `SystemSetting 72k / OrderItem 96M / Order 78M / Customer 34M` seq reads — root causes were `SELECT * FROM SystemSetting` on every `GET /api/dresses?eventDate=` and `Customer LIKE '%...%'` without trigram. Fixes:
- `lib/settingsCache.js` — 30s TTL in-memory cache for all `SystemSetting` reads (`getAllCachedSettings`/`getCachedSetting`), bypassed inside `$transaction` (`isDefaultClient` check), invalidated in `app/api/settings/route.js:143` alongside `require_login`. Patched 9 hot paths: `lib/inventory.js` (4 sites), `lib/pricingEngine.js:388`, `app/layout.js:61` (SSR), `app/api/dresses/route.js:14`, `app/api/orders/route.js:54`, `app/api/inventory/alerts/route.js:14`, `app/api/orders/calculate/route.js:15`.
- DB prod: `CREATE EXTENSION pg_trgm` + 4 GIN trigram indexes `Customer(firstName/lastName/phone1/phone2)` + 5 btree partial indexes `Order(fromDate/toDate/eventDate)` / `OrderItem(barcodePrefix,isReturned)` — `CONCURRENTLY` (see `prisma/schema.prisma:282,387` comments, full SQL in `docs/perf-round-a-summary-2026-08-26.md`).
- `pg_stat_statements` left enabled (was reset 17:36, now accumulating; query after 30m peak: `SELECT query,calls,total_exec_time FROM pg_stat_statements ORDER BY total_exec_time DESC LIMIT 10`).
- Vercel `vercel.json:3` only `04:00 /api/health` cron — 11-15 UTC load is real writes (`n_tup_upd Order 177k`), not cron; runtime logs 13-15 must be pulled from dashboard `prj_ZZT2jYlvQZbQtN2evU5cfL3dOZzQ` (CLI fails `ByteString 1502`).
- Round B remaining — see `docs/perf-round-b-instructions-2026-08-26.md` (15 uncached `systemSetting` reads, `app/api/orders/route.js:219` unpaid N+1, `inventory/alerts` unbounded `findMany`, TX reads in `creditRefundSync`, etc.). Full round-A log: `docs/perf-round-a-summary-2026-08-26.md`.

## Windows `desktop.ini` pollution breaks git (2026-09-07)
The repo lives under `C:\Users\moshe\Desktop\גמח שמלות חדש\` — inside the real Windows Desktop folder, not a regular project directory. Windows Explorer periodically writes a `desktop.ini` shell-metadata file into every subfolder it touches there (folder icon/view customization), with no regard for `.git`. When enough of these land inside `.git/refs/**` (e.g. `.git/refs/heads/desktop.ini`, `.git/refs/remotes/origin/desktop.ini`), git's ref-loading treats each stray file as a malformed ref and `git fetch`/`git pull` fails outright with `fatal: bad object refs/desktop.ini` / `did not send all necessary objects` — a hard stop, not a warning.
- **Symptom:** `git fetch`/`pull` suddenly fails with the error above, with no code change to explain it.
- **Fix:** `find .git -iname "desktop.ini" -delete` (safe — these are pure Windows shell metadata, never real git content) then retry. Takes seconds.
- **It comes back:** Explorer regenerates `desktop.ini` continuously while the folder is open/browsed, including inside `app/api/**` route folders (harmless there, just clutter — but the `.git/refs` case is the one that actually breaks things). Don't bother `.gitignore`-ing them one by one; that doesn't stop `.git` internals from getting them too.
- **Real fix, not yet done:** move the project out from under `Desktop` entirely (e.g. `C:\Projects\gemach-app`) so Explorer stops treating every subfolder as a shell-customizable desktop location. Until that happens, re-run the cleanup above whenever `git fetch`/`pull` fails with a `bad object` error.

## Offline-sync prod-corruption bug fixed (2026-09-09)
`syncOfflineChangesIfAny()` ([app/lib/prisma.js](app/lib/prisma.js)) runs automatically on every server start whenever `@prisma/local-client` exists on the machine, and unconditionally upserted every locally-changed row into real Postgres prod via `cloudClient[model].upsert(...)` — with **no check that the cloud row wasn't already newer**, despite `lib/offlineSync.js`'s own comments claiming last-write-wins. A stale, frozen `prisma/gemach-local.db` (an old offline-mode seed last touched ~2026-07-22, never refreshed) got re-synced by a plain `npm run dev` restart and silently reverted ~91 `Employee` rows to that frozen snapshot — plaintext passwords, and for at least 3 employees, `email`/`roleId`/`hourlyWage`/`emailSuffix` too — with **no AuditLog entry**, since `upsert()` isn't one of the operations (`create`/`update`/`delete`) the audit-logging extension recognizes. Worse, `saveSyncState` was only called once at the very end of the full per-model loop, so a failure partway through discarded progress on every model, not just the one that failed — meaning the same stale data got re-uploaded on every subsequent server start.
- **Fixed** in `lib/offlineSync.js`: `syncModel` now `findUnique`s the cloud row and skips the upsert (logs it) when the cloud row exists and its `updatedAt` is `>=` the local row's — true last-write-wins, not blind overwrite. `saveSyncState` now runs after each model inside the loop in `runOfflineSync`, not only once at the end. Each row that IS synced also gets a manual `AuditLog` entry (`{field:{from,to}}` diff + note, `employeeId: NULL`) written directly by `offlineSync.js` itself, since `upsert()` still bypasses the automatic extension. Verified against isolated fake Prisma-like clients in a throwaway script (never against real prod) — no committed test suite exists for this yet.
- **Mitigation** (still in place, machine-local): `prisma/gemach-local.db` was renamed to `prisma/gemach-local.db.DISABLED-corrupting-prod-2026-09-09` so `syncOfflineChangesIfAny()` hits its "local db doesn't exist" catch branch and no-ops. This protects any process on **this checkout**; a different machine with its own separately-generated local client/db is only protected once it also pulls this fix (the fix alone makes even an un-renamed stale file safe to re-sync, since a newer cloud row is no longer overwritten) — the rename is now a belt-and-braces extra, not the only protection.
- **Before deciding to actually use offline mode again**: confirm this fix is deployed/pulled on that machine, then it's safe to rename `prisma/gemach-local.db.DISABLED-corrupting-prod-2026-09-09` back to `prisma/gemach-local.db` (or regenerate fresh via `npm run generate:offline` + `npm run offline:db-push` instead, if the old file's data is no longer wanted). If you don't plan to use offline mode, no action needed — leave it renamed.
- **Data recovery**: of the ~91 affected `Employee` rows, only 3 had recoverable `AuditLog` history to restore other diverged fields from: רחלי שפרינצלס (#125, legacyId 125 — email/roleId/password+pinHash restored, the password being her own clean self-chosen hash from her own audit history rather than a forced reset), משה שיינועטר (#126 — email only, his password history was too ambiguous to safely restore), פייגי בירנבוים (#183 — email/emailSuffix/hourlyWage). The other 88 have zero recoverable audit history for anything beyond the password; the existing `needsPasswordReset` flag/column in the employee list (`app/api/employees/route.js`, `app/employees/page.js`) already surfaces them for the standard manual reset-password/set-password flow — no further recovery is possible for those.

## Bulk-settings scripts must target-check their DB host (2026-09-08)
`scripts/seed_phase1_settings.js` (the "38 requests" feature seed) imports the shared `app/lib/prisma.js` client with no host safety check, unlike `scratch/write_new_gemach_settings.js` (which hard-aborts if the resolved host isn't the expected one). Running it against the default env silently wrote Neve-Yaakov-only business rules (`enable_alterations=false`, `max_items_per_order=6`, `hide_custom_spacing=true`) onto the **main gemach's own PROD DB**, while leaving Neve Yaakov's actual DB missing all 58 of the settings its own features depend on. Both sides were fixed 2026-09-08 (see the `org1-org2-settings-cross-contamination-fixed` memory for the full incident and remaining open item). **Any script that writes `SystemSetting` rows meant for one org only must verify the target connection's host before writing** — copy the pattern from `write_new_gemach_settings.js` (`if (!host.startsWith('expected-host')) throw ...`) rather than relying on whichever `.env` happens to be active.

## ErrorReportReply "open question" flag (2026-09-10)
`ErrorReportReply.isQuestion` (boolean, default `false`) marks a reply as an open question that genuinely blocks progress until the other side answers, as opposed to a plain status/log reply ("ראיתי, בודק" / "תוקן"). Set explicitly — never inferred from wording. `app/components/ErrorReportButton.js` uses the *last* reply's `isQuestion` to tint a thread's list row `var(--warning-tint)` and show a "ממתין לתשובה" badge/banner; the reply form has a matching checkbox. The automated fix-reports agent sets it via `scripts/error-report-reply.js --question` (see that script's header and `.claude/commands/fix-reports.md` step "התהליך, לכל דיווח פתוח בנפרד"). The column was added to **both** gemachs' DBs (`prisma db push` for the main one, `scratch/add_is_question_column_org2.js` for Neve Yaakov's separate DB — `prisma db push` only ever writes to whichever single DB `DATABASE_URL` points at). Full history: `docs/fix-protocol-error-reports.md` section 9.

## Agent PR-approval digest email (2026-09-10)
A twice-daily email (target ~17:00 and ~00:00 Israel time) lists open GitHub PRs the automated fix-reports agent opened (branch prefix `fix-reports/`) that are still waiting for a manual merge — combined across both gemachs in one email, since it's one shared repo and PRs aren't split per org. Logic lives in [lib/agentDigest.js](lib/agentDigest.js); the Vercel Cron endpoint is `app/api/cron/agent-digest/route.js`, wired from **two** `vercel.json` cron entries (`?slot=evening` at `30 14 * * *` UTC, `?slot=midnight` at `30 21 * * *` UTC). Because Vercel Cron schedules are fixed UTC and don't shift for Israel's DST, those two UTC anchors are a deliberate summer/winter midpoint — actual send time can drift up to ~30 minutes either side of the configured hour depending on the season; this is a known, accepted trade-off, not a bug.
- Reuses the existing `GH_DISPATCH_TOKEN`/`GH_DISPATCH_REPO` Vercel env vars (already used by `app/api/error-report/route.js` for `repository_dispatch`) to list PRs via the GitHub REST API — no new secret. Not yet live-verified end-to-end (no valid token available locally when this was built; verify the first real send after deploy).
- Recipients: `Employee` rows with `roleId===2` (programmer) and an email — same lookup the bug-report notification already uses. Today that's just Moshe on the main gemach.
- Gated by two `SystemSetting` rows, seeded via `scratch/seed_agent_digest_settings.js`, **intentionally only on the main gemach's PROD DB** (not Neve Yaakov's — enabling the same key there too would send the same digest a second time, since GitHub PRs aren't org-specific): `agent_digest_email_enabled` (boolean) and `agent_digest_email_hours` (text, comma list — which of the 2 fixed slots, `"17:00,00:00"`, are actually active; adding a genuinely new hour needs a new cron entry in code, this setting only toggles the existing ones).
- Skips sending during Shabbat **and Yom Tov** (not just Saturdays) via `isReligiousBlackoutNow()` in [lib/hebrewDate.js](lib/hebrewDate.js) — uses `@hebcal/core`'s real candle-lighting/havdalah times for Jerusalem (not a crude day-of-week check), so it correctly skips a winter Friday-17:00 slot that falls after candle lighting.
- Cron-job count risk: this added 2 new `vercel.json` cron entries (4 total with the pre-existing `/api/health` and `/api/cron/daily`). The Vercel plan was Hobby as of a 2026-08-09 check (which historically caps crons to once-daily invocations, and reportedly total job count) — not re-verified here since the local Vercel API token was expired. If the real deploy rejects/ignores the new crons for a plan-limit reason, the documented fallback is a local Windows Scheduled Task (same pattern as `GemachApp-NeonKeepAlive`/`GemachApp-ProdDbBackup`) hitting `/api/cron/agent-digest?slot=...` twice a day.
