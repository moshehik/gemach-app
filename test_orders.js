const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const orders = await prisma.order.count(); const customers = await prisma.customer.count(); const items = await prisma.orderItem.count(); console.log('Orders:', orders, 'Customers:', customers, 'Items:', items); } catch (e) { console.error(e); } finally { await prisma.$disconnect(); } } main();
