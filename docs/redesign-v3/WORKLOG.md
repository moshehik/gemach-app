# WORKLOG — redesign/v3-phase2-foundation

Autonomous agent run, started 2026-09-24 (scheduled task). No live user this run — see AGENT-QUESTIONS.md for anything ambiguous. All times UTC-ish (agent clock), branch `redesign/v3-phase2-foundation` off `redesign/v3-master-plan` @ 8dbd3c7.

Reference order followed: CONSTITUTION.md (binding) → DECISIONS.md → LIBRARY-MAP.md → MASTER-PLAN.md → diagnosis-2026-09-24/*.md → sketch/order-card-sketch-B.html + login-mock.html.

## Setup
- Fetched + checked out `redesign/v3-master-plan` @ 8dbd3c7d, branched `redesign/v3-phase2-foundation`.
- `npm ci` succeeds (472 packages). No DB/secrets in this environment (per instructions) — `npm run dev`/`next build` needing `DATABASE_URL` will not fully run; documenting honestly below rather than claiming browser verification I could not do.

## Verification note (real, ran successfully)
`npm ci` + `npx prisma generate` + `npx next build --webpack` **all succeed** in this
environment (no `DATABASE_URL`/secrets at all) — exit code 0, every route compiles
including `/v3-gallery`. The only "errors" printed are expected, pre-existing
`Dynamic server usage: ... used cookies()` notices for every route that reads the
auth cookie (correct Next.js behavior for a page behind login — it just means that
route is server-rendered on demand, not statically prerendered, which is what it
should be) and one pre-existing, harmless "`Module not found: '@prisma/local-client'`"
(the offline-mode client that CLAUDE.md documents as not generated by default — not
related to anything touched this session). **This means every commit in this run's
milestone log below is verified against a real production build**, not just eslint —
a stronger check than the task brief expected to be possible without a DB.
`next dev`/a real browser were still not available (no way to click through the
gallery visually), so nothing here claims "verified in a browser."

## Milestone log

### M1 — Scope A surgical fixes + Scope B token split — commit `5abb06c` (pushed)
- Fixed F1 (`:where()` on the button font-reset rule), F2 (html/body background scoped via `V3Page`-managed `data-v3-page` attribute + `tokens/base.css`), F3 (scoped Rubik counter-rule inside `[data-v3]`, `design-overrides.css` itself untouched).
- Topbar: confirmed via diagnosis 01 + reading `components.css:1005-1092` against the sketch's `.snav` block that they already match; no CSS changed there (see AGENT-QUESTIONS Q-2).
- Split `app/v3/tokens.css` into `app/v3/tokens/{primitives,semantic,scale,overlay.dark}.css` per LIBRARY-MAP §1, zero value changes (verified with `postcss.parse()` on every resulting file — all parse cleanly, no stray `*/` truncation). Old `tokens.css` is now a 6-line `@import` shim.
- Added the missing names from the constitution's mandatory ~40-token L1 set (all aliases, no new colors), the new `--v3-lz-*` LayerManager z-scale (kept separate from the legacy `--v3-z-*` scale to avoid silently changing values still consumed by `components.css`/`ui/Dialog.js`), fresh `--v3-ov-*` overlay tokens for the overlay system built next, `breakpoints.js`, `contrast.js`.
- Built `tokens/legacy-bridge.css` but deliberately did not wire it in yet (see AGENT-QUESTIONS Q-4) — no live effect until someone imports it after a visual check.
- Verification this milestone: `npx eslint` on changed JS (clean), `postcss.parse()` on every changed/new CSS file (clean), manual brace/comment-count sanity pass. No browser, no `next build` yet (coming after more of the library exists — see below).

### M2 — Scope D icons/motion — commit `59a8d8a` (pushed)
- Extracted `ICON_ALIASES`/`ICON_ANIM`/`resolveIconId` into `app/v3/icons/aliases.js` (re-exported from `ui/Icon.js` for compatibility, zero external consumers of those names before this so nothing broke). Added `DIRECTIONAL_ICON_NAMES` (closed list from CONSTITUTION §ה.5). Added missing sprite symbols (`undo`, `redo`, `cart`, diagnosis 02 §1.3 O14). Caught and reverted my own mistake mid-pass: aliasing `expand`/`collapse` to horizontal chevrons would have shadowed a real, distinct, already-correct `i-expand` sprite symbol (vertical accordion toggle) — removed before committing.
- New `app/v3/motion/`: `presets.css` (named `v3-m-*` classes for pop/row-in/fade-up/dialog-in/sheet-up/draw/gold-sweep/ring-travel/shake/pulse/expand — every entry keyframe uses `from` only, never `animation-fill-mode: backwards/both`, so a browser that never runs the animation still shows the element in its normal, fully-visible state — CONSTITUTION §ה.4's "no content hiding" rule), `useReducedMotion()`, `AnimatedNumber` (tween preset).

### M3 — Scope C: LayerManager built + proven in the gallery — commit (this push)
- New `app/v3/overlays/`: `LayerManager.js` (`LayersProvider` + `useLayers()`), `Popover.js`, `Tip.js` (+`InfoTip`), `overlays.css`, `index.js`. Implements CONSTITUTION §ד: single modal stack (max 3), portal to `#v3-layer-root` (a direct child of `body`, created on demand), scroll lock via the existing `html[data-v3-lock]` mechanism from `base.css`, `inert`+`aria-hidden` on background siblings while any modal is open, focus trap, Esc closes only the top layer, focus returns to the trigger on close.
- Kinds implemented: **confirm** (with an optional third "alt" button, danger styling), **code/PIN** (approver picker + the existing `CodeInput` from `ui/Dialog.js`, reused rather than duplicated), **form** (data-entry, always light per D-2 even if the confirm/code kinds are following a dark site theme), **sheet** (L size), **busy** (blocking, `autoCloseAfter` or closed programmatically via the returned promise's resolution path). Sizes S/M/L/full map to `--v3-lyr-w-*` tokens (new, additive — doesn't touch the old `--v3-dlg-w*` still used by `ui/Dialog.js`). Phone: confirm/code/S/M become a bottom sheet; form/L/full become full-screen (matches D-6).
- **Toast**: bottom-left physical corner (documented exception per R21/§ד.6), stack of up to 3, 6.5s default / 2.6s for `kind:'info'`, timer pauses on hover/focus and — via `modalOpenCount` from the same provider — while any modal layer is open.
- **NoticeBar**: below the topbar, centered, gold draining bar (`animation-duration` = the notice's own duration, default 15000ms per §ד.7), same pause behavior; only one notice shown at a time, the rest queue (dedup beyond "just show the next one" was not built — see below).
- **Popover**: anchored via `getBoundingClientRect`, flips up if it would overflow the bottom of the viewport, RTL-aware (aligns to the anchor's inline-start, i.e. its right edge in RTL), collapses to a bottom sheet under 640px, light-dismiss (Esc + outside click).
- **Tip/InfoTip**: hover (150ms delay) / focus / touch-tap-toggle, portal-positioned so it's never clipped by a scrolling ancestor, `InfoTip` is a 44×44 `ⓘ` button with a required `label`.
- Wired into `app/v3-gallery/Gallery.js` as a new "LayerManager" section, demonstrating every kind — `LayersProvider` wraps **only the gallery**, not `app/layout.js` (see AGENT-QUESTIONS Q-1 for why).
- **Not built this pass** (explicitly deferred, matches MASTER-PLAN Phase 1.1's own "דחוי במכוון לשלב 4" list): a full Combobox, a Drawer (side navigation panel), multi-notice queue/dedup logic beyond FIFO, the `legacyShim.js` that would route `window.customConfirm`/etc. to this new system (Phase 5, needs the manual-conversion work LIBRARY-MAP §7 describes for the synchronous `window.confirm`/`prompt` call sites).
- Verification: `npx eslint app/v3 app/v3-gallery` — clean for everything touched this session. Running eslint across the *whole* `app/v3` tree also surfaced 3 real, pre-existing "cannot access ref/call impure function during render" errors in files this session never touched (`notify/V3NotifyProvider.js`, `ui/Dialog.js`, `ui/Tip.js` — the **old**, still-live Dialog/Tip, not the new ones built this pass). A full-repo `npx eslint .` shows the same class of pre-existing error in 19 files total (11 errors, 16 warnings baseline, `exit 0` because eslint's own severity config doesn't fail the process) — confirms these predate this session and are out of this run's declared scope (touching the *old*, currently-live `ui/Dialog.js`/`ui/Tip.js` without a way to verify in a browser is exactly the kind of risk the task brief asked me to avoid). Left untouched; listed here for whoever does Phase 5's real Dialog migration.

### M4 — Scope E: strings + org-config skeletons, with real gallery usage
- `app/v3/strings/`: `he.js` (the default dictionary, `<area>.<component>.<role>` keys, a real starter set covering `common`/`dialog`/`notice`/`toast`/`order`/`customer`/`tip`), `index.js` (`t()` for non-React use, `useStrings()` which layers the existing `LabelsContext` org-override on top — falls back gracefully with no provider, same pattern `useLabels()` already uses, so it can't crash a component that forgets to wrap one).
- `app/v3/config/`: `settingsRegistry.js` (the catalog `useOrgConfig()` is allowed to read from — `getSettingDef()` throws on an unregistered key, which is the mechanism the future `no-hebrew-literal`/lint #12 rule needs), `OrgConfigProvider.js` (`flag/num/text/list/json` getters; live mode fetches `GET /api/settings` through the existing shared `apiCache`; `profile` mode dynamically imports a static JSON file instead, for the gallery/screenshots), `profiles/{org1,org2,minimal,extreme}.json`.
- **Important limitation, stated honestly**: this environment has zero DB credentials (no `DATABASE_URL`/`TEST_DATABASE_URL` for either org, by design — a cloud agent session never gets secrets). CONSTITUTION §ט.4 requires the org1/org2 profiles to come from a **read-only `SELECT` against the live DB**, done by a local session with real access, logged in `ACCESS-LOG.md`. That was not possible here. Instead, `org1.json`/`org2.json` are a faithful transcription of the org-variance table **already committed in `CONSTITUTION.md` §ט.3** (itself sourced from `CLAUDE.md`/`docs/refund-swap-policy-2026-09-22.md`/`contracts/orders-id.md §ז` — i.e. documented business facts, not secrets, and not a guess). Both files say so in their own `_comment` field. **Whoever has real DB access should verify these against a live `SELECT key,value FROM "SystemSetting"` before treating them as ground truth** — flagged in AGENT-QUESTIONS too.
- Real usage wired into the gallery (not just built and left unused): a new "strings/ + config/" section renders the *same* component (`OrgConfigReadout`, which reads `useOrgConfig()` for `enable_deliveries`/`delivery_price`/`max_items_per_order`/`require_customer_id_number` and `useStrings()` for its title and a field label) under all four profiles side by side — this is literally the org-variance check CONSTITUTION §ט.4 asks every page to do, demonstrated on a toy example ahead of the real pilot page.
- Verification: `npx eslint app/v3/strings app/v3/config app/v3-gallery` clean; `npx next build --webpack` still exits 0 (confirms the dynamic `import(`./profiles/${profile}.json`)` pattern actually bundles correctly under webpack, not just "looks right").

### M5 — Scope F (partial): mechanical lint scripts, real baseline, gallery already extended in M3/M4
- `tools/v3-lint/`: 5 of the ~19 enforcement rules from CONSTITUTION §ח.1, as standalone Node scripts (no new build dependency — `stylelint` isn't installed and I didn't want to add a new devDependency without being able to verify it doesn't collide with anything else in the toolchain): `check-hex.mjs` (#1, no hex/rgb outside `tokens/`), `check-media.mjs` (#11, every `@media` width must be one of 480/640/768/1024/1440/1920 — imports the real `BREAKPOINTS` object from `tokens/breakpoints.js` so the two can't drift), `check-native-dialogs.mjs` (#2, bare/`window.` `alert`/`confirm`/`prompt` banned — correctly does **not** flag `layers.confirm(...)`, verified explicitly), `check-arrows.mjs` (#5, raw `chevron-start`/`chevron-end`/`arrow-start`/`arrow-end` icon names or literal arrow glyphs in JSX text are banned outside the alias/sprite definition files), `check-font-size.mjs` (#4, CSS `font-size` must be `var(--v3-fs-*)`/`inherit`, JS `fontSize:` must not be a bare number).
- Wired as `npm run lint:v3-hex|v3-media|v3-dialogs|v3-arrows|v3-fontsize|v3` in `package.json`.
- **Ran them for real against the actual `app/v3/**` tree** (not just against my own new files) and found genuine pre-existing violations: 8 `#000` inside CSS `mask`/`-webkit-mask` declarations (a transparency technique, not a visible color — diagnosis 01 §7 already called this out as an accepted exception), 7 old breakpoint values in the not-yet-migrated `components.css`, 3 hardcoded `font-size` px values predating the token scale, and 5 arrow-glyph hits (one a script false-positive on an inline code comment, documented as such rather than "fixed" by writing a full comment tokenizer; four are the `HistoryFeed`/`adapter.js` "before ← after" text that is exactly what CONSTITUTION §ה.5's own `<Range>` exception describes, for a `<Range>` component that doesn't exist yet). Recorded all of them in `lint-baseline.json` **with a `reason` on every entry** (CONSTITUTION's own "progressive enforcement" mechanism, §ח.1's closing paragraph) rather than either suppressing them silently or trying to fix code outside this run's `app/v3`-only scope without a browser to check the result. Also found and fixed one real bug of my own this way: `overlays.css` had a hard-coded gradient duplicating `--v3-grad-navy-float` byte-for-byte — replaced with the token.
- Debugged and fixed two real regex bugs while writing these (documented as commit-message detail, not just "it works now"): a negative-lookahead-inside-loop bug in the font-size checker that silently let the regex re-attempt matching one character later and bypass the exclusion entirely, and a global-flag-`.test()`-in-a-loop statefulness trap in the hex checker.
- **Gallery** ("Extend `/v3-gallery` so it doubles as living documentation"): rather than build the full auto-generating `*.gallery.js` + `gallery/build.mjs` machinery LIBRARY-MAP §8 describes (a substantial, separate piece of infrastructure), extended the existing hand-written `app/v3-gallery/Gallery.js` with real, working sections for everything built this run (LayerManager in M3, strings/org-config in M4) — every primitive is demonstrated in a real, interactive state, not just described. The auto-generator remains future work, documented in `tools/v3-lint/README.md`'s "מה עוד חסר" section and here.
- **Not built this pass** (also documented in the README rather than silently skipped): lint rules #3, #6, #7, #8, #9, #10, #12, #13, #14, #15, #16, #19 from CONSTITUTION §ח.1's full list; a real `stylelint` config; Playwright/axe rules (#17/#18 — need a browser, explicitly out of reach this run).
- Verification: every script runs clean (`exit 0`) via both direct `node` invocation and `npm run lint:v3`; `npx eslint tools/v3-lint/*.mjs` clean; `npx next build --webpack` still exits 0 after the `overlays.css` fix.

### M6 — Scope G: the pilot archetype prototype — **this is what the owner should look at first**
- Read the real `/customers/[id]` code end to end (`app/customers/[id]/page.js` + all 6 `components/customers/modern/Modern*.js` tab components) and extracted every field, action, permission check, and API-driven state into a numbered functional checklist: `docs/redesign-v3/prototypes/archetype-detail-card.contract.md` (`P-01`…`P-62`), grouped by hero/rail/5 tabs/dialogs, with an org-variance matrix and an explicit "what's deliberately excluded and why" section (no fabricated block-customer action, no wired change-engine — matches CONSTITUTION §ז.4's "read-only list until D-7 is settled").
- Built `docs/redesign-v3/prototypes/archetype-detail-card.html`: one self-contained file (~1000 lines), only Google Fonts as an external resource, in sketch-B visual language (navy/sky/gold, the same token *values* as `app/v3/tokens/`, not re-derived), implementing the A2 "detail card" archetype from CONSTITUTION §ו: topbar, page head with a back control, hero card (avatar, chips incl. the ID-display rule — **only ever shows `legacyId`, the ID chip is simply omitted if it's missing, never falls back to showing the internal UUID like the real page's `customer.legacyId || customer.id` does** — a deliberate cosmetic fix noted in the contract, not a behavior change since nothing server-side changes), 5 tabs with a count badge, label-over-value rows, a sticky rail (glance tile + a read-only change-list shown with per-row undo icons **visually**, matching sketch B's language, while the contract is explicit that the real undo/redo engine is out of scope until D-7/§ז.6 is resolved by the owner), light/dark-following confirm and code/PIN dialogs, an always-light quick-mail form dialog (D-2), a 3-way exit-with-unsaved-changes confirm (a real, intentional gap-fix vs. today's silent `router.back()` — flagged as such in the contract, not silently "improved"), a bottom-left toast, a top NoticeBar with a real CSS `scaleX` gold-draining-bar animation, and day-grouped history feed entries. A small "בקרות תצוגה לאישור" bar at the top (clearly marked as not part of the design itself) lets the owner open every dialog/toast/notice/dark-mode state and toggle the mobile rail without hunting through devtools.
- **Verified with a real headless browser, not just "should work"**: this environment does have Chromium pre-installed (`/opt/pw-browsers`) even though `playwright` isn't a project dependency — installed it locally with `npm install --no-save` (never touched `package.json`/`package-lock.json`, confirmed with `git diff --stat` after uninstalling), used it to load the actual file and:
  - Checked `document.documentElement.scrollWidth` against the viewport at all 7 required widths (360/480/640/768/1024/1440/1920) — **found a real horizontal-overflow bug** (invisible `opacity:0` tooltip boxes positioned off-screen were still counted in the document's scrollable area) and fixed it with `overflow-x:clip`/`hidden` on `html`/`body`, then re-verified all 7 widths show `scrollWidth === viewport width` with zero overflow.
  - Checked computed `direction` is `rtl`.
  - **Found and fixed a real RTL icon-direction bug of my own**: the page's own "back" button and the rail's "חזרה לרשימה" button used the LEFT-pointing chevron geometry (`m15 6-6 6 6 6`, correct for "next/forward" per C-1.11) instead of the RIGHT-pointing one (`m9 6 6 6-6 6`) a "back" action needs in RTL — exactly the class of bug `diagnosis-2026-09-24/04` documented as still-unfixed in the live app (F6/§2c). Fixed both, confirmed by re-screenshotting.
  - Exercised the interactive parts for real: tab switching, opening/closing the confirm dialog, and — the one that actually matters for R19/D-2 — toggling the page's dark-mode preview and confirming via screenshot that the confirm/code dialogs **do** follow it (navy surface, gold badge, white text) while the quick-mail form dialog **stays light** with its own on-page explanation text ("חלונית הזנת-מידע — בהירה תמיד, גם כשהאתר במצב כהה (D-2)") — this is a real, rendered, correct demonstration of the rule, not just CSS that looks plausible in the source.
  - `postcss.parse()` on the extracted `<style>` block and `new Function()` on the extracted `<script>` block both succeed (no stray `*/` in a comment, no JS syntax error).
  - All scratch verification scripts and the temporary `playwright` install were removed before committing — nothing extra was left in the tree (`git status --short` shows only the new `docs/redesign-v3/prototypes/` files).
- **Not done, stated honestly**: no screenshots were saved into the repo (the task didn't ask for that, and committing binary PNGs of a file the owner can just open directly seemed like clutter) — the verification above was run and its results are reported here, but the artifact to actually look at is the HTML file itself, opened in a real browser. Cross-checking against the **live** org1/org2 settings (CONSTITUTION §ט.4) for the one field that's genuinely org-dependent here (`require_customer_id_number`) was not done for the same no-DB-access reason as M4 — the contract's org-variance table says so explicitly instead of asserting it was checked.

