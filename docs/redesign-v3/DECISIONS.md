# DECISIONS — שאלות פתוחות למשתמש

לכל שאלה: **המלצה** (ברירת מחדל עד שתחליט), **מה חוסם**, ו-**מה הולך לקרות אחרת**. **עודכן 2026-09-24: המשתמש ענה על D-1…D-15** (הועתק ל-`IDEAS-LOG.md` #33). (הקודמות Q1–Q5 ב-`QUESTIONS.md` מאושרות כברירת מחדל; D-1 ו-D-2 מחדדות אותן.)

| # | שאלה | החלטה (2026-09-24) | חוסם | הערה |
|---|---|---|---|---|
| **D-1** | **מצב כהה בכל האתר?** האם העמודים עצמם נתמכים ב-dark (לא רק חלוניות)? | ✅ **"רק הצפים (כמו בהדמיה)"** — כהה רק לחלוניות צפות; עמודים בהירים בלבד. (הופך את ההמלצה; Q3 מאושר במפורש; אין בדיקות dark לעמודים.) _(המלצה מקורית: **כן, בתשתית** (כל token מוגדר בשני המצבים); הפעלה לפי עמוד בדגל. מחייב הרחבת Q3 ("עמודים בהירים בלבד"))_ | Phase 1 (tokens) | הבקשה המקורית: "dark mode built in". Q3 אושר בברירת מחדל בלי אישור מפורש. עלות: בדיקות S6 ×2 |
| **D-2** | **חלוניות הזנת-מידע בהירות בלבד** (תשלום/אשראי, מייל מהיר, חיפוש, pickers, הערה חופשית) גם כשהאתר כהה? חלונית אישור/קוד/PIN עוקבת ערכת נושא? | ✅ כן — חלוניות הזנת-מידע (תשלום/מייל מהיר/חיפוש/pickers/הערה חופשית) בהירות בלבד גם בהקשר כהה; confirm/code/PIN עוקבות ערכת נושא. _(המלצה מקורית: **כן** (כפי ש-R19))_ | Phase 1 (`Dialog`) | pickers ו"חיפוש" נכללים בבהיר בלבד; viewer עם שדה חיפוש = בהיר |
| **D-3** | **מודל השמירה ההיברידי**: ברירת מחדל היום = שמירה מפורשת בכרטיס (רייל+CTA); שדות קטנים בהגדרות/פרופיל = שמירה מיידית? | ✅ כן — מודל שמירה היברידי כפי שהומלץ. _(המלצה מקורית: **היברידי**: A2/A3 = שמירה מפורשת עם change-list; A7 (הגדרות) = שמירה מפורשת עם "נשמר" ב-NoticeBar; toggles בודדים במסך תצוגה = מיידית עם ביטול ב-toast)_ | Phase 2 (A2/A7) | R8: אין שינוי מצבי שמירה קיימים — זו שאלת מצג בלבד |
| **D-4** | **התמדת התראות**: להשלים את הענף `activity_note` ב-`POST /api/notifications` (לעצמי בלבד, בלי מייל, dedupe, `isRead:true`) ולסנן ב-`/messages`? עד אז `persistToBell:false`. שמירה **לעצמי בלבד** או גם למנהלים? כמה זמן נשמרות? | ✅/⚠ **תשובה לא ברורה — לאישור.** פורשה "לא להשלים/לא לשלוח": ענף `activity_note` לא ממומש עכשיו; `persistToBell` נשאר כבוי; אין שידור/מייל בשמירה. לא חוסם. _(המלצה מקורית: להשלים (לפי `reports/notify-api-patch.md`), לעצמי בלבד, 30 יום)_ | Phase 1.3ב; **חובה לפני הפעלת `persistToBell`** | נוגע ב-API (R8) — דורש אישור. הסיכון היום: שידור+מייל לכל העובדים בכל שמירה |
| **D-5** | **מיקום ההודעות**: (א) NoticeBar עליון עם פס זהב יורד 15ש' לשמירה+ניווט; (ב) toast שמאל-תחתון להודעות מערכת/פנימיות. תואם? (המסמכים הישנים תיארו את "נשמר" כטוסט שמאל-תחתון.) | ✅ כן. _(המלצה מקורית: כפי שבחוקה: (א)+(ב))_ | Phase 1.3 | בזמן שכבה מודאלית פתוחה הטיימרים מושהים |
| **D-6** | **חלוניות נבדלות ב-phone**: confirm/code = bottom-sheet או כרטיס ממורכז קטן? form = מסך מלא? | ✅ כן. _(המלצה מקורית: confirm/code sheet, form מסך מלא (כפי ש-`components.css` היום))_ | Phase 2 | |
| **D-7** | **כללי ההפיכות של מנוע השינויים** (R0–R3): (1) ביטול שינוי שאחרים תלויים בו — חסום / מפל / אזהרה; (2) redo אחרי שינוי חדש — למחוק מחסנית? (3) מיזוג עריכות חוזרות של אותו שדה; (4) התמדה בין ביקורים (drafts) ומיזוג עם `orderDrafts`; (5) כשל שמירה חלקי; (6) אילו פעולות נחשבות R2/R3 (תשלום, החזרה, אישור מנהל, מייל); (7) חלון ביטול מיידי (15/120 דק') — נכנס לרשימה? | ✅/הצעה — "עשה כפי שהבנת ותעד; נדון בהמשך". נכתבה מדיניות מוצעת ב-CONSTITUTION §ז.6 — **PROPOSED-AWAITING-REVIEW**; המנוע לא נבנה. _(המלצה מקורית: אין המלצה גורפת — לדון)_ | Phase 5ב | פרויקט נפרד. עד אז רשימה לקריאה בלבד |
| **D-8** | **פלטות משתמש**: לשמור בחירת פלטה לכל עובד (מעל v3) או לקבע navy/sky/gold? אם נשמר — dark נגזר אוטומטית (`deriveDark`) או המשתמש מגדיר שני מצבים? | ✅ **"כללי אין מותאם"** — פלטה כללית אחת קבועה (navy/sky/gold), בלי התאמה אישית; מערכת הפלטות האישית נזנחת בעמודי v3 (הקוד לא נמחק עכשיו). _(המלצה מקורית: לשמור; dark נגזר עם דחיית פלטה שנכשלת ב-AA)_ | Phase 1.2 | Q2: "קבוע + hook" אושר בברירת מחדל |
| **D-9** | **סדר ההמרה של עמודים** (Phase 4): להתחיל בהזמנות → שמלות → לקוחות כמוצע? לאשר לפי נתוני שימוש (`PageVisitLog`)? | ✅ אושר. _(המלצה מקורית: כפי שמוצע)_ | Phase 4 | |
| **D-10** | **דגלי פריסה**: `v3.page.<slug>` כ-`SystemSetting` לכל ארגון בנפרד? מי מפעיל ראשון (איזה ארגון)? לעובדים מסוימים בלבד? | ✅ אושר (דגל לארגון ב-DB של אותו ארגון). ⚠ **איזה ארגון ראשון — עדיין להחלטה** (לא חוסם עד יציאת Phase 3). _(המלצה מקורית: דגל לארגון; נווה יעקב או הראשי — **החלטתך**; החלפה בין ישן לחדש בזמן אמת)_ | Phase 3 יציאה | שינוי הגדרה רק ל-DB של הארגון (host-check) |
| **D-11** | **מילון מחרוזות**: האם לאכוף `no-hebrew-literal` (C-8.12) מיד כשגיאה בעמודי v3, או warn עד סוף Phase 4? מי מאשר ניסוחים חדשים? | ✅ "עשה כפי שהבנת" — error בעמודי v3 שהומרו, warn בשאר; המשתמש מאשר ניסוחי מסכים מרכזיים. _(המלצה מקורית: error בעמודים שהומרו, warn בשאר; המשתמש מאשר מלל של מסכים מרכזיים)_ | Phase 1.6 | R10 (לכתוב מחדש) |
| **D-12** | **הגישות לשני האתרים לאימות חי**: מתי תספק טוקנים/כניסת סוכן (`AGENT_LOGIN_SECRET` או מקביל) לשני הארגונים, ובאיזה היקף (קריאה בלבד)? | ✅ אושר: קריאה בלבד **עם יומן** — `ACCESS-LOG.md` (append בלבד; שם משתנה סביבה, לא ערך); סודות רק במשתני סביבה מקומיים. _(המלצה מקורית: קריאה בלבד; בסביבה מקומית; לעולם לא בקבצים)_ | אימות חי (Phase 3 שלב 5) | סודות לא נכתבים ל-repo |
| **D-13** | **ניהול (`/admin`,`/management`)**: לכלול בסוף Phase 4 או להשאיר בעיצוב הישן לצמיתות? | ✅ ניהול נדחה לשלב הבא, אחרי שכל Phase 4 הושלם. _(המלצה מקורית: לכלול אחרון)_ | Phase 4 | Q1 |
| **D-14** | **מסך רחב (≥1440)**: להגביל מיכל ל-1240/1440 ולהשאיר רקע (כמו כיום) או להרחיב טבלאות/לוח? | ✅/⚠ **"כמו במחשב שלי"** — פורש: שמירת התנהגות נוכחית (מיכל 1240, חריג טבלה/לוח רחבים). **לאישור המשתמש: מידות מדויקות.** _(המלצה מקורית: 1240 ברירת מחדל, 1440 לטבלאות/לוח)_ | Phase 2 | |
| **D-15** | **שינוי breakpoints**: איחוד 420/767/900/1020/1040/1180 לסט 480/640/768/1024/1440/1920 (מזיז נקודות שבירה קיימות)? | ✅ כן — איחוד breakpoints. _(המלצה מקורית: כן)_ | Phase 1.1 | הסרגל העליון יעבור ל-container/overflow |

## ⚠ לא ברור — דורש אישור המשתמש
1. **D-4** — התשובה ("לאהלשלוחף") לא ברורה; מיושם "לא להשלים/לא לשלוח" (persistToBell כבוי).
2. **D-10** — איזה ארגון מקבל את דגל v3 ראשון (נווה יעקב או הראשי).
3. **D-14** — מידות מדויקות של מיכל/מסך רחב (כרגע: כפי שהיום, 1240 + חריג 1440).
4. (הצעה פתוחה) **D-7** — מדיניות ההפיכות ב-CONSTITUTION §ז.6 ממתינה לשיחה.

## 2026-09-25 — עריכת עמוד הבית לבקשת הבעלים (ענף redesign/v3-home-edit)
- **D-16** | כרטיסי דף הבית ב-65% מהרוחב (כלל *עמודי* — חוזה home.md, לא חוקה); כפתורים/שורות בתוכם מוקטנים יחסית. | ✅ הבעלים.
- **D-17** | נוספו C-1.26..C-1.30 + S25..S29: כפתור-אחרי-תווית = שורה מונפשת עם פס זהב (`.lrow`); כותרת כרטיס navy + אייקון navy על זהב עשיר; כל שינוי רקע מונפש; סלמון = מבטא קטן (טון navy+זהב+תכלת); עובי מסגרת כרטיס = טוקן. | ✅ הבעלים ("קבוע"). מיושם בבלוק המעטפת (amendments A7-A15), לא לפי עמוד.
- **D-18** | תחתית משותפת (C-1.25) מיושמת כרכיב `mountSiteFooter` בבלוק המעטפת; נדבקת לתחתית בדף קצר (גוף = עמודה min-height:100dvh). | יישום של כלל קיים.
