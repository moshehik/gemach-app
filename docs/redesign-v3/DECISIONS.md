# DECISIONS — שאלות פתוחות למשתמש

לכל שאלה: **המלצה** (ברירת מחדל עד שתחליט), **מה חוסם**, ו-**מה הולך לקרות אחרת**. סמנו ✅ + החלטה ותאריך; הסוכן הראשי מעתיק ל-`IDEAS-LOG.md`. (הקודמות Q1–Q5 ב-`QUESTIONS.md` מאושרות כברירת מחדל; D-1 ו-D-2 מחדדות אותן.)

| # | שאלה | המלצה | חוסם | הערה |
|---|---|---|---|---|
| **D-1** | **מצב כהה בכל האתר?** האם העמודים עצמם נתמכים ב-dark (לא רק חלוניות)? | **כן, בתשתית** (כל token מוגדר בשני המצבים); הפעלה לפי עמוד בדגל. מחייב הרחבת Q3 ("עמודים בהירים בלבד") | Phase 1 (tokens) | הבקשה המקורית: "dark mode built in". Q3 אושר בברירת מחדל בלי אישור מפורש. עלות: בדיקות S6 ×2 |
| **D-2** | **חלוניות הזנת-מידע בהירות בלבד** (תשלום/אשראי, מייל מהיר, חיפוש, pickers, הערה חופשית) גם כשהאתר כהה? חלונית אישור/קוד/PIN עוקבת ערכת נושא? | **כן** (כפי ש-R19) | Phase 1 (`Dialog`) | pickers ו"חיפוש" נכללים בבהיר בלבד; viewer עם שדה חיפוש = בהיר |
| **D-3** | **מודל השמירה ההיברידי**: ברירת מחדל היום = שמירה מפורשת בכרטיס (רייל+CTA); שדות קטנים בהגדרות/פרופיל = שמירה מיידית? | **היברידי**: A2/A3 = שמירה מפורשת עם change-list; A7 (הגדרות) = שמירה מפורשת עם "נשמר" ב-NoticeBar; toggles בודדים במסך תצוגה = מיידית עם ביטול ב-toast | Phase 2 (A2/A7) | R8: אין שינוי מצבי שמירה קיימים — זו שאלת מצג בלבד |
| **D-4** | **התמדת התראות**: להשלים את הענף `activity_note` ב-`POST /api/notifications` (לעצמי בלבד, בלי מייל, dedupe, `isRead:true`) ולסנן ב-`/messages`? עד אז `persistToBell:false`. שמירה **לעצמי בלבד** או גם למנהלים? כמה זמן נשמרות? | להשלים (לפי `reports/notify-api-patch.md`), לעצמי בלבד, 30 יום | Phase 1.3ב; **חובה לפני הפעלת `persistToBell`** | נוגע ב-API (R8) — דורש אישור. הסיכון היום: שידור+מייל לכל העובדים בכל שמירה |
| **D-5** | **מיקום ההודעות**: (א) NoticeBar עליון עם פס זהב יורד 15ש' לשמירה+ניווט; (ב) toast שמאל-תחתון להודעות מערכת/פנימיות. תואם? (המסמכים הישנים תיארו את "נשמר" כטוסט שמאל-תחתון.) | כפי שבחוקה: (א)+(ב) | Phase 1.3 | בזמן שכבה מודאלית פתוחה הטיימרים מושהים |
| **D-6** | **חלוניות נבדלות ב-phone**: confirm/code = bottom-sheet או כרטיס ממורכז קטן? form = מסך מלא? | confirm/code sheet, form מסך מלא (כפי ש-`components.css` היום) | Phase 2 | |
| **D-7** | **כללי ההפיכות של מנוע השינויים** (R0–R3): (1) ביטול שינוי שאחרים תלויים בו — חסום / מפל / אזהרה; (2) redo אחרי שינוי חדש — למחוק מחסנית? (3) מיזוג עריכות חוזרות של אותו שדה; (4) התמדה בין ביקורים (drafts) ומיזוג עם `orderDrafts`; (5) כשל שמירה חלקי; (6) אילו פעולות נחשבות R2/R3 (תשלום, החזרה, אישור מנהל, מייל); (7) חלון ביטול מיידי (15/120 דק') — נכנס לרשימה? | אין המלצה גורפת — לדון | Phase 5ב | פרויקט נפרד. עד אז רשימה לקריאה בלבד |
| **D-8** | **פלטות משתמש**: לשמור בחירת פלטה לכל עובד (מעל v3) או לקבע navy/sky/gold? אם נשמר — dark נגזר אוטומטית (`deriveDark`) או המשתמש מגדיר שני מצבים? | לשמור; dark נגזר עם דחיית פלטה שנכשלת ב-AA | Phase 1.2 | Q2: "קבוע + hook" אושר בברירת מחדל |
| **D-9** | **סדר ההמרה של עמודים** (Phase 4): להתחיל בהזמנות → שמלות → לקוחות כמוצע? לאשר לפי נתוני שימוש (`PageVisitLog`)? | כפי שמוצע | Phase 4 | |
| **D-10** | **דגלי פריסה**: `v3.page.<slug>` כ-`SystemSetting` לכל ארגון בנפרד? מי מפעיל ראשון (איזה ארגון)? לעובדים מסוימים בלבד? | דגל לארגון; נווה יעקב או הראשי — **החלטתך**; החלפה בין ישן לחדש בזמן אמת | Phase 3 יציאה | שינוי הגדרה רק ל-DB של הארגון (host-check) |
| **D-11** | **מילון מחרוזות**: האם לאכוף `no-hebrew-literal` (C-8.12) מיד כשגיאה בעמודי v3, או warn עד סוף Phase 4? מי מאשר ניסוחים חדשים? | error בעמודים שהומרו, warn בשאר; המשתמש מאשר מלל של מסכים מרכזיים | Phase 1.6 | R10 (לכתוב מחדש) |
| **D-12** | **הגישות לשני האתרים לאימות חי**: מתי תספק טוקנים/כניסת סוכן (`AGENT_LOGIN_SECRET` או מקביל) לשני הארגונים, ובאיזה היקף (קריאה בלבד)? | קריאה בלבד; בסביבה מקומית; לעולם לא בקבצים | אימות חי (Phase 3 שלב 5) | סודות לא נכתבים ל-repo |
| **D-13** | **ניהול (`/admin`,`/management`)**: לכלול בסוף Phase 4 או להשאיר בעיצוב הישן לצמיתות? | לכלול אחרון | Phase 4 | Q1 |
| **D-14** | **מסך רחב (≥1440)**: להגביל מיכל ל-1240/1440 ולהשאיר רקע (כמו כיום) או להרחיב טבלאות/לוח? | 1240 ברירת מחדל, 1440 לטבלאות/לוח | Phase 2 | |
| **D-15** | **שינוי breakpoints**: איחוד 420/767/900/1020/1040/1180 לסט 480/640/768/1024/1440/1920 (מזיז נקודות שבירה קיימות)? | כן | Phase 1.1 | הסרגל העליון יעבור ל-container/overflow |
