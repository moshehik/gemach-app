# חוזה פונקציונלי — ארכיטיפ A6 "מגע / עמדה" (עמדת לקוחות + שעון נוכחות)

> **מקור האמת:** הקוד בענף `main` @ `1060e1b` (פרודקשן) — `app/customer-interface/page.js` (1683 שורות), `app/customer-interface/kiosk.css`, `app/punch-clock/page.js`, והנתיבים שהם קוראים: `app/api/login`, `app/api/attendance`, `app/api/auth/device-status`, `app/api/employees` (GET), `app/api/customers` (POST), `app/api/dresses` (GET), `app/api/pricelists/categories`, `app/api/orders` (GET), `app/api/ai`, `app/api/settings`, `lib/customerValidation.js`, `lib/orderStatus.js`, `app/layout.js` (`isPublicKiosk`/`isPunchClock`), `KIOSK.md`.
> **שיטה:** המלאי חולץ ישירות מהקוד (שורות מצוינות `:NN` לפי `main`). **לא הועתקו markup/CSS/מלל.** בענף `redesign/v3-prototype-review` יש גרסאות מעוצבות-מחדש של שני העמודים — הן לא שימשו מקור (REVIEW-1: main = אמת). חוזים קודמים (`contracts/customer-interface.md`, `contracts/punch-clock.md`) נקראו **אחרי** החילוץ, רק כדי לחפש פער — לא נמצא פריט שחסר כאן.
> **אב-טיפוס:** `archetype-touch.html`. מיפוי IDs → חלקי האב-טיפוס: `README.md` §"A6".
> מקרא: 🔌 = קריאת API · ⚙ = תלוי הגדרה · 🔒 = שער הרשאה/זהות · 🐞 = התנהגות קיימת שנראית כבאג (מתועדת, **לא מתוקנת** — R8).

---

## א. עמדת לקוחות `/customer-interface`

### א.1 גישה ומעטפת
| ID | פריט | מקור |
|---|---|---|
| TK-1 | 🔒 העמוד פטור ממסך הכניסה (`isPublicKiosk` לפי `x-pathname`) גם כש-`require_login` דלוק. **אבל** כל ה-API שהוא קורא (`/api/dresses`, `/api/customers` POST, `/api/orders`, `/api/ai`) דורשים `checkAuth` — כלומר עובדים רק כש-`require_login` כבוי או כשעובד מחובר במחשב הזה. | layout.js:46,279; dresses/route.js:11; customers/route.js:117 |
| TK-2 | העמוד מוסיף ל-`body` מחלקת רקע ייעודית בכניסה ומסיר ביציאה. | :376-379 |
| TK-3 | כשהמסך **נעול** — הסרגל הגלובלי מוסתר (`body.hide-global-nav`); כשלא נעול — הסרגל הגלובלי של האתר מוצג מעל העמדה. | :365-372 |
| TK-4 | שני שלבים (`stage` 1/2) בסרגל עליון פנימי: "בחירת תאריך" ו"קטלוג ותוצאות"; לחיצה על כל שלב עוברת אליו בחופשיות (אין חסימה). שלב שעבר מסומן ✓. | :1015-1027 |
| TK-5 | שם העמדה בסרגל הפנימי = מחרוזת קבועה ("עמדת לקוחות"); אין שם ארגון. | :1013 |

### א.2 שלב 1 — תאריך האירוע
| ID | פריט | מקור |
|---|---|---|
| TK-6 | תאריך נבחר ברירת מחדל = היום (00:00). | :301-305 |
| TK-7 | לוח עברי מוטמע: 3 בוררים (יום באותיות, חודש עם אדר א'/ב' בשנה מעוברת, שנה בגימטריה: שנה קודמת + 30 קדימה). שינוי בורר = בחירה מיידית. | :208-236, :152-155, :78-95 |
| TK-8 | רשת חודשים: **3 חודשים רצופים** (הנוכחי + 2), ימי חודש קודם/הבא מעומעמים ולא לחיצים, "היום" מסומן, הנבחר מסומן; כל תא = יום עברי + מספר לועזי קטן. | :194-274 |
| TK-9 | ניווט חודש קודם/הבא (חצים). | :141-150, :240-246 |
| TK-10 | שורת תחתית: התאריך העברי המלא + "פרשת …" של השבת הקרובה; כפתור "ניקוי" = חזרה להיום. | :157-170, :275-280 |
| TK-11 | **לחיצה על יום ברשת או שינוי בורר → עובר מיד לשלב 2** (`onSelect` = `setSelectedDate` + `setStage(2)`). אין כפתור "הצג מלאי" (ההערה בקוד מזכירה אותו, אבל הוא לא קיים). | :1169-1175; הערה :97-99 |
| TK-12 | ⚙ כרטיס "רישום פרטים אישיים" — רק כש-`kiosk_customer_self_service='true'`. | :1181 |
| TK-13 | טופס רישום: שם פרטי*, שם משפחה*, טלפון* (`tel`, LTR, "נייד או קווי"), מייל (LTR; * כש-`require_customer_email`), עיר/רחוב/מספר בית (* כש-`require_full_address`), ⚙ תיבת הסכמה לדיוור (מוסתרת כש-`hide_marketing_consent_field='true'`; * כש-`require_marketing_consent`). **אין** שדה ת"ז ואין "טלפון נוסף". | :1207-1259 |
| TK-14 | ולידציה בצד לקוח (`getMissingRegFields`): שם פרטי/משפחה/טלפון תמיד; + כל כינוי ב-`mandatory_fields` (טקסט מופרד בפסיקים); + `require_customer_email`; + `require_full_address` (3 שדות); + דיוור. שגיאה = שורה אחת "שדות חובה חסרים: …" מעל הכפתור (לא בשדה). **אין בדיקת תבנית בצד לקוח.** | :585-622 |
| TK-15 | 🔌 `POST /api/customers` body = `{firstName,lastName,phone1,email,city,street,houseNum,marketingConsent}`. הצלחה → מסך "נרשמתם" + "מספר לקוח: {legacyId}" (אם חזר) + "רישום לקוח נוסף" (מאפס). 401 → הודעה "צריך עובד מחובר". אחרת → `data.error` של השרת כלשונו. חריגה → "שגיאת תקשורת". כפתור נעול בזמן שליחה. | :624-646, :1191-1205 |
| TK-16 | ⚙ השרת אוכף **יותר** מהלקוח: `mandatory_field_groups` (ברירת מחדל `[["phone2","email"]]` → בלי מייל הרישום **נכשל** כי אין בטופס טלפון נוסף), `require_customer_id_number` (**אין שדה ת"ז בטופס → בארגון שבו זה דלוק כל רישום נכשל**), `strict_mandatory_fields`, תבנית טלפון (`0` + 8–9 ספרות) / מייל. ההודעה חוזרת כמחרוזת אחת מופרדת בפסיקים. 🐞 | customers/route.js:120-178; customerValidation.js |
| TK-17 | ⚙ כרטיס "הזמנה עצמאית" — **stub מושבת** (כפתור disabled "בקרוב"), מוצג כש-`kiosk_customer_self_service='true'` (לא לפי `kiosk_allow_self_order`, שנקרא לתוך משתנה ולא בשימוש). 🐞 | :348, :1277-1291 |

### א.3 העוזר החכם (AI) — בשני השלבים
| ID | פריט | מקור |
|---|---|---|
| TK-18 | ⚙ פעיל רק כש-`hide_ai_features!=='true'` **וגם** `enable_ai_specific_employees!=='true'`. (בשרת: אנונימי מותר רק כש-`require_login` כבוי.) | :346; CLAUDE.md "checkAiAccess" |
| TK-19 | שלב 1, לפני שיחה: שדה חיפוש גדול ("לדוגמה: …") + כפתור שליחה. אחרי הודעה ראשונה: כרטיס צ'אט מלא (במקום השדה). | :1141-1161 |
| TK-20 | שלב 2: בסרגל התוצאות שדה "שאלו את העוזר"; אחרי שליחה — כרטיס צ'אט נפתח מעל הקטלוג; כפתור "העוזר פעיל — לסגירה". מעבר שלב סוגר את הצ'אט. שיחה נפרדת לכל שלב. | :414, :1044-1063, :1299-1303 |
| TK-21 | 🔌 `POST /api/ai` `{prompt, history, context}` — ה-context מגביל לשאלות הזמנות/מלאי/מחירים ומבקש תגיות `[FILTER:x]`/`[DATE:yyyy-mm-dd]`. תשובה → בועה; כשל → "שגיאה בחיבור"; חריגה → "שגיאת תקשורת". בזמן המתנה: שלוש נקודות; השדה והכפתור נעולים. | :439-474, :972-976 |
| TK-22 | תגית `[DATE:]` → כפתור "הצג מלאי לתאריך …" (קובע תאריך ועובר לשלב 2); `[FILTER:]` → כפתור "סנן והצג: …" (קובע חיפוש, תאריך אם יש, ועובר לשלב 2). התגיות מוסרות מהטקסט. | :891-944 |
| TK-23 | כפתור "שיחה חדשה" (מאפס את השיחה של השלב), "סגירה" (בשלב 2). השיחה נשמרת ב-`localStorage['ai_customer_chat']` (כתיבה בלבד — לא נקראת בחזרה). | :955-966, :429-433 |

### א.4 שלב 2 — קטלוג
| ID | פריט | מקור |
|---|---|---|
| TK-24 | 🔌 `GET /api/dresses?eventDate=<ISO>&limit=10000&filterStatus=active` בכל שינוי תאריך; סינון נוסף בלקוח: בלי `exitDateFromRepo`, רק דגמים עם פריטים. | :476-505 |
| TK-25 | 🐞 כשל טעינה (כולל 401 כש-`require_login` דלוק) → הרשימה ריקה → מוצג "לא נמצאו דגמים" (אין מצב שגיאה נפרד). | :491-500 |
| TK-26 | כותרת שורת תוצאות: "קטלוג שמלות זמינות", צ'יפ תאריך (עברי + לועזי), מונה "N דגמים · M פנויות" (M = סכום יחידות פנויות לפי אותו חישוב של הכרטיסים). | :1032-1042, :778-780 |
| TK-27 | כפתור "סינון ותצוגה" פותח/סוגר פאנל צד (סגור כברירת מחדל); נקודה מסמנת שיש סינון פעיל. | :332, :1065-1070 |
| TK-28 | פאנל: חיפוש חופשי (שם, מספר דגם מדויק, או "מידה 40" = התאמה מדויקת למידה; אחרת מידה שמכילה את המחרוזת) + רמז. | :1311-1323, :698-721 |
| TK-29 | ⚙ קטגוריות מחיר (🔌 `GET /api/pricelists/categories`) — תיבות סימון עם מונה דגמים; קטגוריה עם 0 מעומעמת. מוסתר כשאין קטגוריות. | :516-523, :1325-1343 |
| TK-30 | צ'יפים של מידות (כל המידות בקטלוג הלא-מסונן) עם מונה דגמים; בחירה מרובה. | :735-749, :1345-1362 |
| TK-31 | מתג "הצג גם מידות בלי מלאי פנוי" (ברירת מחדל כבוי). | :292, :1364-1367 |
| TK-32 | בורר צורת תצוגה: כרטיסים גדולים / רשימה (ברירת מחדל) / טבלה. | :293, :1369-1384 |
| TK-33 | "ניקוי כל הסינונים" (חיפוש+קטגוריות+מידות; לא מתג המידות ולא התצוגה). | :1386-1390 |
| TK-34 | גודל תצוגה: מחוון 50%–150% בצעדי 10%, נשמר ב-`localStorage['ka_zoom_level']`, חל על אזור התוצאות. | :296-300, :1072-1098 |
| TK-35 | מיון: לפי שם תצוגה, מספרי-טבעי. | :724-728 |
| TK-36 | שם תצוגה: שם הדגם; אם ריק או מתחיל ב"ללא שם" → מספר הדגם. "יש שם אמיתי" = לא ריק, לא "ללא שם", ולא זהה למספר הדגם. | :42-58 |
| TK-37 | ⚙ עיגול דגם: תמונה (thumb → נפילה לתמונה מלאה) כש-`hide_dress_images!=='true'`, אחרת ראשי תיבות (מספר עד 3 ספרות). | :18-35, :62-76 |
| TK-38 | חישוב זמינות לדגם: לכל מידה (ריק = "כללי") סופרים פריטים לא `notInUse/isDeleted/isUnusable`; פנוי = `quantity>0`. שורת זמינות: "N יחידות פנויות" או "אין יחידות פנויות לתאריך זה" (אייקון + מלל). | :760-774, :1489-1492 |
| TK-39 | גלולות מידה: "מידה · פנויות", מסומנות פנוי/לא; ריק → "אין מידות רשומות" / "אין מלאי פנוי לתאריך זה". | :1494-1513 |
| TK-40 | תצוגת רשימה: שם + תג מספר + תג קטגוריה (לא "כללי") רק כשיש שם אמיתי; כפתור הדפסת הדגם. כרטיסים: אותו + כפתור הדפסה בפינה. טבלה: 6 עמודות (תמונה, שם, מק"ט, קטגוריה, סה"כ פנוי, מידות). | :1411-1572 |
| TK-41 | מצבי טעינה ("טוען") וריק ("לא נמצאו דגמים מתאימים" + "ניקוי ונסו שוב"). | :1396-1410 |
| TK-42 | כלים בסרגל התוצאות: חזרה למערכת (לא נעול → `/`; נעול → חלון שחרור), חיפוש חדש (→ שלב 1), רענון מלאי, הדפסה (נעול → חלון אישור עובד להדפסה), נעילה/שחרור. | :1100-1133 |

### א.5 פרטי דגם (לא נעול בלבד)
| ID | פריט | מקור |
|---|---|---|
| TK-43 | 🔒 לחיצה על דגם (או על גלולת מידה) כשהמסך **לא נעול** → חלון "הזמנות – {דגם} (מידה X)"; כשנעול — לא קורה כלום. | :649-650, :1429, :1446, :1503-1506 |
| TK-44 | 🔌 `GET /api/orders?itemDetails=<שם>&modelBarcodePrefix=<מספר>&eventDateFrom=<תאריך−7>&eventDateTo=<תאריך+7>&filterStatus=all`; סינון מידה בלקוח לפי `description`. | :651-684 |
| TK-45 | חלון: רמז "טווח: שבוע לפני ואחרי"; טעינה; ריק; שורה = "הזמנה #orderId – שם לקוח", תאריך אירוע, סטטוס תשלום מ-`calculatePaymentStatus` (4 ערכים בלבד), קישור לפתיחת ההזמנה **בטאב חדש**. סגירה ב-✕ בלבד. | :1626-1680; orderStatus.js |

### א.6 נעילה ושחרור (מסך ללקוח)
| ID | פריט | מקור |
|---|---|---|
| TK-46 | "נעילת מסך ללקוח": סוגר את חלון ההזמנות, `isLocked=true`, מבקש מסך מלא (דפדפן). | :1122-1131 |
| TK-47 | בזמן נעילה: תפריט קליק-ימני חסום; יציאה ממסך מלא (Esc) → חלון שחרור נפתח מעצמו (אלא אם כבר פתוח או בזמן הדפסה מאושרת). | :381-403 |
| TK-48 | 🔒 חלון שחרור/אישור הדפסה: בחירת עובד (רשימה נפתחת פשוטה, 🔌 `GET /api/employees` — אנונימי מקבל רק id+שמות) + "קוד גישה" = **סיסמה חופשית** (שדה password עם הצגה). כותרת לפי הכוונה: "שחרור מסך" / "אישור עובד להדפסה". | :1579-1623, :507-514 |
| TK-49 | 🔌 `POST /api/login {employeeId, password}` — **נשלח תמיד כ-`password`, לא `pin`** → קוד מקוצר של מחשב מהימן **לא** מתקבל כאן (רק סיסמה מלאה, או קוד ישן שנשמר גלוי מהמערכת הקודמת). הצלחה **יוצרת session של העובד במחשב** (עוגיות). | :536-540; login/route.js |
| TK-50 | שגיאות: בלי עובד → "צריך לבחור עובד" (בלי קריאה); שרת → `data.message` (עובד לא נמצא / לא פעיל / אין סיסמה / סיסמה שגויה); חריגה → "שגיאת תקשורת". בזמן בדיקה הכפתור נעול. | :527-577 |
| TK-51 | הצלחה + כוונה "שחרור" → לא נעול + יציאה ממסך מלא. הצלחה + כוונה "הדפסה" → הדפסה (הכול או דגם בודד) **והמסך נשאר נעול**; אחרי 2.5 ש' ניסיון לחזור למסך מלא. | :544-569 |
| TK-52 | "ביטול" סוגר את החלון **ולא משחרר**. אין סגירה ב-Esc/רקע בקוד (אבל Esc מחוץ למסך מלא הוא בדיוק מה שפותח אותו). | :1615-1616 |

### א.7 הדפסה
| ID | פריט | מקור |
|---|---|---|
| TK-53 | הדפסת דוח זמינות: חלון חדש (חסום → הודעת "לאפשר חלונות קופצים"), טבלה: שם, מספר, כמות זמינה, מידות(זמין); סינון שהוחל; סיכום; הדפסה וסגירה. דגם בודד = אותו דוח לדגם אחד. | :784-888 |

### א.8 מה **אין** בקוד היום (חשוב לארכיטיפ)
| ID | פריט | מקור |
|---|---|---|
| TK-54 | **אין מצב idle / שומר מסך / חזרה אוטומטית אחרי חוסר פעילות** — אין טיימר חוסר פעילות בעמוד כלל (רק 2 `setTimeout` להדפסה). | grep :566, :889 |
| TK-55 | **אין קיפד** — כל קלט קוד הוא שדה טקסט/סיסמה חופשי (סיסמאות אינן רק ספרות). | :1603 |
| TK-56 | שכבת מערכת ההפעלה (`--kiosk` בדפדפן) — מחוץ לעמוד (`KIOSK.md`, `scripts/kiosk/launch-kiosk.bat`). | KIOSK.md |

---

## ב. שעון נוכחות `/punch-clock`
| ID | פריט | מקור |
|---|---|---|
| TK-57 | 🔒 העמוד פטור ממסך הכניסה (`isPunchClock`); הסרגל הגלובלי **מוצג** (אין `hide-global-nav` בעמוד הזה). | layout.js:47-51 |
| TK-58 | שעון חי: מתעדכן כל שנייה, שעות:דקות:שניות בעברית; לפני הטיק הראשון "…". | punch-clock :22-27 |
| TK-59 | 🔌 `GET /api/employees` (מטמון משותף `TTL.STATIC`) → רשימת עובדים פעילים (אנונימי = id+שמות). null = טוען. | :29-31 |
| TK-60 | 🔌 `GET /api/auth/device-status` → `{trusted}`. במחשב מהימן התווית משתנה ל"סיסמה או קוד מקוצר (4 תווים)". | :32-35, :181 |
| TK-61 | בחירת עובד: שדה חיפוש עם רשימה נפתחת (התאמת תת-מחרוזת, רגיש לרישיות); מצבי "טוען" ו"לא נמצא"; בחירה ממלאת את השם ומעבירה פוקוס לסיסמה; הקלדה אחרי בחירה **מבטלת** את הבחירה. | :38-39, :118-178 |
| TK-62 | שדה סיסמה (`dir=auto`, password + הצגה). **קלט חופשי** (הקוד המקוצר = 4 התווים האחרונים של הסיסמה, לא בהכרח ספרות). | :180-200; password/route.js `last4Of` |
| TK-63 | שני כפתורים גדולים: "כניסה" (ראשי) ו"יציאה"; נעולים בזמן שליחה. אין Enter-לשליחה (אין טופס). | :203-225 |
| TK-64 | ולידציה: בלי עובד שנבחר מהרשימה **או** בלי סיסמה → הודעה, בלי קריאה. | :42-45 |
| TK-65 | ⚙ ביציאה בלבד, כש-`laundress_return_check_on_exit='true'`: 🔌 `GET /api/settings` + `GET /api/orders?filterStatus=archive&limit=50` → אם יש הזמנות עם פריט שנלקח ולא הוחזר: חלון אישור "יש N משפחות שלא החזירו (לדוגמה: עד 5 שמות)… לוודא?" — ביטול → הודעה "היציאה בוטלה – בדקו החזרות" ואין רישום. שגיאה בבדיקה → נבלעת, ממשיכים לרישום. הבדיקה רצה **לפני** נעילת הכפתורים. | :48-64 |
| TK-66 | 🔌 `POST /api/attendance {employeeId, password, action:'IN'|'OUT'}`. הצלחה → "הכניסה/היציאה נרשמה", איפוס עובד+סיסמה, ההודעה נעלמת אחרי 3 ש'. כשל → "שגיאה: {error}". חריגה → "שגיאת תקשורת". | :69-94 |
| TK-67 | שגיאות שרת אפשריות: עובד לא נמצא (404), סיסמה שגויה (401), "קוד מקוצר רק ממחשב מהימן" (401, סיסמה ≤4 תווים במחשב לא מהימן), חשבון לא פעיל (403), "כבר נרשמה כניסה" (400), "אין משמרת פתוחה ליציאה" (400), שגיאת שרת (500). | attendance/route.js |
| TK-68 | 🐞 צבע ההודעה נקבע לפי "האם מכילה את המילה שגיאה" — לכן "בחרו עובד…" ו"היציאה בוטלה" מוצגות **בצבע הצלחה**. | :96 |
| TK-69 | 🐞 כש-`require_login` דלוק ואף אחד לא מחובר, `GET /api/orders` מחזיר 401 → בדיקת הכובסת מדלגת בשקט. | :54-57 |
| TK-70 | מסלול "בלי סיסמה" (רק לעובד המחובר) קיים בשרת אבל **העמוד לא משתמש בו** (דורש סיסמה תמיד). | attendance/route.js:100-109 |

---

## ג. מטריצת org-variance (§ט.3)
| חלק | הגדרות שמניעות | שורה חסרה = | מצב קצה שהאב-טיפוס מדגים |
|---|---|---|---|
| כרטיס רישום + "הזמנה עצמאית" | `kiosk_customer_self_service` | כבוי (מוסתר) | דלוק/כבוי |
| כוכביות ושדות רישום | `require_customer_email`, `require_full_address`, `require_marketing_consent`, `hide_marketing_consent_field`, `mandatory_fields` | לא חובה / מוצג | "הכול דלוק" (פרופיל קיצוני) |
| דחיית שרת ברישום | `mandatory_field_groups`, `require_customer_id_number`, `strict_mandatory_fields` | `[["phone2","email"]]` / כבוי / כבוי | הודעת שרת ארוכה חוזרת לטופס |
| עוזר חכם | `hide_ai_features`, `enable_ai_specific_employees`, `require_login` (שרת) | מוצג | מוסתר לגמרי בלי חור |
| תמונות דגם | `hide_dress_images` | מוצגות | ראשי תיבות |
| קטגוריות | נתונים (`PriceList.category`) | — | 0 / 2 / 9 קטגוריות, שם ארוך |
| מידות, דגמים | נתונים | — | 0 / 1 / 60 דגמים; שם דגם ארוך; 14 מידות |
| בדיקת כובסת | `laundress_return_check_on_exit` | כבוי | דלוק עם 0 / 3 / 12 משפחות |
| קוד מקוצר | מחשב מהימן (`TrustedDevice`, לא הגדרה) | לא מהימן | מתג "מחשב מהימן" |
| נעילה | אין הגדרה | — | — |
> **ערכי הפרופילים בבקרת ההדגמה הם דמה** ("ארגון א׳/ב׳") — לא אומתו מול DB (אין גישה בריצה הזו). אין בקוד העמודים שום הבדל בין הארגונים מלבד ההגדרות האלה.

## ד. UI-only מול "צריך נתונים/שרת" (רק איפה שהוכח)
| שינוי באב-טיפוס | סוג | הוכחה |
|---|---|---|
| מצב שגיאה נפרד לטעינת קטלוג (במקום "לא נמצאו") | UI בלבד | אותה קריאה; `res.ok` כבר זמין בלקוח (TK-25) |
| שגיאת רישום מוצגת ליד השדה הרלוונטי + סיכום | UI בלבד **לשדות שהלקוח בודק**; שגיאות שרת נשארות מחרוזת אחת (השרת מחזיר מחרוזת, לא מפתח שדה) | customers/route.js:176 |
| צבע הודעה בשעון לפי סוג (במקום חיפוש מילה) | UI בלבד | כל ההודעות נוצרות בעמוד (TK-68) |
| בחירת עובד ברשימה עם חיפוש גם בחלון השחרור | UI בלבד | אותה רשימה (TK-48) |
| הצגת קוד מקוצר בחלון השחרור | **צריך שינוי קריאה** (לשלוח `pin`) — לא מוצג כפעיל; מסומן כשאלה | TK-49 |
| מצב idle / שומר מסך | **שינוי התנהגות** (אין בקוד) — מוצג רק כ"הצעה" כבויה | TK-54 |
| שדה ת"ז/טלפון נוסף בטופס הרישום | **שינוי נתונים שנשלחים** — לא נוסף; הדחייה מוצגת כמו היום | TK-16 |
| כותרת עם שם הארגון בעמדה | צריך config (`useOrgConfig`) — לא נוסף; הכותרת נשארת כללית | TK-5 |
