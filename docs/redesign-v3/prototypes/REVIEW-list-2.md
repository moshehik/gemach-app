# REVIEW-list-2 — סבב שני (אימות התיקונים) · אב-טיפוס A1 רשימת הזמנות

> סוקר עצמאי, 2026-09-24. בסיס: `8e876d1` + `14d1d30` (הבונה) + תיקוני הסוקר מ-`REVIEW-list-1.md` בעץ העבודה (עוד לא ב-commit — הרכז מבצע commit).
> קבצים ששונו: `archetype-list.html`, `archetype-list.contract.md` (LI-20/35/45/46, LI-67 חדש, רשימת הבאגים, טבלת LQ-1…LQ-5).
> צילומי הסבב: `screenshots/review-list/` (12 קבצים, Playwright/Chromium headless).

## פסק דין: **PASS-with-minors**
אין חוסמים ואין מהותיים פתוחים. נשארים 13 קלים מ-REVIEW-list-1 (m-1, m-2, m-4…m-12, m-14, m-15), כולם לפיילוט או כשאלות עם ברירת מחדל. אפשר להתקדם לפיילוט של ארכיטיפ A1; אישור חזותי של הבעלים עדיין נדרש, כולל שאלות Q-L1…Q-L15 ו-LQ-1…LQ-5.

## אימות כל ממצא מ-REVIEW-list-1
| # | מה הורץ | תוצאה |
|---|---|---|
| M-1 | `flow.cjs`: לשונית "לא שולם", איסוף התגית השנייה בכל שורה + פתיחת השורה הראשונה | לפני: `לא שולם, שולם חלקי` + "שולם ₪0 / יתרת חוב" (`1440-05a-unpaid-before.png`). אחרי: אין תגית מצב תשלום בשום שורה, בשורה המורחבת "שולם — לא מוצג בלשונית הזו" + ⓘ, בלי יתרה (`1440-05b-unpaid-after.png`, `360-05-unpaid-after.png`). ✔ ב-4 הרוחבות |
| M-2 | `flow.cjs`: ערכי `#sortSel` | `eventDate, orderId, customerName, totalAmount, status` — אין `totalPaid`; ⓘ מופיע ל"סכום לחיוב שמור" ול"סטטוס שמור". ✔ |
| M-3 | `flow.cjs`: לשונית "מחוקות" — סטטוסים + כפתור מחיקה; `idt.cjs`: מחיקה מלאה | בלשונית: 5 "מחוק" (כל הפריטים מחוקים, בלי כפתור מחיקה) + 3 מחוקות-מיבוא עם סטטוס הפריטים ("הוחזר", "בקרוב") ועם כפתור מחיקה; אחרי מחיקה דרך החלון — `isDeleted` + כל הפריטים מחוקים, סטטוס "מחוק" (`1440-08-deleted-tab.png`). הסטטוס מחושב בלי `isDeleted` (`payload()`). ✔ |
| M-4 | `flow.cjs`: כפתורי האשף; "מה שמוצג עכשיו" על רשימה ריקה; לחיצה על PDF | כפתורים: "יצירת הדוח · הורדה כ-PDF · ביטול" (`1440-04-print-pdf.png`); ריק → "אין הזמנות ברשימה שמוצגת עכשיו…" בתוך החלון; PDF → טוסט "קובץ ה-PDF ירד". בדוח ההכנה (פרופיל ב׳) הכפתור מוסתר — קוד (`hidden` לפי `type`), נבדק בקריאה ולא בזרימה. ✔ |
| M-5 | `flow.cjs` + `idt.cjs` (פרופיל ב׳) | `maxlength`/`inputmode` = null; ערך של 14 תווים עם אות ומקף נשמר כמו שהוקלד ומקבל "לא תואמת" (תשובת 403 מדומה); `"  123456782 "` → נמחק (trim). ✔ (`360-06-id-dialog.png`) |
| m-3 | קריאת קוד | "לחיוב" דרך `label('order_totalAmount', …)` ✔ |
| m-13 | קריאת חוזה | LQ-3/LQ-5 מוצהרים ✔ |

## רגרסיה (הורץ)
| בדיקה | תוצאה |
|---|---|
| `check.cjs` סטטי | media בסט; פיזי = רק `left` של הטוסט (חריג §ד.6); 0 z-index מספרי; 0 `alert`; תו החץ היחיד שנשאר הוא בהערת JS של הבונה (לא מלל); `opacity:0` רק במצבי יציאה/קלט קובץ |
| 360/768/1024/1440 | 0 `pageerror`; אין גלילה אופקית (גם בפרופיל "קיצוני"); 0 כפתורים בלי תווית |
| שכבות | confirm כהה `alertdialog` עם scrim מעל הסרגל, נעילה + `inert`; Esc 2→1→0; הפוקוס חוזר ל"מחיקת ההזמנה"; סינון = בהיר; חלון סיסמת הייצוא = כהה בשכבה שנייה |
| NoticeBar / טוסט | מתחת לסרגל, פס זהב נעצר בריחוף; טוסט שמאל-תחתון |
| RTL | "הקודם" ימינה / "הבא" שמאלה (גאומטריית `path`); חץ שמאלה בלשוניות = הבאה |

לא רץ: axe, קורא מסך, מכשיר אמיתי, אימות ערכי הגדרות מול DB חי.

## קלים שנשארו (לפיילוט)
m-1 ✕ של pill 32px · m-2 `peri` בהדגשת ציפוף · m-4 ייצוא ריק · m-5 מלל הייצוא במצב AI · m-6 403 בחלון הסיסמה · m-7 400/403 במחיקה · m-8 דגמים בתוצאות AI · m-9 בדיקת "שולם" לעגלה בלי `status` · m-10 אין פרופיל עם `show_not_taken_orders=false` · m-11 tooltip על `span` · m-12 ⓘ של "בקרוב" מול ברירת 3 החודשים · m-14 מחוקות-מיבוא בלשונית "מחוקות" · m-15 ערכים גולמיים ב-CSS.

## שאלות פתוחות לבעלים (ברירת המחדל באב-הטיפוס)
| # | שאלה | ברירת מחדל |
|---|---|---|
| LQ-1 | "לא שולם" בלי תשלומים מהשרת | לא מציגים שם שולם/יתרה/מצב תשלום; תיקון שרת = פרויקט נפרד |
| LQ-2 | מיון "שולם" מחזיר 500 | לא מוצע בבורר |
| LQ-3 | חזרה לעמוד תקין אחרי סינון | כן |
| LQ-4 | מחיקה חוזרת בלשונית "מחוקות" | מוסתרת כשכל הפריטים מחוקים |
| LQ-5 | טוסט אחרי מחיקה/הדפסה | כן |
| OQ-2 | סטטוס מחוקות-מיבוא | כמו היום (סטטוס הפריטים) |
| Q-L5 | סינון מתקדם חל רק ב"הצגת התוצאות" | כמו שהבונה בחר (שינוי התנהגות, לאישור) |
