# יומן סקירת הארכיטיפים (A1, A3, A4, A5, A6, A7)

> סוקר עצמאי (לא הבונים), ריצה אוטומטית בלי בעלים זמין, 2026-09-24. ענף: `redesign/v3-protos-review` (נוצר מ-`origin/redesign/v3-prototype-review`, מוזגו אליו שלושת ענפי ה-proto).
> שיטה: כמו `REVIEW-1.md`/`REVIEW-2.md`. **קודם** נבנה מלאי פונקציונלי עצמאי מהקוד ב-`origin/main` @ `1060e1b` (לפני קריאת החוזים של הבונים) — נשמר ב-`review-inventories/INV-*.md`. אחר כך השוואה לחוזה ולאב-הטיפוס, בדיקת עיצוב מול סקיצה B והחוקה, תיקון כל חוסם/מהותי באב-הטיפוס ובחוזה, וסבב אימות שני. לכל ארכיטיפ: `REVIEW-<name>-1.md` + `REVIEW-<name>-2.md` + `screenshots/review-<name>/`.
> לא נגעתי בקוד האפליקציה, ב-API, ב-Prisma או בנתונים. לא נפרס דבר. אין סודות בקבצים.

## ציר זמן (UTC)
| שעה | אירוע |
|---|---|
| 10:23 | התחלה; אף ענף proto עוד לא קיים. נבנו 6 מלאים עצמאיים מ-`main` |
| 10:35–10:38 | נדחפו גרסאות ראשונות: list, board+dashboard, touch (+ חוזה forms) |
| 10:44 | forms נדחף |
| 10:46–10:50 | מוזג; סקירות list/board/dashboard/touch התחילו |
| 10:52–10:55 | הבונים דחפו wizard ותיקוני QA ל-list/touch/forms/board/dashboard — מוזגו תוך כדי סקירה (בהתנגשות נשמרה גרסת הסוקר, שכיסתה את אותם תיקונים; README — שני הצדדים) |
| 10:57 | סקירות wizard ו-forms התחילו |
| 11:08–11:14 | כל 6 הסקירות הסתיימו (2 סבבים כל אחת); כל אחת נבדקה שוב ע"י המתאם (בדיקה סטטית + 4 רוחבות) ונדחפה |

## סיכום
| ארכיטיפ | עמוד | commit שנסקר | סבב 1 | סבב 2 (סופי) | חוסם/מהותי/קל בסבב 1 | מה תוקן (עיקר) |
|---|---|---|---|---|---|---|
| A1 List | `/orders` | `8e876d1`+`14d1d30` | FAIL | **PASS-with-minors** | 0 / 5 / 15 | לשונית "לא שולם" הציגה סכומי תשלום שהשרת לא מחזיר (תמיד 0) → הוסתרו + ⓘ; מיון "סכום ששולם" (אין עמודה → 500) הוסר; `isDeleted` שלא מגיע בתשובה הוסר מחישוב הסטטוס; כפתור "הורדה כ-PDF" באשף ההדפסה הוחזר; בדיקת ת"ז לפני מחיקה קשוחה מהשרת → כמו השרת |
| A3 Wizard | `/orders/new` | `14d1d30` | FAIL | **PASS-with-minors** | 0 / 4 / 21 | בוחר מאשרים מסונן לפי פעולה (לקוח חסום = הנהלה ראשית/מתכנת בלבד); אמצעי התשלום נרשם מהבחירה כמו היום; כלל עיר משלוח כמו `deliveryValidation` + הוסרה אופציה מומצאת; לקוח חדש בלי קבוצת "לפחות אחד" = שגיאת שדה (השרת תמיד מחזיר 400) |
| A4 Board | `/board` | `85a82b1` | FAIL | **PASS-with-minors** | 0 / 5 / 16 | התנגשות כותרת תא-יום ב-1024–1439 בפרופיל נווה יעקב; שדות תאריך בחיפוש מתקדם שמוצגים כמסננים אבל לא מסננים (באג קיים) → מסומנים "לא מסנן"; סטטוס "מחוק" מומצא להזמנה בלי פריטים → לפי `calculateOrderStatus`; יעדי מגע 44; החזרת פוקוס |
| A5 Dashboard | `/`, `/dashboard` | `0962136`+`85a82b1` | FAIL | **PASS-with-minors** | 0 / 6 / 13 | גלילה אופקית 84px ב-360 בטבלת צ'אט; סגנונות תווית/ערך חסרים; מצב שגיאת שרת ל-`/dashboard`; טעינה הדרגתית לכל אריח; השלמות חוזה (DB-62…70); שערי הרשאה בהדגמה תוקנו |
| A6 Touch | `/customer-interface`, `/punch-clock` | `44aa6d9` | FAIL | **PASS-with-minors** | 0 / 6 / 23 | נראות העוזר החכם תוארה שגוי (אנונימי לעולם לא רואה); ב-360×640 נשארו 86px לקטלוג → הכותרת נגללת מתחת ל-768; אייקוני כניסה/יציאה הפוכים; axe: 0 serious/critical ב-18 מצבים; חלון כהה לא קריא (1.19:1); מצבי שגיאת שרת |
| A7 Forms | `/profile` | `44aa6d9` | FAIL | **PASS-with-minors** | 0 / 3 / 14 | ולידציית סיסמה נוכחית קשוחה מהשרת + רמז שתלוי בנתון שהעמוד לא מקבל → הוסר; 4 מצבי תוצאה (הצלחה/500/רשת/401 עם "התחברות"); toast עם פעולה נמעך ב-360 |

**אף ארכיטיפ לא הכיל חוסם.** כל הממצאים הקודמים מ-REVIEW-1 (קוד 4 ספרות, "אין חיוב", breakpoints מחוץ לסט, gold-700 כטקסט, מאפיינים פיזיים) — נמנעו ע"י כל הבונים. דפוס חוזר שנמצא בכמה ארכיטיפים: **נתון/סטטוס שהתשובה מהשרת לא מספקת** (A1, A4, A7) ו**ולידציה שונה מהשרת** (A1, A3, A7) — אותה משפחה של B-1/M-5 מהסקירה הקודמת.

### בדיקת המתאם אחרי כל סקירה (הורץ בפועל)
סקריפט אחיד (`check.cjs`, Playwright/Chromium headless) על כל 6 הקבצים הסופיים: 0 ערכי `@media` מחוץ לסט; מאפיינים פיזיים רק ב-`left` של ה-toast (חריג מתועד C-§ד.6); 0 `alert/confirm/prompt`; 0 זוגות token מתחת 4.5:1 מבין הזוגות שנבדקו; ב-360/768/1024/1440 — אין שגיאות JS ואין גלילה אופקית ואין אלמנט שחורג מהמסך (מלבד פקדי "בקרת הדגמה" קטנים, שאינם חלק מהעיצוב). כשל טעינת הגופן Rubik = רשת הסביבה (תעודת proxy), לא האב-טיפוס.
**לא רץ בשום ארכיטיפ:** קורא מסך, מכשיר מגע/iOS אמיתי, רוחב 1920, ערכי הגדרות מה-DB החי (פרופילי org בהדגמה). axe רץ רק ב-A6.

## מה הבעלים צריך לראות קודם
1. **אבטחה בפרודקשן (לא חלק מהעיצוב, נשלחה התראה):** `app/layout.js:343-353` — מיירט ה-fetch הגלובלי שומר גוף כל POST ב-`PageVisitLog.requestQuery`, כולל `/api/login` (מסך כניסה, שחרור עמדה), `/api/attendance` (שעון נוכחות) ו-`/api/auth/verify-pin` — כלומר **סיסמאות בטקסט גלוי** נשמרות ומוצגות בהיסטוריה. אומת בקריאת קוד בלבד (שני סוקרים בנפרד + המתאם), לא ב-DB.
2. **A3 OQ-W3** — שלושה פערים שבהם היום בדיקה נעקפת (קפיצה בסטפר עוקפת אישור לקוח חסום; בחירת לקוח לפי שם עוקפת בדיקת פרטים חסרים; חיוב אשראי מלא עוקף אישור תאריך עבר). ברירת מחדל: כמו היום עד החלטה (R8).
3. **A1 LQ-1/LQ-2** — לשונית "לא שולם" ומיון לפי "שולם" שבורים בשרת; ברירת מחדל: הסתרה ב-UI, תיקון שרת כפרויקט נפרד.
4. **A4 RB-Q1** + צילומי `screenshots/review-board/01`/`02` — השבירה הייתה דווקא בפרופיל נווה יעקב.
5. **A5 OQ-D2** (פרטיות: צ'אט AI וחיפושים אחרונים נשמרים בדפדפן משותף) ו-BDQ-D1 ("דורש טיפול" בבית — כבוי, בלי מספרים מומצאים).
6. **A7 FQ-2/FQ-6** — אזהרת יציאה עם שינויים (התנהגות חדשה) ותמונה בלי מגבלת גודל.

## שאלות פתוחות וברירת המחדל שנבחרה (מלא: בקבצי REVIEW-*-2 ובקבצי השאלות של הבונים)
| # | ארכיטיפ | שאלה | ברירת מחדל |
|---|---|---|---|
| LQ-1 | A1 | לשונית "לא שולם" — השרת לא מחזיר תשלומים | להסתיר שולם/יתרה/סטטוס תשלום בלשונית + ⓘ; תיקון שרת בנפרד |
| LQ-2 | A1 | מיון לפי "שולם" מחזיר 500 | להשמיט עד תיקון שרת |
| LQ-4 | A1 | מחיקה לטיוטות ולהזמנות מחוקות מהייבוא | זמינה כמו היום |
| Q-L5 | A1 | סינון מתקדם רק ב"הצגת התוצאות" (היום: כל הקשה) | כמו באב-טיפוס — **דורש אישור** (שינוי התנהגות) |
| OQ-W1 | A3 | לקוח חדש בלי קבוצת חובה | שגיאת שדה (התוצאה זהה להיום — 400) |
| OQ-W2 | A3 | "חיוב באשראי" מחליף את בחירת אמצעי התשלום לאשראי | כן |
| OQ-W3 | A3 | סגירת 3 עקיפות בדיקה | כמו היום עד החלטה |
| RB-Q1 | A4 | שדות תאריך שלא מסננים בחיפוש מתקדם | נשארים, מסומנים "לא מסנן"; תיקון `prefetchRoutes.js` בנפרד |
| RB-Q2 | A4 | מונה הזמנות ליום מתחת ל-1440 | מספר בלבד (+ tooltip) |
| RB-Q3 | A4 | כרטיס השכרה מעל חלון היום | נערם (היום: חלון היום נסגר) |
| RB-Q4 | A4 | כפתורי AI כשאין גישה | מוסתרים |
| BDQ-D1 | A5 | "דורש טיפול" בבית | כבוי, מקום שמור בלבד |
| OQ-D1 | A5 | תגיות AI גולמיות בצ'אט הבית | `[OPEN_LINK]` → כפתור קישור; `[FILTER]` מוסר (UI בלבד) |
| OQ-D2 | A5 | צ'אט/חיפושים בדפדפן משותף | כמו היום; מדווח כפער פרטיות |
| OQ-D3 | A5 | עמודת הזהב = התקופה האחרונה עם נתונים | נשאר, מקרא "התקופה האחרונה" |
| TQ | A6 | עוזר AI מול לקוחות; כותרת נגללת בטלפון; אישור הדפסה מחבר עובד לעמדה | כמו היום; כן מתחת ל-768; ללא שינוי + דווח |
| RQ-1 | A7 | הודעות שגיאת סיסמה | הודעת השרת (מבחינה בין שני סוגי 401) |
| RQ-3 | A7 | Enter בשדה סיסמה שולח את טופס הפרופיל | לא לשנות |
| FQ-2 | A7 | אזהרה ביציאה עם שינויים | דורש אישור בעלים |

## ממצאים בקוד הפרודקשן (לא תוקנו — מחוץ לתחום R8; אומתו בקריאת קוד בלבד)
| ממצא | אומת ע"י המתאם |
|---|---|
| סיסמאות גלויות ב-`PageVisitLog` (ראו למעלה) | ✔ `app/layout.js:343-353` |
| `/api/orders/calculate`, `/pricing`, `/validate-inventory` בלי `checkAuth` | ✔ 0 קריאות |
| `GET /api/settings` בלי בדיקת התחברות (סודות ממוסכים) | ✔ |
| `GET /api/dashboard`, `/api/dashboard/debts` — רק התחברות, העמוד להנהלה ראשית בלבד | ✔ |
| `GET /api/orders?sort=totalPaid` → שגיאה (אין עמודה; `sort` נכנס ל-`orderBy` כמות שהוא) | ✔ |
| לקוח חסום נאכף רק בדפדפן (`orders/route.js` בלי `isBlocked`) | ✔ |
| אין הגבלת ניסיונות ב-`/api/attendance`/`/api/login`; רענון עמוד משחרר את נעילת העמדה | קריאת סוקר |
| חיפוש AI בלוח מחפש בלקוחות → לוח ריק; תאריכי חיפוש מתקדם בלוח לא מסננים | קריאת סוקר |
| תמונת פרופיל base64 בלי מגבלת גודל, נכתבת במלואה ל-AuditLog בכל שמירה (עומס Neon) | קריאת סוקר |

## קבצים
- `REVIEW-{list,wizard,board,dashboard,touch,forms}-{1,2}.md`, `screenshots/review-*/`
- `review-inventories/INV-*.md` — המלאים העצמאיים מ-`main` (בסיס מטריצות העקיבות)
- אבות-הטיפוס והחוזים המתוקנים: `archetype-*.html`, `archetype-*.contract.md`
