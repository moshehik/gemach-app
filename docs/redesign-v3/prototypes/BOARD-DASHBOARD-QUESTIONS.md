# שאלות מבניית אב-הטיפוס A4 (לוח) ו-A5 (בית/סיכום) — וברירת המחדל שנבחרה

> נבנה בריצה אוטומטית בלי אפשרות לשאול (2026-09-24). בכל שאלה נבחרה האפשרות הבטוחה שתואמת את החוקה ואת הקוד הקיים, והיא מה שמוצג באב-הטיפוס. **כל שורה מחכה לאישור או לתיקון של הבעלים.**
> קבצים: `archetype-board.html` + `.contract.md` · `archetype-dashboard.html` + `.contract.md`. מקור האמת לפונקציות: `origin/main` @ `1060e1b`.

## לוח (A4)

| # | שאלה | מה נבחר באב-הטיפוס | למה | נוגע ב |
|---|---|---|---|---|
| BDQ-B1 | הארכיטיפ A4 דורש "מצב יום/שבוע/חודש" ו"בטלפון רשימה יומית". בלוח היום יש רק חודש. | נוספו **שבוע** ו**רשימה** (אג'נדה). בטלפון (<768) ברירת המחדל = רשימה; בטאבלט ובטלפון רשת החודש מציגה רק מספר יום + מונה, והקשה פותחת את חלון היום. | אותה קריאת חודש כבר מכילה את כל הימים (BD-4) — סידור בלבד. | UI בלבד |
| BDQ-B2 | תא עם הרבה הזמנות גדל היום בלי גבול. | עד 3 כרטיסים בתא + "עוד N" שפותח את חלון היום (עם סינון). | שומר על גובה שורה סביר; אותו חלון קיים (BD-41). | UI בלבד |
| BDQ-B3 | **באג קיים:** החיפוש החכם בלוח שולח `pageContext:'board'`, השרת לא מכיר אותו ומחפש בלקוחות — הלוח מתרוקן (BD-33). | מוצג באנר "תוצאות חיפוש חכם" + לוח ריק + הסבר, כלומר ההתנהגות האמיתית. לא מומצאות תוצאות. | R8 — לא מתקנים שרת בפרויקט עיצוב. **מומלץ לתקן בנפרד** (להוסיף `board` ל-smart-search עם אותו טיפול כמו `orders`). | באג שרת |
| BDQ-B4 | **באג קיים:** בדיקת התאמת ברקוד (`enforce_rental_barcode_match`) רצה רק בהזנה ידנית (BD-58). | לא מודגם בכרטיס ההשכרה; רק מתועד. | שייך לחוזה כרטיס ההשכרה, לא ללוח. | באג שרת |
| BDQ-B5 | "ריקה" (הזמנה בלי פריטים) מופיעה בכרטיסים אבל לא במקרא. | נוספה למקרא ("בלי פריטים", מסגרת מקווקו זהב). | אין מצב בצבע בלבד בלי הסבר (C-1.12). | UI בלבד |
| BDQ-B6 | שורת "לקוח" בכרטיס הפרטים מוצגת היום **בלי ערך**. | מוצג שם הלקוח. | הנתון כבר בתשובה (`customerName`). | UI בלבד |
| BDQ-B7 | כרטיס הפרטים נפתח היום רק בריחוף עכבר — אין גישה במגע/מקלדת. | ריחוף עכבר = כרטיס פרטים צף; לחיצה/Enter/מגע = חלונית צפייה (עם כפתור "פעולות להזמנה"). | חוקה A4: "כרטיס פרטים בריחוף/מגע". | UI בלבד |
| BDQ-B8 | צבעי הקטגוריות: אסור ירוק/אדום/כתום. | הוחזר = navy מלא · בהשכרה = sky · תיקונים = מסגרת navy · לא שולם = אפרסק · שולם = sky בהיר · בלי פריטים = זהב מקווקו · אחר = בלי תגית · **באיחור = מסגרת plum + אייקון שעון + המילה**. תמיד אייקון + מילה. | C-1.1, C-1.13. | UI בלבד |
| BDQ-B9 | כפתור "היום" (חזרה לחודש הנוכחי) לא קיים היום. | נוסף, מושבת כשכבר בחודש הנוכחי; ברשימה — גולל לסמן "היום". | ניווט בלבד. | UI בלבד |
| BDQ-B10 | חיפוש מתקדם מסנן **תוך כדי הקלדה** (כל אות = בקשה לשרת), ו"החלה" רק סוגרת. | נשמר כמו שהוא; הכפתור נקרא "סיום" ומתחתיו שורה חיה "N שדות מסננים את הלוח". שורת "מסונן:" עם צ'יפים מעל הלוח. | R8 — לא משנים מתי נשלחת בקשה. (שקלו debounce בפיילוט.) | UI + ביצועים |
| BDQ-B11 | בורר התאריך העברי (קפיצה, חיפוש מתקדם). | באב-הטיפוס: בחירת יום+חודש פשוטה עם הערה "במימוש: הבורר המשותף". | הבורר הוא רכיב משותף שלא בתחום. | רכיב משותף |
| BDQ-B12 | כרטיס ההשכרה ואשף ההדפסה הם רכיבים משותפים גדולים. | מוצגים בקיצור (סריקה, השכרה/החזרה, אישור, סגירה עם סריקות ממתינות, מחסן → אישור מנהל, איחור → "לא תקין"). חוזה מלא — בחוזה כרטיס ההשכרה (A2) ובאשף (A3). | היקף. | תחום |
| BDQ-B13 | מקום "משלוחים" ו"זמינות מלאי" בלוח (הוזכרו במשימה). | **לא נוספו.** הלוח לא קורא אותם היום (`enable_deliveries` לא נקרא בלוח; `lib/inventory.js` לא בנתיב). | אסור להמציא נתון. | דורש החלטה + נתונים |
| BDQ-B14 | גרירת הזמנה ליום אחר (drag). | **לא נוספה.** אין גרירה היום, ושינוי תאריך = שמירת הזמנה. | R8. | — |
| BDQ-B15 | `restrict_board_to_managers` מופיע בהגדרות אבל לא נקרא בשום מקום. | לא מוצג; שער העמוד = `page:board` בלבד (מצב "אין הרשאה" בבקרה). | מתועד כ-"לא בשימוש". מומלץ להסיר מהממשק. | ניקוי |
| BDQ-B16 | ימים באותיות: פורמט `י״ג` (גרשיים) + לועזי `24/9`. | כך. | כמו `renderGematriya`. | UI |

## בית וסיכום (A5)

| # | שאלה | מה נבחר באב-הטיפוס | למה | נוגע ב |
|---|---|---|---|---|
| BDQ-D1 | הארכיטיפ A5 מבקש "רשימות דורש טיפול (חובות, לא הוחזרו)" ואריחי KPI בבית. **בעמוד הבית אין היום שום מספר**, ו-`/api/dashboard/debts` קיים אבל אין לו שימוש (DB-61). | **לא הומצאו מספרים.** מקום שמור מקווקו "דורש טיפול" — **כבוי כברירת מחדל**, נדלק מהבקרה, בלי ערכים. | "אסור להמציא נתון" (REVIEW-1). הוספה = נתונים חדשים בעמוד → החלטת בעלים. | דורש נתונים + החלטה |
| BDQ-D2 | **פער קיים:** חלונית ההגדרה המהירה נטענת רק למנהלת סניף/מתכנת; הנהלה ראשית מקבלת 401, אבל רק הנהלה ראשית יכולה **לשמור** (DB-40/42). | מודגם בדיוק כך: בתפקיד "הנהלה ראשית" — הודעת "אין הרשאה"; בתפקיד "מנהלת סניף" — טעינה, ושמירה דורשת אישור מנהל. | R8. **מומלץ לתקן בשרת** (לאחד את שני הבדיקות). | באג הרשאות |
| BDQ-D3 | כפתור החיפוש החכם בבית לא מוסתר כשה-AI כבוי/חסום, ולחיצה מסתיימת בשגיאה (DB-30). | באב-הטיפוס המצב "חכם" **לא מוצג** כשאין גישה (לפי אותם כללים שהשרת בודק). | UI בלבד — אותו מידע כבר קיים ב-layout. | UI בלבד |
| BDQ-D4 | סמל הסטטוס בתוצאות ההזמנות נלקח מ-`Order.status` הגולמי, שכמעט תמיד ריק → כמעט הכול "פעיל" (DB-23). | מוצג כמו היום (4 ערכים + "פעיל"), עם שורת הסבר. | תיקון = השרת צריך להחזיר פריטים לכל הזמנה כדי להשתמש ב-`orderStatus.js`. | דורש נתונים |
| BDQ-D5 | הסכום בתוצאות = השדה הישן `totalAmount` (ריק בהזמנות חדשות) (DB-24). | מוצג כמו היום (כולל ₪0). | תיקון = שינוי שאילתה. | דורש נתונים |
| BDQ-D6 | "חיפושים אחרונים" נשמרים ולא מוצגים (DB-15). | מוצגים כצ'יפים מתחת לחיפוש במצב פתיחה; לחיצה מחפשת שוב. | הנתון כבר נשמר מקומית. | UI בלבד |
| BDQ-D7 | גרף עוגה (אמצעי תשלום) ו-3 גרפי מגמה נפרדים. | **עמודות אופקיות** במקום עוגה (קריאות, בלי צבעים קטגוריאליים — שם + סכום + אחוז על כל שורה); 3 המגמות בכרטיס אחד עם בורר "30 ימים / 12 שבועות / 12 חודשים". לכל גרף כפתור "טבלה". העמודה האחרונה בזהב (= "עכשיו"). | C-1.1 (אין סגול/כתום), נגישות A5 ("חלופה טקסטואלית"). אותם מערכים. | UI בלבד |
| BDQ-D8 | ציר הזמן בגרף המגמה ב-RTL. | הזמן זורם מימין לשמאל (הישן מימין, האחרון משמאל). | עקבי עם "קדימה = שמאלה" (C-1.11). **לאישור** — בחלק מהגרפים העבריים נהוג משמאל לימין. | UI |
| BDQ-D9 | אריח "הזמנות" סופר גם מחוקות וטיוטות (DB-51). | מוצג כמו היום, עם תת-כותרת שאומרת את זה במפורש. | R8 — חישוב לא משתנה. | הכרעה עסקית |
| BDQ-D10 | אין מצב ריק/שגיאה לגרפים היום (DB-57). | נוספו מצב טעינה (שלד), מצב ריק ("אין עדיין תשלומים"). מצב שגיאה בשרת = העמוד כולו (רכיב שרת). | A5: "טעינה הדרגתית לכל אריח". | UI בלבד |
| BDQ-D11 | טקסט "מדיניות פרטיות" הוא זמני בקוד. | נוסח קצר חדש + הערה "נוסח סופי — החלטת הבעלים". | C-1.7. | מלל |
| BDQ-D12 | שני עמודים (`/` ו-`/dashboard`) בקובץ אחד. | בורר "עמוד" בבקרת ההדגמה. הניווט העליון מסמן "ראשי" בבית. | שני העמודים = ארכיטיפ אחד. | — |
| BDQ-D13 | שדה שאלת ההמשך צף היום (fixed) מעל הכול. | `sticky` בתחתית אזור התוכן (לא מכסה toast/פס עליון). | §ד — רק שכבות ב-LayerManager הן `fixed`. | UI |

## כללי (שניהם)

| # | שאלה | מה נבחר |
|---|---|---|
| BDQ-G1 | ערכי הפרופילים (ארגון א׳/ב׳) — לא אומתו מול DB (אין גישה בריצה הזו). | ערכי דמה לפי חוקה §ט.3; שמות סניפים מומצאים. |
| BDQ-G2 | הגופן Rubik לא נטען בסביבת הבדיקה (חסימת רשת) — הצילומים בגופן חלופי. | נשאר; קובץ ה-HTML טוען Rubik מ-Google Fonts כמו A2. |
| BDQ-G3 | מגירת הניווט בטלפון מוצגת כגיליון (שכבה) ולא כמגירה מהצד כמו ב-A2. | פשטות באב-טיפוס; בפיילוט — רכיב המגירה של `SHELL.md`. |
