# אב-טיפוס מוקדם — ארכיטיפ "כרטיס פרט" (A2) על כרטיס הלקוח

| קובץ | מה יש בו |
|---|---|
| `archetype-detail-card.contract.md` | חוזה פונקציונלי ממוספר (CC-1…CC-74) שחולץ מהקוד הנוכחי של `/customers/[id]`, בלי העתקת markup/CSS/מלל |
| `archetype-detail-card.html` | אב-טיפוס סטטי, קובץ אחד, נתוני דמה, בלי API. החיצוני היחיד: Google Fonts (Rubik) |
| `PROTOTYPE-QUESTIONS.md` | שאלות שעלו (Q1–Q14) והבחירה שנעשתה בכל אחת — **לאישור** |
| `screenshots/` | 22 צילומים נבחרים מהבדיקה בדפדפן headless (שם הקובץ = רוחב-שלב) |
| `verify-prototype.cjs` | סקריפט הבדיקה שהפיק אותם (Playwright; לא חלק מהאפליקציה) |

## איך פותחים
פותחים את `archetype-detail-card.html` ישירות בדפדפן (לחיצה כפולה / גרירה לחלון). אין שרת ואין התקנה. בלי אינטרנט הגופן נופל ל-Heebo/גופן מערכת — הכול עדיין עובד.
למעלה יש **"בקרת הדגמה"** (מסגרת זהב מקווקו) — היא לא חלק מהעיצוב. היא מחליפה פרופיל ארגון, נתונים, מצבי טעינה, ערכת נושא של חלוניות, ופותחת כל סוג חלונית.

## מה לבדוק (רשימה לבעלים)
1. **שפה חזותית** מול סקיצה B: סרגל עליון navy עם קו זהב, רייל navy בצד שמאל, כפתור ראשי זהב אחד לאזור, כפתורים משניים אפרסק, Rubik גדול ומרווח.
2. **נתון בשורה**: בטאב "פרטים" — תווית קטנה מעל, ערך גדול מתחת; נתון חסר = "חסר" עם אייקון זהב (נסו בקרה → נתונים "דליל").
3. **עריכה ורשימת שינויים**: "עריכה" → שנו טלפון/עיר → כל שינוי מופיע ברייל עם "לפני → אחרי", אפשר לבטל שינוי בודד ואז להחזיר אותו. השדה שהשתנה מקווקו (ממתין). הטאב מקבל נקודה מקווקו.
4. **שמירה**: "שמירת השינויים" ברייל → פס עליון כחול עם **פס זהב יורד (15 ש׳)** — ריחוף עוצר אותו; "להיסטוריה" מעביר לטאב ההיסטוריה ושם מופיעה הרשומה החדשה.
5. **שגיאות**: מחקו שם פרטי ושמרו → הודעה בשדה עצמו + toast בפינה השמאלית-התחתונה (לא `alert`).
6. **יציאה עם שינויים**: החץ הימני (חזרה) → חלון תלת-דרכי.
7. **מייל**: "מייל ללקוח" → חלון אישור מנהל: בחירת מאשר עם חיפוש (ברירת מחדל = אני אם מורשה) + **סיסמה מלאה** (כהה/בהיר לפי המתג; הסיסמה "0000" = שגויה ומדגימה רעידה) → חלון הזנה בהיר תמיד. ✕ שומר טיוטה; "ניקוי הטיוטה" → חלון אישור שני מעל הראשון. בקרה: "כשל בפעולות שרת", "אין מאשרים מורשים". (תוקן ב-REVIEW-1 B-1/M-2)
8. **חסימה**: באנר בטאב פרטים; "ביטול החסימה" רק כש"העובד מהנהלה ראשית" מסומן בבקרה, אחרת מופיע מנעול עם הסבר.
9. **חוב**: טאב "תשלומים" — אריח היתרה (חוב/זכות/מאוזן — אייקון + מילה, לא צבע בלבד) + טבלה של 3 עמודות ושורה מורחבת.
10. **שכבות**: כל חלונית מכסה את כל המסך כולל הסרגל העליון, העמוד מאחור לא גולל ולא נגיש (inert), Esc סוגר רק את העליונה, הפוקוס חוזר לכפתור שפתח.
11. **רספונסיביות**: <1024 הרייל הופך לפס תחתון + גיליון תחתון, ו"במבט אחד" עולה מעל הטאבים; <640 חלון אישור/קוד = גיליון תחתון, חלון הזנה = מסך מלא; הטאבים גוללים אופקית.
12. **org variance**: בחרו "קיצוני" או סמנו "תוויות וערכים ארוכים" — שם של 60+ תווים, סכומים של 7 ספרות, סיבת חסימה ארוכה, שם סניף ארוך — הפריסה לא נשברת. "קטע הסכמה לדיוור" מסתיר/מציג שדה בלי להשאיר חור. "לקוח חדש" מראה כוכביות חובה שמשתנות לפי הפרופיל.
13. **RTL**: חץ "חזרה" מצביע ימינה; "פתיחת הזמנה", "להיסטוריה" ו"לפני → אחרי" מצביעים שמאלה; חיצי מקלדת בטאבים: שמאלה = הבא.
14. **תנועה**: אייקונים זזים בריחוף/מיקוד (חץ נוגע, רענון מסתובב, פעמון מתנדנד…). מתג "הדמיית הפחתת תנועה" (או הגדרת המערכת) מכבה הכול; פס הזהב ממשיך לרדת.

## מיפוי חוזה → אב-טיפוס
| CC | איפה באב-הטיפוס |
|---|---|
| CC-1 | מחוץ לתחום — שער הרשאה בשרת (layout). לא מוצג בעמוד |
| CC-2, CC-71…74 | בקרה → "לקוח חדש" (טופס בעמודה אחת) |
| CC-3, CC-28 | בקרה → "העובד מהנהלה ראשית" (מציג/מסתיר ביטול חסימה) |
| CC-4, CC-37, CC-65, CC-66 | בקרה → פרופיל הגדרות + "קטע הסכמה לדיוור"; כוכביות בטופס החדש |
| CC-5, CC-8 | בקרה → מצב "טוען…" (שלד + אייקון מסתובב) |
| CC-6, CC-7 | בקרה → "שגיאת טעינה" (מצב שגיאה + לנסות שוב / לרשימה). CC-6 (הפניה שקטה) מחוץ לתחום — ניווט |
| CC-9 | מחוץ לתחום — "נצפו לאחרונה" נשמר בדפדפן, בלי UI בעמוד |
| CC-10…CC-18 | כותרת העמוד: ראשי תיבות, שם, צ'יפים (מספר, הזמנות, חסום, עודכן). טלפון/מייל/כתובת עברו לטאב פרטים (שורה לכל נתון) |
| CC-19 | ⓘ ליד "שינויים שלא נשמרו" ברייל |
| CC-20, CC-21, CC-22 | רייל: "שמירת השינויים", "ביטול כל השינויים", מונה שינויים; בטלפון — פס תחתון |
| CC-23, CC-60, CC-61 | "מייל ללקוח" → קוד מנהל → חלון מייל; בלי מייל → חלון "ללקוח אין מייל" (נתונים "דליל") |
| CC-24 | כפתור החץ העגול בכותרת (+ חלון תלת-דרכי, Q3) |
| CC-25, CC-26 | 5 טאבים עם מונה/סימן; מקלדת. (mount של כל הטאבים = כלל מימוש, לא חזותי) |
| CC-27, CC-28, CC-62 | באנר חסימה בטאב פרטים + חלון אישור |
| CC-29 | טאב פרטים במצב תצוגה |
| CC-30, CC-41 | "עריכה" / "סיום עריכה" בכרטיס הפרטים (Q2) |
| CC-31…CC-38 | טופס העריכה; עיר/רחוב עם השלמה (datalist), ת"ז עם ⓘ, מתג דיוור |
| CC-33 | "העתקה", "שליחת מייל", "השלמה ל-@gmail.com" מתחת לשדה המייל |
| CC-39 | כפתור שמירה בתחתית טופס העריכה |
| CC-40 | כרטיס "הערות" (שורה אוטומטית מפורקת לצ'יפים + קישור להזמנה) |
| CC-42…CC-47 | טאב הזמנות (שורה מורחבת; ריק: נתונים "חדש לגמרי"; הרבה: "14 הזמנות") |
| CC-48…CC-52 | טאב תשלומים: אריחים + יתרה + טבלה מורחבת; ריק |
| CC-53…CC-57 | טאב זיכוי ובנק: טופס בנק (נכנס לאותה רשימת שינויים) + טבלת זיכויים; ריק |
| CC-58, CC-59 | טאב היסטוריה: חיפוש + ניקוי, קיבוץ לפי יום, פירוט לפני/אחרי; בקרה → "שגיאה רק בהיסטוריה"; ריק = חיפוש בלי תוצאות |
| CC-63 | שגיאות בשדה + toast (במקום חלונית הודעה) |
| CC-64 | פס עליון עם טיימר זהב (זמני בלבד, בלי פעמון — D-4) |
| CC-67 | ולידציית טלפון/מייל/ת"ז/טלפון כפול — מודגמת בשמירה |
| CC-68, CC-70 | שמירה מדומה (אין API) |
| CC-69 | בקרה → "התנגשות שמירה" (חלון עם "רענון הכרטיס") |

## מה נבדק בפועל (ומה לא)
- **נבדק** (Chromium headless דרך Playwright, 2026-09-24): טעינה ב-360/768/1024/1440 בלי שגיאות JS; `scrollWidth ≤ clientWidth` בכל הרוחבות (אין גלילה אופקית של המסמך) ואף אלמנט שחורג מהמסך (מלבד טאבים/טבלה שגוללים בתוך עצמם); זרימה אוטומטית: עריכה → רייל/גיליון → קוד מנהל → חלון מייל → Esc → חזרה עם שינויים → שמירה ויציאה; `data-v3-lock` פעיל והעמוד `inert` כשחלונית פתוחה; שתי שכבות נערמות (חלון מייל + אישור סגירה) ו-Esc פעמיים סוגר את שתיהן ומשחרר את הנעילה; ביטול+החזרה של שינוי בודד; פרופיל "קיצוני" וטופס "לקוח חדש" עם שגיאות — בלי גלישה אופקית. הבדיקה מצאה שני באגים שתוקנו: toast ופס עליון כיסו חלונית פתוחה (Q15).
- **נבדק בעין** על הצילומים: כיווני חצים (חזרה = ימינה, קדימה = שמאלה), מיקום רייל משמאל, toast בפינה שמאלית-תחתונה.
- **לא נבדק**: קורא מסך אמיתי, Safari/iOS (safe-area, `100dvh`), מקלדת וירטואלית, ניגודיות מדודה (axe) — רק עין, תנועה בפועל בהגדרת מערכת "הפחתת תנועה" (נבדק רק מתג ההדמיה בקוד). הגופן Rubik לא נטען בחלק מהצילומים (רשת), ולכן חלק מהצילומים בגופן חלופי.
- זה **לא** בנוי על ספריית `app/v3` (שלב 1 עוד בבנייה בענף אחר) — ה-CSS/JS כאן מקומיים לקובץ, ושמות ה-tokens (`--v3-*`) תואמים את החוקה כדי שיהיה קל להעביר.

---

# אב-טיפוס A6 — מגע / עמדה (עמדת לקוחות + שעון נוכחות)

| קובץ | מה יש בו |
|---|---|
| `archetype-touch.contract.md` | חוזה ממוספר TK-1…TK-70 שחולץ מ-`main` @ `1060e1b` (`/customer-interface`, `/punch-clock` והנתיבים שלהם), כולל מטריצת org-variance וטבלת "UI בלבד מול צריך נתונים" |
| `archetype-touch.html` | אב-טיפוס סטטי, קובץ אחד, נתוני דמה, בלי API. שני מסכים שמחליפים ב"בקרת הדגמה" (הכפתור הצף המקווקו בפינה) |
| `TOUCH-FORMS-QUESTIONS.md` | שאלות TQ-1…TQ-20 (ו-FQ של A7) והבחירה שנעשתה — **לאישור** |
| `verify-touch-forms.cjs` | סקריפט הבדיקה (Playwright) שהפיק את `screenshots/touch/` ו-`screenshots/forms/` |

**איך פותחים:** לחיצה כפולה על `archetype-touch.html`. בקרת ההדגמה = הכפתור "בקרת הדגמה" בפינה (לא חלק מהעיצוב; נעלם כשחלונית פתוחה). הלוח העברי מחושב באמת (לוח עברי מובנה בדפדפן); הנתונים — דמה.

## מה לבדוק (A6)
1. **שלב 1 — תאריך:** שלוש רשימות (יום/חודש/שנה) + שלושה חודשים ברשת; נגיעה ביום → ישר לשמלות הפנויות (כמו היום, TQ-4). "חזרה להיום". תאי יום 56 גובה, ≥44 רוחב גם ב-360.
2. **רישום עצמי** (בקרה → "ארגון ב׳" או "רישום עצמי בעמדה"): כוכביות לפי ההגדרות; "סיום הרישום" ריק → שגיאה בכל שדה; עם שם+טלפון → **הודעת השרת** "חובה למלא לפחות אחד מבין: טלפון נוסף / אימייל, תעודת זהות חובה" — זה מה שקורה היום (TQ-5, באג לבדיקה).
3. **שלב 2 — שמלות פנויות:** כותרת עם התאריך (עברי+לועזי, כל אחד בשורה), מונה דגמים/פנויות; כלים עם **אייקון + מילה** (אין ריחוף במגע). "סינון" = פאנל בצד (≥1024) או גיליון תחתון (<1024). שלוש צורות תצוגה, מחוון גודל.
4. **נגיעה בדגם / במידה** (עמדה פתוחה) → חלון "הזמנות לדגם" (שבוע לפני ואחרי), סטטוס תשלום רק מ-4 הערכים האמיתיים.
5. **נעילה ללקוחה** → מסך מלא, הסרגל העליון נעלם, "נעול ללקוחה", נגיעה בדגם לא פותחת כלום, קליק ימני חסום. "הדפסה"/"יציאה"/"שחרור"/כפתור "יציאה ממסך מלא (Esc)" בבקרה → חלון אישור עובדת (בחירה מרשימה עם חיפוש + **סיסמה מלאה**, "0000" = שגויה + רעידה). "ביטול" = נשאר נעול. אישור הדפסה → מדפיס ונשאר נעול.
6. **שעון נוכחות** (בקרה → מסך): שעון גדול חי, "מי את?" עם רשימה נפתחת (56 לשורה, מקלדת ↑↓ Enter), סיסמה (במחשב מאושר — גם קוד קצר + ⓘ), שני כפתורים 72. בלי בחירה → "בחרו שם…" (אפרסק). "יציאה" עם "בדיקת החזרות" דלוקה → חלון עם רשימת משפחות (כל שם בשורה). הצלחה → כחול עם ✓ ופס זהב שיורד 3 ש׳. נסו: "0000", כניסה פעמיים, קוד של 4 תווים בלי "מחשב מאושר".
7. **הצעה (כבויה):** "שומר מסך אחרי 20 ש׳" — לא קיים היום (TQ-1).
8. **org variance:** "קיצוני" = הכול דלוק, שם דגם ארוך, 9 קטגוריות, 12 משפחות; "הרבה" = 60 דגמים; "אין דגמים"; תמונות/עוזר חכם מוסתרים בלי חור.
9. **RTL:** שלב הבא / "חודש הבא" = חץ שמאלה; "תאריך אחר" / "חודש קודם" / "יציאה" = ימינה.

## מיפוי חוזה → אב-טיפוס (A6)
| TK | איפה |
|---|---|
| TK-1 | בקרה → "אין עובד מחובר + חובת כניסה": הקטלוג → מצב שגיאה "העמדה צריכה עובדת מחוברת"; הרישום → 401 |
| TK-2 | מחוץ לתחום — רקע העמוד (הרקע v3 הוא sky קבוע) |
| TK-3 | הסרגל העליון מוצג כשפתוח, נעלם כשנעול |
| TK-4, TK-5 | כותרת העמדה: "עמדת לקוחות" + שני שלבים (✓ על שלב שעבר), מעבר חופשי |
| TK-6…TK-11 | כרטיס "מתי האירוע?" (רשימות, 3 חודשים, ניווט, היום/נבחר, תחתית עם התאריך + ⓘ פרשה, "חזרה להיום"); מעבר מיידי |
| TK-12…TK-16 | כרטיס "רישום פרטים" (לפי ההגדרה), שדות, בדיקת לקוח, הודעת שרת, 401, "נרשמתם" + מספר לקוח + "רישום לקוחה נוספת" |
| TK-17 | כרטיס "הזמנה עצמאית" מושבת |
| TK-18…TK-23 | שדה העוזר (שלב 1 ושלב 2), כרטיס שיחה, "שיחה חדשה", סגירה, כפתורי "סינון והצגה"/"שמלות פנויות ל…" מתוך תשובה; מוסתר לפי ההגדרה. שמירה בדפדפן (TK-23) — לא מוצג |
| TK-24…TK-26 | טעינה (שלד), שגיאה (UI בלבד, TQ-7), כותרת + מונה |
| TK-27…TK-35 | פאנל/גיליון סינון: חיפוש, קטגוריות עם מונה, מידות עם מונה, מתג "גם מידות בלי פנוי", צורת תצוגה, ניקוי; מחוון גודל; מיון |
| TK-36…TK-41 | שם תצוגה, עיגול (תמונה/ראשי תיבות לפי ההגדרה), שורת זמינות (אייקון+מילה), גלולות מידה, שלוש תצוגות, ריק/טעינה |
| TK-42 | כלי הסרגל (סינון, גודל, תאריך אחר, רענון, הדפסה, למערכת/יציאה, נעילה/שחרור) |
| TK-43…TK-45 | חלון "הזמנות לדגם" (עמדה פתוחה בלבד) |
| TK-46…TK-52 | נעילה, מסך מלא, קליק ימני, חלון אישור עובדת (שחרור/הדפסה), שגיאות, ביטול = נשאר נעול |
| TK-53 | הדפסה → toast (בהדגמה לא נפתח חלון); "כשל בפעולות שרת" = חלון קופץ חסום |
| TK-54 | מתג "הצעה" (כבוי) — TQ-1 |
| TK-55 | אין קיפד (TQ-2) |
| TK-56 | מחוץ לתחום — שכבת מערכת ההפעלה (`KIOSK.md`) |
| TK-57, TK-58 | שעון נוכחות עם סרגל עליון; שעון חי |
| TK-59…TK-64 | "מי את?" (טוען/לא נמצא/מקלדת), סיסמה/קוד קצר לפי "מחשב מאושר", שני כפתורים, בדיקה בלי קריאה |
| TK-65 | חלון "יש משפחות שעוד לא החזירו" (עד 5 שמות + "ועוד N"); ביטול → "היציאה בוטלה" |
| TK-66, TK-67 | הצלחה (3 ש׳) ושגיאות השרת המדומות: סיסמה שגויה, קוד קצר במחשב לא מאושר, כבר נרשמה כניסה, אין משמרת פתוחה, אין חיבור |
| TK-68 | צבע לפי סוג (TQ-10) |
| TK-69 | עם "אין עובד מחובר" הבדיקה לא קופצת (כמו היום) |
| TK-70 | מחוץ לתחום — מסלול בלי סיסמה לא בשימוש בעמוד |

---

# אב-טיפוס A7 — טופס / הגדרות (הפרופיל שלי)

| קובץ | מה יש בו |
|---|---|
| `archetype-forms.contract.md` | חוזה ממוספר FM-1…FM-44 מ-`main` @ `1060e1b` (`/profile`, `/api/me/profile`, `/api/employees/[id]/password`) |
| `archetype-forms.html` | אב-טיפוס סטטי, קובץ אחד, נתוני דמה |

## מה לבדוק (A7)
1. **עמודה אחת 720** בכרטיסים לפי נושא (פרטים אישיים / יצירת קשר / כתובת / התראות / סיסמה / תמונה), כל נתון בשורה. ב-≥1024 — "בעמוד הזה" בצד.
2. **מה נשמר מתי** (D-3): שתי תיבות בראש העמוד. פרטים+מתג התראות+תמונה → "שמירת השינויים" בפס הדביק; סיסמה → מתחלפת מיד בכפתור שלה.
3. **עריכה:** שנו טלפון/עיר → השדה מקווקו + "לא נשמר" + "היה: …", הכרטיס מקווקו, נקודה ב"בעמוד הזה", הפס הדביק מונה ומציין אילו שדות.
4. **שמירה:** מייל "not-an-email" → שגיאה בשדה + toast (אותו כלל של הדפדפן היום). תקין → "שומרים…" → **פס עליון עם פס זהב יורד 15 ש׳** ("הפרטים נשמרו") והשם בסרגל העליון מתעדכן. בקרה → "כשל בשמירה" → toast עם "לנסות שוב", השינויים נשארים.
5. **ביטול השינויים** → חלון אישור (כהה/בהיר לפי המתג). **חזרה עם שינויים** → חלון תלת-דרכי (FQ-2).
6. **סיסמה:** "החלפת סיסמה" נפתח בעמוד; חדשה קצרה מ-4 → שגיאה; הנוכחית "0000" → "לא נכונה" + רעידה; בקרה → "אחרי איפוס" → הנוכחית לא חובה. הצלחה → toast "הסיסמה הוחלפה… גם הקוד הקצר התעדכן".
7. **תמונה:** בחירת קובץ אמיתי מהמחשב (נשמר רק ב"שמירה"), "הסרה"; בקרה → הסתרת האזור — נעלם בלי חור.
8. **מצבים:** טוען (שלד), "צריך להתחבר", "לא הצלחנו לטעון" + "לנסות שוב" (FQ-8); עובדת עם מעט פרטים; בלי מחלקה; "קיצוני" = שמות/מייל/רחוב/מחלקה ארוכים מאוד.

## מיפוי חוזה → אב-טיפוס (A7)
| FM | איפה |
|---|---|
| FM-1, FM-2 | מחוץ לתחום — כניסה מתפריט המשתמש / זיהוי בשרת. כפתור המשתמש בסרגל העליון מציג את השם והתמונה השמורים |
| FM-3 | השדות בכרטיסים |
| FM-4, FM-5, FM-6 | בקרה → "לא מחוברת" / "טוען…" / "שגיאת שרת בטעינה" |
| FM-7, FM-8, FM-9 | כותרת "הפרופיל שלי" + ⓘ, תג מחלקה (בשורה נפרדת), חזרה (עם בדיקת שינויים — FQ-2) |
| FM-10…FM-17 | כרטיסי "פרטים אישיים", "יצירת קשר", "כתובת"; "תאריך הצטרפות" קריא בלבד + ⓘ |
| FM-18 | כרטיס "התראות" — מתג בתוך השמירה המפורשת (FQ-3) |
| FM-19, FM-20 | `autocomplete=new-password` בשדות; `emailSuffix` נשמר בנתונים בלי שדה |
| FM-21…FM-24 | אין שדות חובה; מייל = כלל הדפדפן (FQ-4) |
| FM-25…FM-28 | פס השמירה הדביק, Enter = שמירה, פס עליון/toast כשל |
| FM-29 | מעקב שינויים (FQ-1) — תוספת תצוגה |
| FM-30 | מחוץ לתחום — רישום היסטוריה אוטומטי בשרת, אין תצוגה בעמוד |
| FM-31…FM-37 | כרטיס "סיסמה" + אזור ההחלפה, בדיקות, שגיאות, מצב "אחרי איפוס", הודעת הצלחה |
| FM-38…FM-42 | כרטיס "תמונה" (לפי ההגדרה), ראשי תיבות, בחירה, הסרה; בלי "5MB" ובלי גרירה (FQ-6, FQ-7) |
| FM-43, FM-44 | מחוץ לתחום — עיצוב ותצוגה / שכר ותפקיד |


## מה נבדק בפועל ב-A6/A7 (ומה לא) — 2026-09-24
- **הורץ** (`node docs/redesign-v3/prototypes/verify-touch-forms.cjs`, Playwright 1.56 + Chromium headless מקומי):
  - **A6** ב-360×780, 768×1024, 1024×768, 1440×900 ובעמדה **גבוהה 1080×1920** ו**נמוכה 1366×600**: אין שגיאות JS; `scrollWidth = clientWidth` (אין גלילה אופקית של המסמך) בשלב 1, רישום, קטלוג (רשימה/כרטיסים/טבלה) ושעון; **כל יעד מגע ≥44** (כולל תאי הלוח ב-360); זרימה: שלב 1 → רישום ריק (3 שגיאות בשדות) → רישום עם שם+טלפון בארגון ב׳ (הודעת השרת המדומה) → יום → קטלוג → סינון (פאנל/גיליון) → חלון הזמנות (`data-v3-lock` + העמוד `inert`) → נעילה (הסרגל העליון נעלם) → אישור הדפסה בבהיר → ביטול = נשאר נעול → שחרור עם "0000" (שגיאה) ואז סיסמה (משוחרר) → שעון: בלי בחירה (הודעת "שימו לב"), רשימה, יציאה עם חלון משפחות → "היציאה נרשמה".
  - **A7** ב-360/768/1024/1440: אין שגיאות JS ואין גלילה אופקית (גם "קיצוני"); 3 שדות מסומנים כשונו; שגיאת מייל בשדה; שמירה → פס עליון; סיסמה קצרה/נוכחית שגויה; חזרה עם שינויים → חלון + נעילה; שגיאת טעינה; יעדי מגע ≥44.
  - **באגים שהבדיקה מצאה ותוקנו:** toast כיסה את כפתור השמירה בפס הדביק (FQ-13); כיתוב נסתר של הטבלה ברח מתיבת הגלילה והרחיב את המסמך ל-588px ב-360; תאי לוח 39–43px ב-360/768/1080; לחיצה בבדיקה על מרכז שורת דגם פגעה בכפתור ההדפסה (תוקן בבדיקה).
  - **ניגודיות:** חושבה (WCAG) לכל זוג צבעים בשימוש — הנמוך 4.52:1 (`ink-3` על רקע העמוד), כל השאר 5.0–16:1. ימים מעומעמים של חודש סמוך מתחת ל-AA בכוונה (לא אינטראקטיביים, `aria-hidden`).
  - **RTL:** נבדק בעין בצילומים — "שלב הבא"/"חודש הבא" שמאלה, "תאריך אחר"/"חודש קודם"/"חזרה" ימינה; מאפיינים פיזיים רק ב-toast (`left`, חריג מתועד) וב-`translateX` של מתג/טוסט/מגירה.
- **לא נבדק:** axe / קורא מסך, מסך מגע אמיתי ומקלדת וירטואלית, Safari/iOS, מסך מלא אמיתי (בדפדפן headless הבקשה נכשלת בשקט), הדפסה אמיתית. הגופן Rubik לא נטען בסביבה (שגיאת תעודה של ה-proxy ל-Google Fonts) — הצילומים בגופן חלופי. הצילומים ב-`screenshots/touch/` נבחרו (360, 1440, וכמה מהעמדה הגבוהה/נמוכה); `screenshots/forms/` מלא.
