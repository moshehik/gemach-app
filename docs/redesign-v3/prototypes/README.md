# אב-טיפוס מוקדם — ארכיטיפ "כרטיס פרט" (A2) על כרטיס הלקוח

| קובץ | מה יש בו |
|---|---|
| `archetype-detail-card.contract.md` | חוזה פונקציונלי ממוספר (CC-1…CC-74) שחולץ מהקוד הנוכחי של `/customers/[id]`, בלי העתקת markup/CSS/מלל |
| `archetype-detail-card.html` | אב-טיפוס סטטי, קובץ אחד, נתוני דמה, בלי API. החיצוני היחיד: Google Fonts (Rubik) |
| `PROTOTYPE-QUESTIONS.md` | שאלות שעלו (Q1–Q14) והבחירה שנעשתה בכל אחת — **לאישור** |
| `screenshots/` | 22 צילומים נבחרים מהבדיקה בדפדפן headless (שם הקובץ = רוחב-שלב) |
| `verify-prototype.cjs` | סקריפט הבדיקה שהפיק אותם (Playwright; לא חלק מהאפליקציה) |

## איך פותחים
פותחים את `archetype-detail-card.html` ישירות בדפדפן (לחיצה כפולה / גרירה לחלון). אין שרת ואין התקנה. בלי אינטרנט הגופן נופל ל-Heebo/גופן מערכת — הכול עדיין עובד.
למעלה יש **"בקרת הדגמה"** (מסגרת זהב מקווקו) — היא לא חלק מהעיצוב. היא מחליפה פרופיל ארגון, נתונים, מצבי טעינה, ערכת נושא של חלוניות, ופותחת כל סוג חלונית.

## מה לבדוק (רשימה לבעלים)
1. **שפה חזותית** מול סקיצה B: סרגל עליון navy עם קו זהב, רייל navy בצד שמאל, כפתור ראשי זהב אחד לאזור, כפתורים משניים אפרסק, Rubik גדול ומרווח.
2. **נתון בשורה**: בטאב "פרטים" — תווית קטנה מעל, ערך גדול מתחת; נתון חסר = "חסר" עם אייקון זהב (נסו בקרה → נתונים "דליל").
3. **עריכה ורשימת שינויים**: "עריכה" → שנו טלפון/עיר → כל שינוי מופיע ברייל עם "לפני → אחרי", אפשר לבטל שינוי בודד ואז להחזיר אותו. השדה שהשתנה מקווקו (ממתין). הטאב מקבל נקודה מקווקו.
4. **שמירה**: "שמירת השינויים" ברייל → פס עליון כחול עם **פס זהב יורד (15 ש׳)** — ריחוף עוצר אותו; "להיסטוריה" מעביר לטאב ההיסטוריה ושם מופיעה הרשומה החדשה.
5. **שגיאות**: מחקו שם פרטי ושמרו → הודעה בשדה עצמו + toast בפינה השמאלית-התחתונה (לא `alert`).
6. **יציאה עם שינויים**: החץ הימני (חזרה) → חלון תלת-דרכי.
7. **מייל**: "מייל ללקוח" → חלון אישור מנהל: בחירת מאשר עם חיפוש (ברירת מחדל = אני אם מורשה) + **סיסמה מלאה** (כהה/בהיר לפי המתג; הסיסמה "0000" = שגויה ומדגימה רעידה) → חלון הזנה בהיר תמיד. ✕ שומר טיוטה; "ניקוי הטיוטה" → חלון אישור שני מעל הראשון. בקרה: "כשל בפעולות שרת", "אין מאשרים מורשים". (תוקן ב-REVIEW-1 B-1/M-2)
8. **חסימה**: באנר בטאב פרטים; "ביטול החסימה" רק כש"העובד מהנהלה ראשית" מסומן בבקרה, אחרת מופיע מנעול עם הסבר.
9. **חוב**: טאב "תשלומים" — אריח היתרה (חוב/זכות/מאוזן — אייקון + מילה, לא צבע בלבד) + טבלה של 3 עמודות ושורה מורחבת.
10. **שכבות**: כל חלונית מכסה את כל המסך כולל הסרגל העליון, העמוד מאחור לא גולל ולא נגיש (inert), Esc סוגר רק את העליונה, הפוקוס חוזר לכפתור שפתח.
11. **רספונסיביות**: <1024 הרייל הופך לפס תחתון + גיליון תחתון, ו"במבט אחד" עולה מעל הטאבים; <640 חלון אישור/קוד = גיליון תחתון, חלון הזנה = מסך מלא; הטאבים גוללים אופקית.
12. **org variance**: בחרו "קיצוני" או סמנו "תוויות וערכים ארוכים" — שם של 60+ תווים, סכומים של 7 ספרות, סיבת חסימה ארוכה, שם סניף ארוך — הפריסה לא נשברת. "קטע הסכמה לדיוור" מסתיר/מציג שדה בלי להשאיר חור. "לקוח חדש" מראה כוכביות חובה שמשתנות לפי הפרופיל.
13. **RTL**: חץ "חזרה" מצביע ימינה; "פתיחת הזמנה", "להיסטוריה" ו"לפני → אחרי" מצביעים שמאלה; חיצי מקלדת בטאבים: שמאלה = הבא.
14. **תנועה**: אייקונים זזים בריחוף/מיקוד (חץ נוגע, רענון מסתובב, פעמון מתנדנד…). מתג "הדמיית הפחתת תנועה" (או הגדרת המערכת) מכבה הכול; פס הזהב ממשיך לרדת.

## מיפוי חוזה → אב-טיפוס
| CC | איפה באב-הטיפוס |
|---|---|
| CC-1 | מחוץ לתחום — שער הרשאה בשרת (layout). לא מוצג בעמוד |
| CC-2, CC-71…74 | בקרה → "לקוח חדש" (טופס בעמודה אחת) |
| CC-3, CC-28 | בקרה → "העובד מהנהלה ראשית" (מציג/מסתיר ביטול חסימה) |
| CC-4, CC-37, CC-65, CC-66 | בקרה → פרופיל הגדרות + "קטע הסכמה לדיוור"; כוכביות בטופס החדש |
| CC-5, CC-8 | בקרה → מצב "טוען…" (שלד + אייקון מסתובב) |
| CC-6, CC-7 | בקרה → "שגיאת טעינה" (מצב שגיאה + לנסות שוב / לרשימה). CC-6 (הפניה שקטה) מחוץ לתחום — ניווט |
| CC-9 | מחוץ לתחום — "נצפו לאחרונה" נשמר בדפדפן, בלי UI בעמוד |
| CC-10…CC-18 | כותרת העמוד: ראשי תיבות, שם, צ'יפים (מספר, הזמנות, חסום, עודכן). טלפון/מייל/כתובת עברו לטאב פרטים (שורה לכל נתון) |
| CC-19 | ⓘ ליד "שינויים שלא נשמרו" ברייל |
| CC-20, CC-21, CC-22 | רייל: "שמירת השינויים", "ביטול כל השינויים", מונה שינויים; בטלפון — פס תחתון |
| CC-23, CC-60, CC-61 | "מייל ללקוח" → קוד מנהל → חלון מייל; בלי מייל → חלון "ללקוח אין מייל" (נתונים "דליל") |
| CC-24 | כפתור החץ העגול בכותרת (+ חלון תלת-דרכי, Q3) |
| CC-25, CC-26 | 5 טאבים עם מונה/סימן; מקלדת. (mount של כל הטאבים = כלל מימוש, לא חזותי) |
| CC-27, CC-28, CC-62 | באנר חסימה בטאב פרטים + חלון אישור |
| CC-29 | טאב פרטים במצב תצוגה |
| CC-30, CC-41 | "עריכה" / "סיום עריכה" בכרטיס הפרטים (Q2) |
| CC-31…CC-38 | טופס העריכה; עיר/רחוב עם השלמה (datalist), ת"ז עם ⓘ, מתג דיוור |
| CC-33 | "העתקה", "שליחת מייל", "השלמה ל-@gmail.com" מתחת לשדה המייל |
| CC-39 | כפתור שמירה בתחתית טופס העריכה |
| CC-40 | כרטיס "הערות" (שורה אוטומטית מפורקת לצ'יפים + קישור להזמנה) |
| CC-42…CC-47 | טאב הזמנות (שורה מורחבת; ריק: נתונים "חדש לגמרי"; הרבה: "14 הזמנות") |
| CC-48…CC-52 | טאב תשלומים: אריחים + יתרה + טבלה מורחבת; ריק |
| CC-53…CC-57 | טאב זיכוי ובנק: טופס בנק (נכנס לאותה רשימת שינויים) + טבלת זיכויים; ריק |
| CC-58, CC-59 | טאב היסטוריה: חיפוש + ניקוי, קיבוץ לפי יום, פירוט לפני/אחרי; בקרה → "שגיאה רק בהיסטוריה"; ריק = חיפוש בלי תוצאות |
| CC-63 | שגיאות בשדה + toast (במקום חלונית הודעה) |
| CC-64 | פס עליון עם טיימר זהב (זמני בלבד, בלי פעמון — D-4) |
| CC-67 | ולידציית טלפון/מייל/ת"ז/טלפון כפול — מודגמת בשמירה |
| CC-68, CC-70 | שמירה מדומה (אין API) |
| CC-69 | בקרה → "התנגשות שמירה" (חלון עם "רענון הכרטיס") |

## מה נבדק בפועל (ומה לא)
- **נבדק** (Chromium headless דרך Playwright, 2026-09-24): טעינה ב-360/768/1024/1440 בלי שגיאות JS; `scrollWidth ≤ clientWidth` בכל הרוחבות (אין גלילה אופקית של המסמך) ואף אלמנט שחורג מהמסך (מלבד טאבים/טבלה שגוללים בתוך עצמם); זרימה אוטומטית: עריכה → רייל/גיליון → קוד מנהל → חלון מייל → Esc → חזרה עם שינויים → שמירה ויציאה; `data-v3-lock` פעיל והעמוד `inert` כשחלונית פתוחה; שתי שכבות נערמות (חלון מייל + אישור סגירה) ו-Esc פעמיים סוגר את שתיהן ומשחרר את הנעילה; ביטול+החזרה של שינוי בודד; פרופיל "קיצוני" וטופס "לקוח חדש" עם שגיאות — בלי גלישה אופקית. הבדיקה מצאה שני באגים שתוקנו: toast ופס עליון כיסו חלונית פתוחה (Q15).
- **נבדק בעין** על הצילומים: כיווני חצים (חזרה = ימינה, קדימה = שמאלה), מיקום רייל משמאל, toast בפינה שמאלית-תחתונה.
- **לא נבדק**: קורא מסך אמיתי, Safari/iOS (safe-area, `100dvh`), מקלדת וירטואלית, ניגודיות מדודה (axe) — רק עין, תנועה בפועל בהגדרת מערכת "הפחתת תנועה" (נבדק רק מתג ההדמיה בקוד). הגופן Rubik לא נטען בחלק מהצילומים (רשת), ולכן חלק מהצילומים בגופן חלופי.
- זה **לא** בנוי על ספריית `app/v3` (שלב 1 עוד בבנייה בענף אחר) — ה-CSS/JS כאן מקומיים לקובץ, ושמות ה-tokens (`--v3-*`) תואמים את החוקה כדי שיהיה קל להעביר.

---

# אב-טיפוס A4 — "לוח / ציר זמן" על הלוח החודשי (`/board`)

| קובץ | מה יש בו |
|---|---|
| `archetype-board.contract.md` | חוזה פונקציונלי ממוספר (BD-1…BD-59) מ-`origin/main`, כולל מטריצת org-variance ו"UI בלבד מול דורש נתונים" |
| `archetype-board.html` | אב-טיפוס סטטי, קובץ אחד, נתוני דמה, בלי API. החיצוני היחיד: Google Fonts |
| `BOARD-DASHBOARD-QUESTIONS.md` | שאלות BDQ-B* ו-BDQ-G* והבחירה שנעשתה — **לאישור** |
| `screenshots/board/` | צילומי הבדיקה (ראו "מה נבדק") |

**איך פותחים:** לחיצה כפולה על `archetype-board.html`. "בקרת הדגמה" (מסגרת זהב מקווקו) מחליפה פרופיל ארגון, מתגי הגדרות, נתונים (רגיל/עמוס/ריק), מצבי טעינה/שגיאה/אין הרשאה, כשל שרת, הפחתת תנועה וערכת נושא של חלוניות.

## מיפוי חוזה → אב-טיפוס (לוח)
| BD | איפה |
|---|---|
| BD-1 | בקרה → "אין הרשאה לעמוד" (מסך נעילה + קישור הביתה). השער עצמו בשרת |
| BD-2 | מחוץ לתחום — ההגדרה לא נקראת (BDQ-B15) |
| BD-3, BD-30, BD-46 | בקרה → פרופיל + מתגי `enable_alterations` / `hide_custom_spacing` / `enable_batch_print_prep` |
| BD-4, BD-5, BD-6 | מחוץ לתחום חזותי (לוגיקת טעינה) — נשמר במימוש |
| BD-7 | בקרה → "טוען…" (שלד רשת + אייקון מסתובב) |
| BD-8 | בקרה → "שגיאת טעינה" + "לנסות שוב" (תוספת, BDQ ב-§9 בחוזה) |
| BD-9 | כרטיס ההזמנה + כרטיס הפרטים |
| BD-10 | בקרה → "הסתרת הזמנות שנמסרו כולן" |
| BD-11, BD-12 | כותרת: חץ ימינה = החודש הקודם, חץ שמאלה = הבא; תווית חודש עברי + טווח לועזי |
| BD-13 | כפתור לוח שנה ליד "היום" → חלון "קפיצה לתאריך" (BDQ-B11) |
| BD-14, BD-45 | "הדפסת הזמנות להכנה" (רק כשההגדרה דלוקה) → אשף מקוצר |
| BD-15, BD-16, BD-17, BD-18 | רשת החודש: ראשון→שבת, יום באותיות + לועזי, פרשה בשבת, תגיות חג, סמן "היום" זהב (טבעת + לשונית "היום") |
| BD-19, BD-28 | מונה "N הזמנות" בתא; בתא עד 3 כרטיסים + "עוד N" (BDQ-B2) |
| BD-20 | תגית קטגוריה בכל כרטיס; אותה פונקציה בדיוק (`category()` בקובץ) |
| BD-21 | "באיחור": מסגרת plum + אייקון + מילה בכרטיס; פס + אייקון ביום |
| BD-22, BD-23 | לחיצה על כרטיס → תפריט מעוגן (בטלפון: גיליון): כרטיס הזמנה / כרטיס לקוח (רק כשיש) / כרטיס השכרה |
| BD-24, BD-25 | כפתור ⓘ בכרטיס: ריחוף = כרטיס צף; לחיצה = חלונית צפייה (BDQ-B6/B7); ציפוף רק כשמוגדר ולא מוסתר |
| BD-26, BD-41, BD-42 | כפתור הרחבה (>2 הזמנות) / "עוד N" / הקשה על יום בטאבלט-טלפון → חלון היום עם סינון + הדפסה |
| BD-27 | כפתור מדפסת בתא (רק עם ההגדרה) → הודעה "נפתחת הדפסה" |
| BD-29 | מקרא: כל הקטגוריות + "באיחור" + "היום" (BDQ-B5); בטלפון מקופל |
| BD-31 | שדה החיפוש (רגיל) + שורת "מסונן:" עם ניקוי |
| BD-32, BD-33, BD-34 | כפתור הכוכב → מצב חכם; שליחה → באנר + לוח ריק (הבאג הקיים, BDQ-B3); בקרה "כשל שרת" → חלונית שגיאה |
| BD-35 | בקרה → "חיפוש חכם זמין" (כבוי = הכפתורים נעלמים) |
| BD-36, BD-48 | כפתור הגרף בשדה החיפוש → חלונית שאלות סטטיסטיקה (מקוצרת) |
| BD-37, BD-43, BD-44 | "בכל החודשים" → חלונית צפייה (כהה/בהיר): טוען / אין תוצאות / רשימה + "לחודש הזה" + "פתיחת ההזמנה"; טקסט ריק → הודעה |
| BD-38, BD-39, BD-40 | "חיפוש מתקדם" → חלון הזנה בהיר, 2 לשוניות, סינון חי, "סיום", "ניקוי", מתג חכם |
| BD-47 | כל ההודעות = חלוניות/הודעות (לא `alert`) |
| BD-49..BD-59 | תפריט הכרטיס → "כרטיס השכרה" (מקוצר, BDQ-B12): סריקה, "ממתין לאישור" מקווקו, אישור, ביטול סריקות, סגירה עם ממתינים (3 דרכים), 9999 = בחוץ בהזמנה אחרת, ברקוד שמתחיל ב-8 = מחסן → אישור מנהל (מאשר + סיסמה מלאה), החזרה מאוחרת → "לסמן לא תקין?", ביטול השכרה/החזרה, היסטוריית פריט. BD-58 מתועד בלבד |
| תצוגות | בורר חודש / שבוע / רשימה (BDQ-B1); ברשימה — קו "היום" זהב בין עבר לעתיד |

---

# אב-טיפוס A5 — "בית / סיכומים" על `/` ו-`/dashboard`

| קובץ | מה יש בו |
|---|---|
| `archetype-dashboard.contract.md` | חוזה פונקציונלי ממוספר (DB-1…DB-61) מ-`origin/main` |
| `archetype-dashboard.html` | אב-טיפוס סטטי, קובץ אחד; שני העמודים (בורר "עמוד" בבקרה) |
| `screenshots/dashboard/` | צילומי הבדיקה |

## מיפוי חוזה → אב-טיפוס (בית/סיכום)
| DB | איפה |
|---|---|
| DB-1 | מחוץ לתחום — מסך הכניסה ב-layout |
| DB-2, DB-3, DB-4 | בקרה → "תפקיד" (הנהלה ראשית / מנהלת סניף / עובדת / לא מחוברת) + פרופיל (`require_login`) |
| DB-5 | כותרת הבית לפי `home_welcome_title` (פרופיל "מינימלי" = ברירת מחדל; "קיצוני" = 70+ תווים) |
| DB-6..DB-11 | "קיצורי דרך": 1–5 אריחים לפי תפקיד ו-`hide_internal_messaging`; רק במצב פתיחה |
| DB-12 | בורר "חיפוש רגיל / חיפוש חכם" בתוך תיבת החיפוש; המעבר שומר את הטקסט |
| DB-13, DB-17, DB-19 | חיפוש → "מחפשים…" (שדה נעול) → תוצאות; כפתור ✕ מנקה הכול |
| DB-14, DB-15 | "אחרונים" — צ'יפים (BDQ-D6) |
| DB-16 | מחוץ לתחום חזותי (פרמטר כתובת) |
| DB-18 | בקרה → "חיפוש נכשל" (מצב שגיאה + "לנסות שוב") |
| DB-20..DB-27 | 3 כרטיסי תוצאות עם מונה, כל נתון בשורה, 5 + "עוד N", מצב ריק לכל קבוצה (בקרה → "חיפוש בלי תוצאות"); סמל סטטוס לפי 4 הערכים + "פעיל" |
| DB-28..DB-39 | בקרה → "שיחה חכמה עם טבלה": בועות, העתקה, תגיות העתקה למייל/טלפון, קישור "הזמנה N", "פתיחת ההגדרה" (רק למנהלים), טבלה של 3 עמודות + שורה מורחבת + עמודת פעולה, ייצוא, אינדיקטור הקלדה, שדה המשך דביק, ניקוי; כשל שרת → בועת שגיאה |
| DB-30 | ראו BDQ-D3 (המצב החכם מוסתר כשאין גישה) |
| DB-40..DB-44 | "פתיחת ההגדרה" → חלון הזנה בהיר: טוען → שדה → שמירה → אישור מנהל; בתפקיד הנהלה ראשית — "אין הרשאה" (BDQ-D2); "לכל ההגדרות" |
| DB-45 | "פרטיות" → חלונית צפייה |
| DB-46 | מחוץ לתחום (ווידג'ט ה-AI ב-layout) |
| DB-47 | `/dashboard` בתפקיד שאינו הנהלה → מסך "פתוח להנהלה ראשית בלבד" |
| DB-48 | שבב "מחושב מחדש בכל כניסה" |
| DB-49..DB-52 | 4 אריחי "במבט אחד" (הכנסות מודגש); לחיצה → חלונית "איך זה מחושב" |
| DB-53 | "לפי אמצעי תשלום" — עמודות אופקיות + טבלה (BDQ-D7) |
| DB-54..DB-56 | "הכנסות לאורך זמן" — בורר 30 ימים / 12 שבועות / 12 חודשים + טבלה; האחרונה בזהב |
| DB-57 | בקרה → "הגרפים נטענים" / "אין תשלומים" (BDQ-D10) |
| DB-58 | אין הבדל לפי ארגון בעמוד הזה (פרופיל "קיצוני" = סכומים של 7 ספרות) |
| DB-59, DB-60 | מחוץ לתחום — קוד בלי שימוש |
| DB-61 | בקרה → "מקום שמור ל'דורש טיפול'" — מסגרת מקווקו בלי מספרים (BDQ-D1) |

## מה נבדק בפועל ב-A4/A5 (ומה לא) — 2026-09-24
- **הורץ** (Chromium headless דרך Playwright, `verify-board.cjs` / `verify-dashboard.cjs`), ב-360/768/1024/1440:
  - טעינה בלי שגיאות JS (ההודעה היחידה בקונסול: הגופן מ-Google Fonts נחסם ברשת הסביבה).
  - אין גלילה אופקית של המסמך (`scrollWidth ≤ clientWidth`), גם בפרופיל "קיצוני", יום עמוס (14 הזמנות), תצוגות שבוע/רשימה, שיחה חכמה עם טבלה, וסיכום עם סכומים של 7 ספרות. באג שנמצא ותוקן: `sr-only` בתוך טבלה גוללת ברח למסמך ב-360.
  - שכבות: `data-v3-lock` + `#page` inert בכל חלונית; גלילת עכבר לא מזיזה את העמוד; Esc סוגר רק את העליונה והפוקוס חוזר לכפתור שפתח (ⓘ בכרטיס, אריח KPI, כרטיס בתפריט); שתי שכבות נערמות (כרטיס השכרה + אישור מנהל; כרטיס השכרה + סגירה עם סריקות ממתינות).
  - מצבי חלונית: צפייה/אישור/קוד = כהה (במתג: בהיר), הזנה (חלון יום, חיפוש מתקדם, השכרה, הגדרה) = בהיר תמיד.
  - RTL: גליף "החודש הקודם" = חץ ימינה, "הבא" = חץ שמאלה (קריאת `path d`).
  - לוגיקה: סינון חי בחיפוש מתקדם + שורת "מסונן"; מעבר רגיל↔חכם שומר טקסט; קיצורי דרך לפי תפקיד (מנהלת סניף = 2, ארגון ב׳ = 1) והמצב החכם מוסתר כשה-AI כבוי; הגדרה מהירה בהנהלה ראשית = "אין הרשאה" (פער קיים), במנהלת סניף = שמירה → אישור מנהל.
- **חושב ידנית (WCAG):** plum על לבן 5.92, ink-3 על gold-100 5.17, rose-700 על rose-100 5.21, on-dark-2 על navy 9.82, שחור על זהב 8.68. העמודה הזהובה בגרף קיבלה קו navy (ניגודיות לא-טקסט).
- **נבדק בעין** על הצילומים (`screenshots/board/`, `screenshots/dashboard/`): חפיפות בכותרת התא (תוקנו), סמן "היום", גרף בטלפון (תוויות ציר קטנות — הטבלה היא החלופה הקריאה).
- **לא נבדק:** axe / קורא מסך, Safari/iOS (safe-area, `100dvh`), מקלדת וירטואלית, מגע אמיתי, רוחב 1920, תנועה בהגדרת מערכת (רק מתג ההדמיה), וגופן Rubik (לא נטען בסביבה).
