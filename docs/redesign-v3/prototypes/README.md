# אב-טיפוס מוקדם — ארכיטיפ "כרטיס פרט" (A2) על כרטיס הלקוח

| קובץ | מה יש בו |
|---|---|
| `archetype-detail-card.contract.md` | חוזה פונקציונלי ממוספר (CC-1…CC-74) שחולץ מהקוד הנוכחי של `/customers/[id]`, בלי העתקת markup/CSS/מלל |
| `archetype-detail-card.html` | אב-טיפוס סטטי, קובץ אחד, נתוני דמה, בלי API. החיצוני היחיד: Google Fonts (Rubik) |
| `PROTOTYPE-QUESTIONS.md` | שאלות שעלו (Q1–Q14) והבחירה שנעשתה בכל אחת — **לאישור** |
| `screenshots/` | 22 צילומים נבחרים מהבדיקה בדפדפן headless (שם הקובץ = רוחב-שלב) |
| `verify-prototype.cjs` | סקריפט הבדיקה שהפיק אותם (Playwright; לא חלק מהאפליקציה) |

## איך פותחים
פותחים את `archetype-detail-card.html` ישירות בדפדפן (לחיצה כפולה / גרירה לחלון). אין שרת ואין התקנה. בלי אינטרנט הגופן נופל ל-Heebo/גופן מערכת — הכול עדיין עובד.
למעלה יש **"בקרת הדגמה"** (מסגרת זהב מקווקו) — היא לא חלק מהעיצוב. היא מחליפה פרופיל ארגון, נתונים, מצבי טעינה, ערכת נושא של חלוניות, ופותחת כל סוג חלונית.

## מה לבדוק (רשימה לבעלים)
1. **שפה חזותית** מול סקיצה B: סרגל עליון navy עם קו זהב, רייל navy בצד שמאל, כפתור ראשי זהב אחד לאזור, כפתורים משניים אפרסק, Rubik גדול ומרווח.
2. **נתון בשורה**: בטאב "פרטים" — תווית קטנה מעל, ערך גדול מתחת; נתון חסר = "חסר" עם אייקון זהב (נסו בקרה → נתונים "דליל").
3. **עריכה ורשימת שינויים**: "עריכה" → שנו טלפון/עיר → כל שינוי מופיע ברייל עם "לפני → אחרי", אפשר לבטל שינוי בודד ואז להחזיר אותו. השדה שהשתנה מקווקו (ממתין). הטאב מקבל נקודה מקווקו.
4. **שמירה**: "שמירת השינויים" ברייל → פס עליון כחול עם **פס זהב יורד (15 ש׳)** — ריחוף עוצר אותו; "להיסטוריה" מעביר לטאב ההיסטוריה ושם מופיעה הרשומה החדשה.
5. **שגיאות**: מחקו שם פרטי ושמרו → הודעה בשדה עצמו + toast בפינה השמאלית-התחתונה (לא `alert`).
6. **יציאה עם שינויים**: החץ הימני (חזרה) → חלון תלת-דרכי.
7. **מייל**: "מייל ללקוח" → חלון אישור מנהל: בחירת מאשר עם חיפוש (ברירת מחדל = אני אם מורשה) + **סיסמה מלאה** (כהה/בהיר לפי המתג; הסיסמה "0000" = שגויה ומדגימה רעידה) → חלון הזנה בהיר תמיד. ✕ שומר טיוטה; "ניקוי הטיוטה" → חלון אישור שני מעל הראשון. בקרה: "כשל בפעולות שרת", "אין מאשרים מורשים". (תוקן ב-REVIEW-1 B-1/M-2)
8. **חסימה**: באנר בטאב פרטים; "ביטול החסימה" רק כש"העובד מהנהלה ראשית" מסומן בבקרה, אחרת מופיע מנעול עם הסבר.
9. **חוב**: טאב "תשלומים" — אריח היתרה (חוב/זכות/מאוזן — אייקון + מילה, לא צבע בלבד) + טבלה של 3 עמודות ושורה מורחבת.
10. **שכבות**: כל חלונית מכסה את כל המסך כולל הסרגל העליון, העמוד מאחור לא גולל ולא נגיש (inert), Esc סוגר רק את העליונה, הפוקוס חוזר לכפתור שפתח.
11. **רספונסיביות**: <1024 הרייל הופך לפס תחתון + גיליון תחתון, ו"במבט אחד" עולה מעל הטאבים; <640 חלון אישור/קוד = גיליון תחתון, חלון הזנה = מסך מלא; הטאבים גוללים אופקית.
12. **org variance**: בחרו "קיצוני" או סמנו "תוויות וערכים ארוכים" — שם של 60+ תווים, סכומים של 7 ספרות, סיבת חסימה ארוכה, שם סניף ארוך — הפריסה לא נשברת. "קטע הסכמה לדיוור" מסתיר/מציג שדה בלי להשאיר חור. "לקוח חדש" מראה כוכביות חובה שמשתנות לפי הפרופיל.
13. **RTL**: חץ "חזרה" מצביע ימינה; "פתיחת הזמנה", "להיסטוריה" ו"לפני → אחרי" מצביעים שמאלה; חיצי מקלדת בטאבים: שמאלה = הבא.
14. **תנועה**: אייקונים זזים בריחוף/מיקוד (חץ נוגע, רענון מסתובב, פעמון מתנדנד…). מתג "הדמיית הפחתת תנועה" (או הגדרת המערכת) מכבה הכול; פס הזהב ממשיך לרדת.

## מיפוי חוזה → אב-טיפוס
| CC | איפה באב-הטיפוס |
|---|---|
| CC-1 | מחוץ לתחום — שער הרשאה בשרת (layout). לא מוצג בעמוד |
| CC-2, CC-71…74 | בקרה → "לקוח חדש" (טופס בעמודה אחת) |
| CC-3, CC-28 | בקרה → "העובד מהנהלה ראשית" (מציג/מסתיר ביטול חסימה) |
| CC-4, CC-37, CC-65, CC-66 | בקרה → פרופיל הגדרות + "קטע הסכמה לדיוור"; כוכביות בטופס החדש |
| CC-5, CC-8 | בקרה → מצב "טוען…" (שלד + אייקון מסתובב) |
| CC-6, CC-7 | בקרה → "שגיאת טעינה" (מצב שגיאה + לנסות שוב / לרשימה). CC-6 (הפניה שקטה) מחוץ לתחום — ניווט |
| CC-9 | מחוץ לתחום — "נצפו לאחרונה" נשמר בדפדפן, בלי UI בעמוד |
| CC-10…CC-18 | כותרת העמוד: ראשי תיבות, שם, צ'יפים (מספר, הזמנות, חסום, עודכן). טלפון/מייל/כתובת עברו לטאב פרטים (שורה לכל נתון) |
| CC-19 | ⓘ ליד "שינויים שלא נשמרו" ברייל |
| CC-20, CC-21, CC-22 | רייל: "שמירת השינויים", "ביטול כל השינויים", מונה שינויים; בטלפון — פס תחתון |
| CC-23, CC-60, CC-61 | "מייל ללקוח" → קוד מנהל → חלון מייל; בלי מייל → חלון "ללקוח אין מייל" (נתונים "דליל") |
| CC-24 | כפתור החץ העגול בכותרת (+ חלון תלת-דרכי, Q3) |
| CC-25, CC-26 | 5 טאבים עם מונה/סימן; מקלדת. (mount של כל הטאבים = כלל מימוש, לא חזותי) |
| CC-27, CC-28, CC-62 | באנר חסימה בטאב פרטים + חלון אישור |
| CC-29 | טאב פרטים במצב תצוגה |
| CC-30, CC-41 | "עריכה" / "סיום עריכה" בכרטיס הפרטים (Q2) |
| CC-31…CC-38 | טופס העריכה; עיר/רחוב עם השלמה (datalist), ת"ז עם ⓘ, מתג דיוור |
| CC-33 | "העתקה", "שליחת מייל", "השלמה ל-@gmail.com" מתחת לשדה המייל |
| CC-39 | כפתור שמירה בתחתית טופס העריכה |
| CC-40 | כרטיס "הערות" (שורה אוטומטית מפורקת לצ'יפים + קישור להזמנה) |
| CC-42…CC-47 | טאב הזמנות (שורה מורחבת; ריק: נתונים "חדש לגמרי"; הרבה: "14 הזמנות") |
| CC-48…CC-52 | טאב תשלומים: אריחים + יתרה + טבלה מורחבת; ריק |
| CC-53…CC-57 | טאב זיכוי ובנק: טופס בנק (נכנס לאותה רשימת שינויים) + טבלת זיכויים; ריק |
| CC-58, CC-59 | טאב היסטוריה: חיפוש + ניקוי, קיבוץ לפי יום, פירוט לפני/אחרי; בקרה → "שגיאה רק בהיסטוריה"; ריק = חיפוש בלי תוצאות |
| CC-63 | שגיאות בשדה + toast (במקום חלונית הודעה) |
| CC-64 | פס עליון עם טיימר זהב (זמני בלבד, בלי פעמון — D-4) |
| CC-67 | ולידציית טלפון/מייל/ת"ז/טלפון כפול — מודגמת בשמירה |
| CC-68, CC-70 | שמירה מדומה (אין API) |
| CC-69 | בקרה → "התנגשות שמירה" (חלון עם "רענון הכרטיס") |

## מה נבדק בפועל (ומה לא)
- **נבדק** (Chromium headless דרך Playwright, 2026-09-24): טעינה ב-360/768/1024/1440 בלי שגיאות JS; `scrollWidth ≤ clientWidth` בכל הרוחבות (אין גלילה אופקית של המסמך) ואף אלמנט שחורג מהמסך (מלבד טאבים/טבלה שגוללים בתוך עצמם); זרימה אוטומטית: עריכה → רייל/גיליון → קוד מנהל → חלון מייל → Esc → חזרה עם שינויים → שמירה ויציאה; `data-v3-lock` פעיל והעמוד `inert` כשחלונית פתוחה; שתי שכבות נערמות (חלון מייל + אישור סגירה) ו-Esc פעמיים סוגר את שתיהן ומשחרר את הנעילה; ביטול+החזרה של שינוי בודד; פרופיל "קיצוני" וטופס "לקוח חדש" עם שגיאות — בלי גלישה אופקית. הבדיקה מצאה שני באגים שתוקנו: toast ופס עליון כיסו חלונית פתוחה (Q15).
- **נבדק בעין** על הצילומים: כיווני חצים (חזרה = ימינה, קדימה = שמאלה), מיקום רייל משמאל, toast בפינה שמאלית-תחתונה.
- **לא נבדק**: קורא מסך אמיתי, Safari/iOS (safe-area, `100dvh`), מקלדת וירטואלית, ניגודיות מדודה (axe) — רק עין, תנועה בפועל בהגדרת מערכת "הפחתת תנועה" (נבדק רק מתג ההדמיה בקוד). הגופן Rubik לא נטען בחלק מהצילומים (רשת), ולכן חלק מהצילומים בגופן חלופי.
- זה **לא** בנוי על ספריית `app/v3` (שלב 1 עוד בבנייה בענף אחר) — ה-CSS/JS כאן מקומיים לקובץ, ושמות ה-tokens (`--v3-*`) תואמים את החוקה כדי שיהיה קל להעביר.

---

# אבות-טיפוס A1 (רשימה) ו-A3 (אשף) — ענף `redesign/v3-proto-list-wizard`

| קובץ | מה יש בו |
|---|---|
| `archetype-list.contract.md` | חוזה פונקציונלי ממוספר (LI-1…LI-66) שחולץ מ-`main` עבור `/orders`, כולל באגים שנמצאו ו"UI בלבד מול דורש נתונים" |
| `archetype-list.html` | אב-טיפוס סטטי של רשימת ההזמנות (קובץ אחד, נתוני דמה, בלי API; החיצוני היחיד: Google Fonts) |
| `archetype-wizard.contract.md` | חוזה פונקציונלי ממוספר (WZ-1…WZ-173) שחולץ מ-`main` עבור `/orders/new`, + מטריצת org-variance + החלטות תצוגה |
| `archetype-wizard.html` | אב-טיפוס סטטי של ההזמנה החדשה (5 שלבים) |
| `LIST-WIZARD-QUESTIONS.md` | שאלות שעלו (Q-L1…Q-L15, Q-W1…Q-W19) וברירת המחדל שנבחרה — **לאישור** |
| `verify-list.cjs`, `verify-wizard.cjs` | סקריפטי הבדיקה (Playwright; לא חלק מהאפליקציה) |
| `screenshots/list/`, `screenshots/wizard/` | צילומים נבחרים מהבדיקה (שם = רוחב-שלב) |

אותה שפה חזותית, אותם tokens ואותם כללי שכבות כמו A2 (אחרי REVIEW-1/2): בסיס ה-CSS וה-LayerManager הועתקו מ-A2, והלקחים יושמו — חלון אישור = בחירת מאשר + סיסמה מלאה (או סיסמה בלבד כשזה מה שקיים היום), סטטוסים רק מהפונקציות האמיתיות, `@media` רק מהסט, מאפיינים לוגיים (חריגים מסומנים `v3-physical`), טקסט זהב על רקע בהיר ב-`gold-800`.

## איך פותחים
פותחים את הקובץ ישירות בדפדפן. למעלה **"בקרת הדגמה"** (מסגרת זהב מקווקו) — לא חלק מהעיצוב: פרופיל ארגון (א׳/ב׳/מינימלי/קיצוני), נתונים, מצבי טעינה, כשלי שרת, ערכת נושא לחלוניות אישור/קוד, הפחתת תנועה.

## A1 — מיפוי חוזה → אב-טיפוס (`archetype-list.html`)
| LI | איפה |
|---|---|
| LI-1…3 | מחוץ לתחום — שערי הרשאה בשרת; אין כפתור מוסתר בעמוד כי אין היום בדיקה בצד הלקוח |
| LI-4…6, LI-15 | מצב פתיחה: לשונית "בקרוב", מיון לפי אירוע עולה, 50 לעמוד |
| LI-7 | pill מקווקו "תאריך אירוע: מ-…" עם ⓘ + הסרה (Q-L4) |
| LI-8…11 | מחוץ לתחום החזותי (מטמון/טעינה ברקע/רענון אוטומטי) — נשמר במימוש |
| LI-12 | בקרה → "טוען…" (שלד טבלה/כרטיסים, `aria-busy`) |
| LI-13 | בקרה → "שגיאת טעינה" (הודעה + "לנסות שוב") |
| LI-14 | חיפוש בלי תוצאות / נתונים "אין הזמנות בכלל" — מצב ריק חכם (+ "ניקוי החיפוש והסינון") |
| LI-16, LI-31 | עגלה ממתינה ראשונה בעמוד + תג "שמור עוד מ:שש" שיורד כל שנייה, "השמירה פגה" |
| LI-17 | כותרת + "N הזמנות מתאימות" |
| LI-18, LI-51…55 | "סינון מתקדם" → חלון הזנה (בהיר תמיד), 2 לשוניות, בחירה מרובה של מצב פריטים, "הצגת התוצאות", יציאה עם שינויים = חלון 3 אפשרויות (Q-L5); חיפוש חכם כמתג (מוסתר בפרופיל "קיצוני" שבו AI כבוי) |
| LI-19 | "תפוסה" → הודעה (חלון נפרד, מחוץ להיקף) |
| LI-20 | "הדפסה" → טופס אחד (לא סטפר): סוג דוח + תאריכים; בפרופיל ב׳ "פירוט הזמנות להכנה" מופיע וברירת מחדל |
| LI-21, LI-66 | "ייצוא" → כמות שורות (100), אקסל/PDF, לשונית AI; מעל התקרה → חלון סיסמה (בלי בחירת מאשר, Q-L13) כשכבה שנייה |
| LI-22 | "הזמנה חדשה" — כפתור זהב יחיד |
| LI-23, LI-24 | שורת חיפוש (Enter/כפתור) + ניקוי |
| LI-25 | "שאלות סטטיסטיקה" → הודעה (רכיב נפרד) |
| LI-26…29 | Seg סטטוס; "טיוטות" רק כש-`draft_orders_show_as_deleted` כבוי (פרופיל "קיצוני"); "הכול" מנקה; מספר רק בלשונית הפעילה (Q-L7) |
| LI-30 | פרופיל ב׳: הזמנות שכל פריטיהן נלקחו לא מופיעות ב"בקרוב"/"הכול" |
| LI-32 | תג מקווקו "לא נשמר" + ⓘ עם רשימת השינויים; כרטיס "שינויים שלא נשמרו" בשורה המורחבת; מונה בכותרת. בקרה → "יש טיוטות לא שמורות" |
| LI-33, LI-34 | 5 עמודות + שורה מורחבת (Q-L1); פס צבע בתחילת השורה **וגם** תג מלל (לא צבע בלבד) |
| LI-35, LI-36 | תגי מצב = בדיוק 8 ערכי `calculateOrderStatus` + 4 ערכי `calculatePaymentStatus`, כל אחד עם אייקון |
| LI-37, LI-38 | סכומים ביתרה בשורה המורחבת; דגם "ללא שם - 817" מוצג כמספר (בנתוני הדמה של A3) |
| LI-39 | פרטי הריחוף → שורה מורחבת (Q-L9) |
| LI-40, LI-41 | לחיצה על שורה / מספר ההזמנה = פתיחת הכרטיס (הודעה בהדמיה) |
| LI-42 | "עוד" → "השכרה או החזרה" (הודעה — תהליך נפרד) |
| LI-43…45 | מחיקה: חסימה לפי מצב (חלון "אי אפשר למחוק"), אישור הרסני (פוקוס על "ביטול", רקע לא סוגר), ת״ז אחרי 401 (פרופיל ב׳/קיצוני; 123456782 = נכונה) (Q-L10) |
| LI-46…48 | כותרות ממוינות (`aria-sort`) + בורר מיון לכל 6 המפתחות (Q-L2/Q-L3); ב"לא שולם" המיון נעול עם ⓘ |
| LI-49, LI-50 | עימוד: הקודם/הבא (חצים RTL), שדה עמוד, "עמוד X מתוך N", "מוצגות A–B מתוך N"; במצב AI — בלי עמודים |
| LI-56…65 | פרופילי הבקרה (org variance); תוויות עמודה ארוכות בפרופיל "קיצוני" (LI-65) |

## A3 — מיפוי חוזה → אב-טיפוס (`archetype-wizard.html`)
| WZ | איפה |
|---|---|
| WZ-1…4 | מחוץ לתחום — שערי הרשאה בשרת; אין פרמטרי כתובת; אין שחזור טיוטה מקומית בעמוד הזה (רק טיוטת שרת, WZ-138) |
| WZ-5…8 | כותרת "הזמנה חדשה", שבב "טיוטה N" (נפתח בלשונית חדשה), "יציאה"/חזרה → חלון אישור לפי מצב הטיוטה |
| WZ-9…12 | אחרי יצירה: מסך "עוברים ל…" לפי `order_new_redirect_screen` (קיצוני: כרטיס הלקוח) + NoticeBar עם פס זהב (Q-W15) + הודעת הדפסה אוטומטית (פרופיל ב׳) |
| WZ-13…19 | סטפר v3 (סקיצה B): done=navy+✓, cur=זהב+טבעת, נעול=מקווקו+מנעול+ⓘ סיבה; ערך מתחת לכל צומת; "שלב X מתוך 5"; כותרת שלב כשאלה; פס מקטעים בטלפון |
| WZ-20…26 | StepNav: המשך (זהב, בתחילת השורה, חץ שמאלה) + חזרה/ביטול (בסוף, חץ ימינה); כפתור כבוי + ⓘ סיבה; דביק בטלפון; בשלב 5 "יצירת ההזמנה"; קפיצה בסטפר = אותן בדיקות (Q-W3) |
| WZ-27…36 | שלב 1 "לפי טלפון": ≥9 תווים, תוצאות ככרטיסים (כל נתון בשורה), "נמצאו 2", חסר ללקוח + "עריכת הפרטים", "זו לא הלקוחה — לקוח חדש" (טלפונים לבדיקה בבקרה) |
| WZ-37…42 | "חיפוש בשם": combobox עם טעינה/ריק/חסומה; אותה בדיקה כמו בטלפון (Q-W4) |
| WZ-43…53 | "לקוח חדש": שדות חובה לפי ההגדרות, כוכבית דינמית לקבוצה, הודעת "לפחות אחד מבין", השלמה ל-gmail, "פרטים נוספים" נפתח לבד כשצריך, Enter עובר שדה, שגיאות בשדה (Q-W17), חלון לקוחות כפולים |
| WZ-54, WZ-55 | חסומה → חלון אישור (בחירת מאשר + סיסמה); חסר קבוצת קשר → חלון אישור; חסרים שדות → אישור פשוט; `strict_mandatory_fields` → חסימה (קיצוני + 0524444444) |
| WZ-57…67 | שלב 2: אירוע רגיל / חו״ל (טווח, בדיקת "חוזרת לפני יוצאת"), תאריך עברי מתחת (Q-W1); שינוי תאריך עם סל → בדיקת מלאי (בהדמיה: יום 7/14/21/28 בחודש = חוסר) |
| WZ-68…72 | "הערות וריווח ימים": ריווח קטן מהקודם → "בדקתם את המלאי?" → חלון אישור (`feature:special_spacing_approval`); מוסתר בפרופיל ב׳ |
| WZ-73…82 | "משלוח וסניף" (פרופיל ב׳/קיצוני): הזמנה טלפונית, סניף, סניף לקיחה, משלוח, כיוון, עיר מרשימת המחירים, כתובת חובה כשהעיר שונה, יום לפני; "המשך" נעול עם הסיבה (Q-W14) |
| WZ-83…106 | שלב 3: חיפוש דגם (שם/מספר, Enter), מידות עם "N פנויות"/"אין פנויות" (+ציפוף, Q-W12), תיקונים (מוסתר בפרופיל ב׳, Q-W13), מגבלת פריטים (6/3), סל עם תפוסה/עריכה/הסרה, "ביטול העריכה" (Q-W10), "מחשבים מחירים…", רענון זמינות |
| WZ-107…113 | שלב 4: בלוקים עם "עריכה" → חזרה ישירה לסיכום; שורת משלוח + בלוק משלוח (Q-W9) |
| WZ-114…123 | שלב 5: אריחי לחיוב/שולם/נשאר (אייקון+מלל), סכום, אופן תשלום מהרשימה, הערה, "רישום התשלום" / "חיוב באשראי", תשלומי כרטיס נעולים (🔒) |
| WZ-118…121 | חלון אשראי (בהיר תמיד): מספר מקובץ, תוקף MM/YY, סכום, 1–12 תשלומים, "העברת כרטיס בקורא" (שכבה שנייה), דחייה/כשל בתוך החלון |
| WZ-124…129 | "יצירת ההזמנה": בדיקות מקדימות (תאריך שעבר → אישור), תשלום לא מלא → חלון עם "בחירת יציאה באישור מנהל", אישור לפי `PAYMENT_APPROVAL_LEVEL` |
| WZ-130…136 | שמירה: מסך "יוצרים את ההזמנה" חוסם; בקרה → הזמנה כפולה (2 אפשרויות), מלאי השתנה (חזרה לסל), כשל (callout בראש השלב) |
| WZ-137…145 | busy, טיוטת שרת אוטומטית (1.5ש׳ אחרי לקוח+תאריך+פריט → הודעה פעם אחת + שבב), Esc בשכבות, מצבי טעינה בכל רשימה |
| WZ-146…173 | באגים — כל אחד מופיע בחוזה; אלה שנפתרו בתצוגה בלבד מסומנים בשאלות Q-W2…Q-W14 |
| טבלת הגדרות (§I) | פרופילי הבקרה; ראו מטריצת org-variance בסוף החוזה |

## מה נבדק בפועל (A1 + A3) — 2026-09-24
- **הורץ** (Chromium headless דרך Playwright 1.56, `verify-list.cjs` + `verify-wizard.cjs`) ב-360/768/1024/1440: 0 שגיאות JS; `scrollWidth ≤ clientWidth` ו-0 אלמנטים חורגים בכל רוחב ובכל שלב שצולם (כולל פרופיל "קיצוני").
  - A1: הרחבת שורה (טבלה / כרטיס בטלפון), תפריט "עוד" (popover; בטלפון — גיליון), מחיקה → חלון אישור כהה עם פוקוס על "ביטול" ונעילת עמוד (`data-v3-lock` + `inert`), סינון מתקדם בהיר → Esc עם שינויים = חלון שני (2 שכבות) → "הצגת התוצאות" סוגר הכול, ייצוא 500 שורות → חלון סיסמה כשכבה שנייה → Esc פעמיים משחרר נעילה, אשף הדפסה בפרופיל ב׳ (ברירת מחדל "פירוט הזמנות להכנה"), טעינה/שגיאה/ריק, 1,240 הזמנות = 12 עמודים.
  - A3: לקוחה חסומה → חלון אישור כהה; לקוחה רגילה → שלב 2; תאריך → פריטים → דגם + 2 מידות → סל → טיוטה נשמרה (שבב) → סיכום → תשלום → חלון אשראי בהיר עם שגיאות שדה → חיוב → ההזמנה נוצרה + נעילה שוחררה; פרופיל ב׳: 2 לקוחות לאותו טלפון, משלוח לעיר אחרת → "המשך" נעול עד שממלאים כתובת, "יציאה באישור מנהל" שומר; פרופיל "קיצוני": לקוח חדש עם שגיאות.
- **נבדק בעין** בצילומים: כיווני חצים (חזרה → ימינה, המשך/הבא → שמאלה), סטפר done/cur/נעול, toast בפינה השמאלית-התחתונה, NoticeBar עליון.
- **לא נבדק**: axe/קורא מסך, Safari/iOS, מקלדת וירטואלית, ניגודיות מדודה (הצבעים = אותם tokens שעברו חישוב ב-REVIEW-2), הפחתת תנועה אמיתית של המערכת (רק מתג ההדמיה), סקירה עצמאית (S12) — עוד לא נעשתה. ערכי הפרופילים לא אומתו מול DB.
