# TOUCH-FORMS-QUESTIONS — שאלות שעלו בבניית A6 (מגע) ו-A7 (טפסים), וברירת המחדל שנבחרה

> הסוכן עבד בלי אפשרות לשאול. בכל שאלה נבחרה האפשרות הבטוחה ביותר שתואמת את הקוד ב-`main` ואת המסמכים, והיא מה שמוצג באב-הטיפוס. **כל שורה מחכה לאישור או לתיקון של הבעלים.**
> קבצים: `archetype-touch.html` + `archetype-touch.contract.md` (TK-…), `archetype-forms.html` + `archetype-forms.contract.md` (FM-…). תאריך: 2026-09-24.
> סוג: **UI** = תצוגה בלבד, אותן קריאות · **התנהגות** = משנה מה המשתמש יכול לעשות (R8 — דורש אישור) · **באג** = התנהגות קיימת שנראית שגויה (לא תוקנה) · **חוקה** = הצעה לתיקון מסמך.

## A6 — עמדת לקוחות ושעון נוכחות
| # | שאלה | מה נבחר באב-הטיפוס | למה | סוג |
|---|---|---|---|---|
| TQ-1 | החוקה (A6) דורשת "מצב idle / שומר מסך / הפניה אוטומטית". **אין דבר כזה בקוד** (TK-54). | לא ממומש כהתנהגות. בבקרת ההדגמה יש מתג **"הצעה"** כבוי: אחרי 20 ש׳ בלי מגע מופיע מסך "נוגעים כדי להתחיל", והנגיעה מחזירה לשלב 1 ומנקה סינונים ושיחה. מסומן "הצעה — לא קיים היום". | "רק מה שיש בקוד". שומר מסך שמאפס את העמדה = שינוי התנהגות. | התנהגות |
| TQ-2 | החוקה (A6) מבקשת "קלט PIN/קוד בקיפד". אבל הסיסמאות **חופשיות** (אותיות+ספרות), והקוד הקצר = 4 התווים **האחרונים של הסיסמה** — לא רק ספרות (TK-55, TK-62). | **אין קיפד מספרי.** שדה סיסמה גדול (56) עם כפתור הצגה, `dir=auto`, מקלדת המכשיר. | קיפד מספרי ישבור סיסמאות אמיתיות (אותה טעות כמו REVIEW-1 B-1). | חוקה |
| TQ-3 | חלון שחרור המסך שולח תמיד `password` ולא `pin` → **הקוד הקצר של מחשב מערכת לא עובד בו**, גם במחשב מאושר (TK-49). | מוצג כמו היום + ⓘ "כאן צריך את הסיסמה המלאה". | שינוי = קריאה אחרת לשרת. | באג/התנהגות |
| TQ-4 | בשלב 1 **כל שינוי של אחת משלוש הרשימות (יום/חודש/שנה) מעביר מיד לשלב 2** — אי אפשר לבחור יום+חודש+שנה ברצף (TK-11). | נשמר כמו היום (כל בחירה = מעבר). | R8. הצעה לבעלים: כפתור "הצגת שמלות" אחרי הרשימות, ולחיצה על יום בלוח = מעבר מיידי. | התנהגות |
| TQ-5 | **רישום עצמי בעמדה נכשל תמיד בארגון שבו `require_customer_id_number` דלוק** (אין שדה ת"ז בטופס), ובכל ארגון **בלי מייל** נכשל בגלל ברירת המחדל של `mandatory_field_groups` (`טלפון נוסף / אימייל` — ואין בטופס טלפון נוסף) (TK-16). | הטופס שולח את אותם שדות; השגיאה של השרת מוצגת כמו שהיא (מחרוזת אחת) בתיבת שגיאה מתחת לטופס. בהדגמה (ארגון ב׳) רואים בדיוק את זה. | לא מוסיפים שדות (שינוי נתונים). **ממליץ לבעלים לבדוק בהקדם** — אם ההגדרה דלוקה בארגון שבו הרישום העצמי פעיל, הפיצ'ר לא עובד בכלל. | באג |
| TQ-6 | כרטיס "הזמנה עצמאית" (מושבת) מוצג לפי `kiosk_customer_self_service`, לא לפי `kiosk_allow_self_order` (שנקרא ולא בשימוש) (TK-17). | מוצג כמו היום, מושבת, עם ⓘ. | R8. | באג |
| TQ-7 | כשל בטעינת הקטלוג (כולל 401 כשחובת כניסה דלוקה ואין עובדת מחוברת) מוצג היום כ"לא נמצאו דגמים" (TK-25). | מצב שגיאה נפרד + "לנסות שוב"; כשאין עובדת מחוברת — "העמדה צריכה עובדת מחוברת". | אותה קריאה, `res.ok` זמין. | UI |
| TQ-8 | שעון "קריא ממרחק" צריך גופן גדול מסולם החוקה (display 36). | token חדש מוצע `--v3-fs-kiosk-clock: 56px` (36 במסך נמוך). | החוקה §ב.4 לא כוללת גודל לעמדה. | חוקה |
| TQ-9 | החוקה (§ג.1) אומרת "אין סרגל עליון" במסכי kiosk, אבל **בשעון הנוכחות הסרגל הגלובלי מוצג היום**, ובעמדה — רק כשהיא **לא** נעולה (TK-3, TK-57). | כמו היום: שעון = עם סרגל עליון (מקוצר); עמדה פתוחה = עם סרגל; עמדה נעולה = בלי. | R8 + "רק מה שיש בקוד". לאשר אם השעון צריך מצב מסך מלא. | חוקה |
| TQ-10 | צבע ההודעה בשעון נקבע לפי המילה "שגיאה" — לכן "בחרו עובד…" ו"היציאה בוטלה" מופיעות בצבע הצלחה (TK-68). | צבע לפי סוג ההודעה: הצלחה (כחול+✓, נעלמת אחרי 3 ש׳ עם פס זהב יורד), שגיאה (זהב+⚠), "שימו לב" (אפרסק מקווקו). | כל ההודעות נוצרות בעמוד. | UI |
| TQ-11 | בדיקת "משפחות שלא החזירו" ביציאה מדלגת בשקט כשאין עובדת מחוברת ויש חובת כניסה (TK-69). | לא מוצג (בהדגמה: עם "אין עובד מחובר" הבדיקה לא קופצת, כמו היום). | R8. לדווח לבעלים. | באג |
| TQ-12 | תצוגת טבלה היום = 6 עמודות; החוקה C-1.5 = עד 5. | 5: דגם (תמונה+שם+מספר בשורה מתחת) · קטגוריה · פנויות · מידות · הדפסה. | C-1.5. | UI |
| TQ-13 | בתצוגת "רשימה", דגם בלי שם אמיתי מוצג היום רק כעיגול עם המספר. | "דגם 521" + קטגוריה ליד העיגול. | שם נגיש לכל שורה. | UI |
| TQ-14 | בסרגל התוצאות היום רוב הכלים = אייקון בלבד עם `title` (ריחוף) — במסך מגע אין ריחוף. | כל כלי = אייקון + מילה (סינון, גודל, תאריך אחר, רענון, הדפסה, למערכת, נעילה). בטלפון/טאבלט = רשת כפתורים. | A6: "בלי hover-only". | UI |
| TQ-15 | "פרשת השבוע" בתחתית הלוח מחושבת היום מ-`@hebcal`. | באב-הטיפוס לא מחושבת (ⓘ מסביר). הלוח העברי עצמו מחושב אמיתי מהדפדפן (`Intl` לוח עברי). | קובץ עצמאי בלי ספריות. | תיעוד |
| TQ-16 | חלון "הזמנות לדגם" (עובדות בלבד) — כהה או בהיר? | צפייה בלי שדה קלט → עוקב ערכת נושא (כהה/בהיר), מסך מלא בטלפון; פתיחת הזמנה = חלון חדש (כמו היום). | חוקה §ד.5. | UI |
| TQ-17 | פאנל הסינון: בצד (≥1024) או גיליון? | ≥1024 = פאנל בצד ההתחלה; <1024 = גיליון תחתון בהיר (יש בו חיפוש). | חוקה §ד.5/§ד.10. | UI |
| TQ-18 | ערכי הפרופילים "ארגון א׳/ב׳" בבקרה. | ערכי דמה שנגזרו מחוקה §ט.3 — **לא אומתו מול DB**. | אין גישה בריצה הזו. | תיעוד |
| TQ-19 | שיחת העוזר נשמרת היום ב-`localStorage` אבל לא נקראת בחזרה (TK-23). | לא מוצג (אין שחזור שיחה). | אין השפעה נראית. | תיעוד |
| TQ-20 | מסך מגע גבוה/נמוך — החוקה מגדירה רק breakpoints של רוחב. | שתי שאילתות גובה מקומיות (`max-height:760px` ללוח, `700px` לשעון) להקטנת ריווח. | אין בסט. להוסיף "short" לסט או לוותר. | חוקה |

## A7 — הפרופיל שלי
| # | שאלה | מה נבחר באב-הטיפוס | למה | סוג |
|---|---|---|---|---|
| FQ-1 | היום אין מעקב שינויים, אין סימון שדה ששונה, אין "ביטול שינויים" (FM-29). | שדה ששונה = מקווקו אפרסק + "לא נשמר" + "היה: …"; כרטיס עם שינוי = מסגרת מקווקו; פס שמירה דביק עם מונה ושמות השדות; "ביטול השינויים" עם חלון אישור. | A7 בחוקה + D-3. אותו PUT. | UI |
| FQ-2 | יציאה ("חזרה") עם שינויים שלא נשמרו — היום נזרקים בשקט (FM-9). | חלון תלת-דרכי: שמירה ויציאה / יציאה בלי לשמור / להישאר. | חוקה A2/A7, כמו Q3 של A2. **תוספת התנהגות.** | התנהגות |
| FQ-3 | D-3: "מתגים בודדים = שמירה מיידית עם ביטול ב-toast". מתג "התראות במייל" בפרופיל **נשמר היום רק ב"שמירה"** (FM-18). | נשאר חלק מהשמירה המפורשת (מקווקו כשהשתנה, "נשמר יחד עם שאר הפרטים"). | R8 — שינוי מודל שמירה. | התנהגות |
| FQ-4 | בדיקת המייל: היום זו בדיקת הדפדפן (`type=email`), לא הכלל של כרטיס הלקוח. | בדיקה באתר עם **אותו כלל בדיוק של הדפדפן** (שדה מבחן נסתר), הודעה בשדה + toast. לא מחמיר. | REVIEW-1: "ולידציה לא חמורה מהמציאות". | UI |
| FQ-5 | כפתור השמירה פעיל גם כשאין שינויים (היום). | נשאר פעיל (אותה קריאה); הפס אומר "הכול שמור". | R8 — לא משנים מצבי שמירה. | UI |
| FQ-6 | הרמז אומר "עד 5MB" אבל **אין בדיקת גודל או סוג** בלקוח או בשרת (FM-40); התמונה נשמרת כ-data URL בתוך ה-PUT. | הרמז "PNG או JPG" בלבד, בלי הבטחת גודל. | לא מבטיחים מה שלא נאכף. לבעלים: האם להוסיף מגבלה (שינוי התנהגות). | באג |
| FQ-7 | אזור התמונה נראה היום "גררו או לחצו" אבל **אין גרירה** (FM-42). | כפתור "בחירת תמונה" בלבד. | לא מבטיחים גרירה. | UI |
| FQ-8 | שגיאת שרת בטעינה מוצגת היום כ"צריך להתחבר" (FM-6). | מצב נפרד "לא הצלחנו לטעון" + "לנסות שוב". | הסטטוס זמין בלקוח. | UI |
| FQ-9 | החלפת סיסמה: חלונית או אזור בעמוד? | אזור נפתח בעמוד (כמו היום), פעולה מיידית נפרדת מהשמירה; הצלחה = toast (פעולה מיידית), לא הפס העליון. בדיקת אורך ≥4 לפני שליחה (אותו כלל של השרת). | D-3 + R8. | UI |
| FQ-10 | תוכן עניינים צדדי ("בעמוד הזה") ב-≥1024 עם נקודה על קבוצה שיש בה שינוי. | נוסף. | A7 עמודה 720 משאירה שוליים; עוזר לעמוד ארוך. | UI |
| FQ-11 | "שם מלא" הוא שדה נפרד שלא מתעדכן משם פרטי+משפחה (FM-12). | נשאר נפרד + ⓘ שמסביר. | שינוי = שינוי נתונים. | תיעוד |
| FQ-12 | Enter בשדה טקסט = שמירה (היום, `submit`). | נשמר. | R8. | תיעוד |
| FQ-13 | ה-toast ישב על כפתור "שמירה" בפס הדביק (נמצא בבדיקה בדפדפן). | כשיש פס שמירה, ערימת ה-toast עולה מעליו (לפי גובה הפס בפועל). | חוקה §ד.6 מדברת רק על StepNav/bottom-sheet. **להוסיף "פס שמירה דביק" לכלל.** | חוקה |
| FQ-14 | פרופיל "ארגון ב׳" בהדגמה מסתיר את התמונה (`show_employee_profile_image=false`). | ערך דמה — לא אומת מול DB. | אין גישה. | תיעוד |
