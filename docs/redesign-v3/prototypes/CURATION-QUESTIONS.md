# CURATION-QUESTIONS — questions met during the best-of pass, with the default taken

| # | Question | Default taken (so work could continue) |
|---|---|---|
| CQ-1 | The owner said "keep the nicer things" about the customer card. Does that extend to details that live inside owner-listed shared components (rail tiles with big numbers, labelled title buttons, compact item rows, who·time in history)? | **No.** The owner also said the shared components must be identical to the order card. Those details are listed in BEST-OF.md §9 as promotion candidates instead of being re-implemented on one page. |
| CQ-2 | Should restored helpers that every page will need (content link `.olink`, ringed empty state) go into the shell block now? | **No** (the brief forbids it). They are page-only, marked `/* CURATION Rn */`, and listed in §9. |
| CQ-3 | Board: is the gold "today" tab a chip under C-1.14? | Treated as **a live marker, one per screen** (like a status of the day) — allowed within the ≤3 rule. Not counted by the chip selector. |
| CQ-4 | Forms: the original had a sticky side index. The rail now owns that side. | Rebuilt as a card at the top of the main column (plain text links + unsaved marks), not sticky. If the owner wants it sticky on desktop, it needs a place in the rail (shared). |
| CQ-5 | Touch T2 size tiles: are bordered size boxes "chips"? | **No** — a size and its free-unit count are data, not a status; the boxes have no fill colour or status meaning. The table view keeps the text line. |
| CQ-6 | Login branch has no shell block. Rebuild it on the shell here? | **No** — the brief says observations only; see BEST-OF.md §8. |
