# REVIEW-forms-2 — סבב שני (אימות התיקונים) — A7 "הפרופיל שלי"

> סוקר עצמאי, 2026-09-24, **סבב 2 מתוך 2**. בסיס: `44aa6d9` + התיקונים של הסוקר ב-`archetype-forms.html` וב-`archetype-forms.contract.md` (§יא בחוזה מפרט אותם). לא בוצע commit (הרכז מבצע).
> צילומי הסבב: `screenshots/review-forms/` — `1440-save-fail-session.png`, `1440-state-error.png`, `r2-360-toast-action.png`, `r2-full-360.png`, `r2-full-1024.png` (Playwright/Chromium headless; גופן Rubik לא נטען — אין רשת לגופנים בסביבה).

## פסק דין: **PASS-with-minors**
אין חוסמים ואין מהותיים פתוחים. נשארים הקלים m-9…m-14 מ-REVIEW-forms-1 (מתועדים, לטיפול במימוש/ב-tokens) ו-4 שאלות פתוחות עם ברירת מחדל (RQ-1…RQ-4), בנוסף לשאלות FQ-1…FQ-14 של הבונה שממתינות לבעלים.

## אימות כל ממצא מ-REVIEW-forms-1
| # | מה הורץ | תוצאה |
|---|---|---|
| M-1 | `flow2.cjs` ב-360 וב-1440: פתיחת אזור הסיסמה → סיסמה נוכחית ריקה + חדשה "abcd12" → "החלפת הסיסמה" | הכפתור עובר ל"מחליפים…" (כלומר נשלח, אין חסימה בלקוח); אחרי תשובת השרת המדומה: "צריך להקליד את הסיסמה הנוכחית." והפוקוס בשדה. עם "אחרי איפוס" דלוק — אותה פעולה מצליחה (האזור נסגר, טוסט "הסיסמה הוחלפה"). ⓘ קבוע קיים ליד התווית. ✔ |
| M-2 | אותה זרימה עם בקרת "תשובת השרת" = 500 / אין חיבור / 401, לסיסמה ולשמירה | סיסמה: "תקלה בשרת…", "אין חיבור לשרת…", "צריך להתחבר מחדש כדי להחליף סיסמה." שמירה: "השמירה לא הצליחה" + "לנסות שוב", "אין חיבור לשרת" + "לנסות שוב", "צריך להתחבר מחדש" + "התחברות"; בכל המקרים הפס נשאר "שינוי אחד שלא נשמר". ✔ (`1440-save-fail-session.png`) |
| M-3 | `toast360.cjs` ב-360: כשל רשת בשמירה | רוחב המלל 244px (לפני: עמודה צרה של מילה-שתיים); הפעולה בשורה שנייה (top 573 > תחתית המלל 565); הטוסט מעל פס השמירה (629 < 651). ✔ (`r2-360-toast-action.png`) |
| m-1 | פס שמירה / NoticeBar | "שינוי אחד שלא נשמר", "2 שינויים שלא נשמרו", "עודכן פרט אחד." ✔ |
| m-2 | `profileImage` = כתובת רגילה / data URL | כתובת רגילה → "רפ" בכרטיס ובסרגל, "הסרה" מוצג (כמו היום); data URL → `<img>` בשניהם ✔ |
| m-3 | לחיצה על העין של "סיסמה חדשה" | `#i-eye-off`, `aria-pressed=true`, `type=text` ✔ |
| m-4 | `querySelectorAll('[novalidate]')` | 0 ✔ |
| m-5 | מצב "שגיאת שרת בטעינה" | "יש תקלה בחיבור או בשרת. הפרטים שלך לא נפגעו. נסו שוב בעוד רגע." ✔ |
| m-6…m-8 | חוזה | תועדו ב-§י ו-FM-17א ✔ |
| רגרסיה | `check.cjs` מחדש + `flow2.cjs` | ערכי `@media`: רק 640 / 1023 / 1024 (בסט); 0 חצים בטקסט (הוסר `→` שנכנס בהערת קוד שלי ונתפס בבדיקה); מאפיינים פיזיים = רק עוגן `left` של הטוסט; 0 pageerror; אין גלילה אופקית ב-360/768/1024/1440 ובפרופיל "קיצוני" ב-360/1440; חלון החזרה: `data-v3-lock`, סרגל `inert`, כהה; Esc משחרר נעילה ומחזיר פוקוס ל"חזרה"; 0 כפתורים בלי תווית ✔ |

## קלים שנשארו (לא חוסמים)
- m-9 ⓘ בתוך `<label>` (שם נגישות ארוך) · m-10 תיבות הסבר קבועות (RQ-2) · m-11 כפתור קובץ = label לא ממוקד · m-12 אזורים חיים מקוננים · m-13 גבול שדה 1.60:1 ומסגרת שגיאה 2.95:1 (token משותף — לבונה ה-tokens) · m-14 קו "לא נשמר" 2.04:1 (מסומן גם במלל).
- הערה: שני כפתורי זהב גלויים בו-זמנית כשאזור הסיסמה פתוח ("החלפת הסיסמה" + "שמירת השינויים" בפס) — אזורים נפרדים, תואם C-1.1 "אחד לאזור". לא שונה.
- לא נבדק (אין בסביבה): axe, קורא מסך, Safari-iOS, מקלדת וירטואלית, רוחב 1920, ערכי ההגדרות האמיתיים של שני הארגונים.

## שאלות פתוחות לבעלים (ברירת המחדל שבה ייבנה הפיילוט)
| # | שאלה | ברירת מחדל |
|---|---|---|
| RQ-1 | הודעות שגיאה בסיסמה: מלל חדש או `message` של השרת? | `message` של השרת (מבחין בין שני סוגי 401) |
| RQ-2 | תיבות "פרטים / סיסמה" בראש העמוד | להשאיר |
| RQ-3 | Enter בשדה סיסמה (היום שומר את טופס הפרטים) | לא לעשות כלום |
| RQ-4 | רמז "PNG או JPG" מול `accept="image/*"` | להשאיר כהמלצה |
| FQ-1…FQ-14 | שאלות הבונה (`TOUCH-FORMS-QUESTIONS.md`) | כמו שנבחר שם; החשובות: FQ-2 (חלון יציאה 3-דרכי = תוספת התנהגות), FQ-6 (אין מגבלת גודל לתמונה) |

## לידיעת הבעלים — ממצאים בקוד הפרודקשן (לא תוקנו; R8)
- `profileImage` נשמר כ-data URL מלא בלי מגבלת גודל בלקוח או בשרת (`page.js:105-114`, `me/profile/route.js:79-81`), ונכנס כמו שהוא ל-`changesJson` של AuditLog בכל שמירה (F-47/F-56) — תמונת טלפון של כמה MB מנפחת את שורת העובד, את גוף ה-PUT ואת יומן ההיסטוריה (רלוונטי למכסת ה-egress של Neon).
- הרמז "עד 5MB" ו"גרור/י" בעמוד הנוכחי לא נאכפים/לא ממומשים (F-33).
- Enter בשדה סיסמה שולח את טופס הפרטים (RQ-3).
- `lib/howToGuide.js:133` מפנה לקישור מהיר לפרופיל בדף הבית שלא קיים (F-54).
