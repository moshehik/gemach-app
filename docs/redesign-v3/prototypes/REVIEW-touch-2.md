# REVIEW-touch-2 — סבב שני (אימות התיקונים)

> סוקר עצמאי, 2026-09-24. בסיס: `44aa6d9` + תיקוני REVIEW-touch-1 (עריכה ישירה ב-`archetype-touch.html` וב-`archetype-touch.contract.md`, לא commit — המתאם מבצע commit).
> צילומי הסבבים: `screenshots/review-touch/` (`r1-*` = לפני, `r2-*` = אחרי; Playwright/Chromium headless).

## פסק דין: **PASS-with-minors**
אין חוסמים ואין מהותיים פתוחים. נשארים 11 קלים פתוחים (m-13…m-23 ב-REVIEW-touch-1) ו-6 שאלות (OQ-1…OQ-6) בנוסף ל-TQ-1…TQ-20 של הבונה. אפשר להתקדם לפיילוט A6 עם ברירות המחדל שנרשמו.

## אימות כל ממצא מהותי
| # | מה הורץ | תוצאה |
|---|---|---|
| M-1 | ברירת מחדל → שדה העוזר קיים; כיבוי "לאיש הצוות המחובר יש הרשאת עוזר חכם" → נעלם; "אין עובד מחובר" → נעלם. TK-18 + שורת המטריצה נוסחו מחדש | ✔ (`true / false / false`) |
| M-2 | 360×640 שלב 2: `.screen` `overflow-y:auto`, גובה גלוי 582px, התוכן נגלל והכותרת יוצאת מהמסך (כרטיס ראשון ב-146px אחרי גלילה); אין גלילת מסמך/אופקית; חלון פתוח → גלילת עכבר לא מזיזה את `.screen` (499→499) ו-scrollTop נשמר אחרי סגירה. 768×1024 ומעלה — ללא שינוי (הכותרת קבועה) | ✔ (`r2-360x640-catalog-top.png`, `r2-360x640-catalog-scrolled.png`) |
| M-3 | רינדור הגליפים: `login` = חץ שמאלה אל דלת בשמאל, `exit` = חץ שמאלה שיוצא מדלת בימין; `[data-punch=IN]`→`#i-login`, `OUT`→`#i-exit` | ✔ (`r2-glyphs-after.png`) |
| M-4 | axe על 10 המצבים של סבב 1 + 8 מצבים נוספים (חלון הזמנות: טעינה/נטען/ריק/שגיאה; חלון שחרור: רשימה בכשל, "אין שם כזה"; שעון: רשימה בכשל, "אין שם כזה") | ✔ **0 serious/critical** בכולם. מקלדת: פוקוס על "שם הדגם" + Enter → חלון נפתח; Esc → הפוקוס חוזר לכפתור השם |
| M-5 | חלון הזמנות כהה: "ממתין לזיכוי"/"לא שולם" rose-700 על rose-100 (5.21:1), "שולם חלקי" gold-800 על gold-100 (6.18:1); טעינה/ריק/שגיאה בצבעי החלונית | ✔ (`r2-1440-viewer-dark-chips.png`, `r2-1440-viewer-error-dark.png`) |
| M-6 | "כשל בפעולות שרת" → חלון ההזמנות: "לא הצלחנו לטעון את ההזמנות" + "לנסות שוב" (אחרי כיבוי הכשל — נטען); "שגיאת טעינה" → שעון: "לא הצלחנו לטעון את השמות" + "לנסות שוב" (12 שמות אחרי); חלון שחרור: "לא הצלחנו לטעון את רשימת הצוות" + "לנסות שוב" (12 אחרי) ושחרור מצליח | ✔ (`r2-1440-punch-names-error.png`, `r2-1440-unlock-list-error-dark.png`) |

## אימות הקלים שתוקנו
| # | תוצאה (הורץ) |
|---|---|
| m-1 | צ'יפי מידה: `34…48` בלי "כללי" ב-360/1024/1440 ✔ |
| m-2 | שורת הזמנה: "הזמנה 53180 · רחל כהן" ✔ |
| m-3 | כיבוי "תאריך לועזי בלוח מוצג" → 0 מספרים לועזיים בתאים, התחתית "פרשת השבוע" בלבד ✔ (`r2-1440-cal-no-gregorian.png`) |
| m-4 | אין "עובדת"/"מי את" במלל הצוות (נשאר רק בהערת קוד) ✔ |
| m-5 | `scrollIntoView` על השגיאה — קוד בלבד; **לא** צולם מחדש ב-360 |
| m-6 | אחרי נעילה הפוקוס על "שחרור", אחרי שחרור על "נעילה ללקוחה" ✔ |
| m-7 | Esc סוגר את חלונית "גודל" והפוקוס חוזר לכפתור ✔ |
| m-8 | ארגון ב׳ + כשל שרת + "יציאה" → אין חלון כובסת, הודעה "אין חיבור. נסו שוב." ✔ |
| m-9 | הצעת שומר המסך מאפסת גם רישום ותאריך — קוד בלבד (לא הורץ 20 ש') |
| m-10…m-12 | החוזה: הפניות, TK-8/18/30/45/48/49/52/59, TK-71/72, שורות מטריצה ו-§ד חדשות ✔ (קריאה) |

## רגרסיה (הורץ אחרי כל התיקונים)
- `check.cjs` ב-360/768/1024/1440: 0 שגיאות JS, 0 גלילה אופקית, 0 יעד <44, 0 כפתור בלי תווית, 0 breakpoint חריג, מאפיינים פיזיים = רק `left` של toast (חריג מתועד), 0 z-index מספרי, 0 `alert`.
- זרימת סבב 1 המלאה ב-360/1024/1440 עוברת (scrim מעל הסרגל, נעילת גלילה, Esc, נעילה/שחרור כהה+בהיר, סיסמה שגויה, גיליון סינון בהיר, דחיית שרת ברישום, חלון כובסת כהה, יציאה/כניסה) — 0 `pageerror`.
- org variance (פרופיל קיצוני, 60 דגמים, 0 דגמים, שגיאה) ב-360/768/1024/1440 — 0 גלילה אופקית.
- לא הורץ: קורא מסך, מכשיר מגע אמיתי, Fullscreen API אמיתי, `verify-touch-forms.cjs` של הבונה (כותב לתיקיות צילומים מחוץ לתחום העריכה שלי; הסלקטורים שבו — `[data-model] .d-name`, `#dProfile`, `#dScreen`, `#dLoad` — נשמרו).

## קלים שנשארו פתוחים (לא חוסמים)
m-13 hover דביק במגע · m-14 כפתורי-אייקון שמשמעותם ב-tooltip · m-15 `send` לא מתהפך (כל אבי-הטיפוס) · m-16 גדלי גופן מחוץ לסולם (TQ-8) · m-17 ברכת פתיחה של העוזר · m-18 הודעות 403/404 בשעון · m-19 בוררים בלי סימן נפתח · m-20 chip "שולם" בחלונית כהה · m-21 אין `h1` בשלב 1 · m-22 מתג ההסכמה בלי סימון שגיאה · m-23 Enter בשעון.

## שאלות לבעלים (ברירת המחדל שבה הפיילוט ייבנה)
| # | שאלה | ברירת מחדל |
|---|---|---|
| TQ-1 | idle/שומר מסך | לא בפיילוט (R8); ההצעה בהדגמה — כולל ניקוי פרטי הרישום של הלקוחה הקודמת |
| TQ-2 | קיפד | אין; סיסמה חופשית |
| TQ-4 | בורר = מעבר מיידי | כמו היום |
| TQ-5 | רישום עצמי נכשל עם `require_customer_id_number` / בלי מייל | כמו היום; לבדוק בהקדם בארגון שמפעיל רישום עצמי |
| TQ-9 | סרגל עליון בשעון ובעמדה פתוחה | כמו היום |
| OQ-1 | עוזר חכם מול לקוחות | כמו היום (רק עם `feature:ai` של המחובר) |
| OQ-2 | כותרת נגללת בטלפון | כן, מתחת ל-768 |
| OQ-3 | אישור הדפסה מחבר את העובד בעמדה | לא משנים; לדווח |
| OQ-4 | מלל ללקוחות בלשון נקבה | נשאר |
| OQ-5 | hover דביק | hover רק תחת `(hover:hover)` |
| OQ-6 | Enter בשעון | לא רושם (כמו היום) |

## לבעלים — מה לבדוק ראשון
1. **אבטחה בפרודקשן (לא עיצוב):** סיסמאות בטקסט גלוי ב-`PageVisitLog` דרך ה-interceptor ב-`app/layout.js` (שחרור העמדה, מסך הכניסה, שעון הנוכחות), גלויות ב-`/management/history`; ואין הגבלת ניסיונות ב-`/api/attendance`/`/api/login` (TK-71). נמצא בקריאת קוד, לא הורץ.
2. TQ-5 — אם רישום עצמי פעיל בנווה יעקב עם `require_customer_id_number`, הוא נכשל תמיד.
3. OQ-3 — אישור הדפסה משאיר את העמדה הנעולה מחוברת בשם העובד.
