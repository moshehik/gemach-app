# LIST-WIZARD-QUESTIONS — שאלות מבניית אבות-הטיפוס A1 (רשימה) ו-A3 (אשף), וברירת המחדל שנבחרה

> נבנה בריצה אוטומטית בלי אפשרות לשאול. בכל שאלה נבחרה האפשרות הבטוחה שתואמת לחוקה ול-R8 (לא משנים לוגיקה/API), והיא מה שמוצג באב-הטיפוס. **כל שורה מחכה לאישור או לתיקון של הבעלים.**
> קבצים: `archetype-list.html` + `archetype-list.contract.md` · `archetype-wizard.html` + `archetype-wizard.contract.md` · 2026-09-24.

## A1 — רשימת ההזמנות
| # | שאלה | מה נבחר | למה | נוגע ב |
|---|---|---|---|---|
| Q-L1 | ברשימה של היום 9 עמודות; החוקה מתירה עד 5. מה נשאר בטבלה? | הזמנה · לקוח (+ 2 דגמים בשורה משנית) · אירוע (עברי + לועזי) · מצב (שני התגים) · פעולות. סכומים, כמות, טלפון, מייל, עיר, נלקחו/הוחזרו וציפוף — בשורה המורחבת. | C-1.5; ייצוא עדיין מוציא את כל 11 העמודות | UI בלבד |
| Q-L2 | מיון לפי סכום / שולם / סטטוס: העמודות האלה כבר לא בטבלה. | בורר "מיון" נפרד מעל הטבלה עם כל 6 המפתחות + כפתור היפוך. 3 כותרות בטבלה נשארות לחיצות (`aria-sort`). | אותם 6 מפתחות שהשרת מקבל היום | UI בלבד |
| Q-L3 | מיון "סטטוס" ממיין לפי שדה ששמור ב-DB (כמעט תמיד ריק), לא לפי המצב שמוצג (LI-47). | נשאר, נקרא "סטטוס שמור" עם ⓘ שמסביר. | R8 — תיקון דורש שינוי שרת | הכרעה |
| Q-L4 | הסינון הנסתר של 3 חודשים (LI-7) — היום לא רואים אותו בכלל. | מוצג כ-pill מקווקו "תאריך אירוע: מ-…" עם ⓘ ואפשרות להסיר. | שקיפות; אותו פרמטר בדיוק | UI בלבד |
| Q-L5 | הסינון המתקדם חל היום **מיד** עם כל הקלדה (LI-53), ו"החל" רק סוגר. | חל רק ב"הצגת התוצאות"; סגירה עם שינויים שואלת (3 אפשרויות). | חוקה §ד.2 (Form: אזהרה אם שונה); חוסך בקשות לשרת. **שינוי התנהגות קטן — לאישור** | התנהגות |
| Q-L6 | "נקה הכל" בחלונית מנקה גם את 3 החודשים, ו"הכל" בלשוניות מחזיר אותם (LI-7/LI-53). | נשמר כמו היום, והחלונית מודיעה "גם ברירת המחדל של שלושה חודשים הוסרה". | R8 | הכרעה |
| Q-L7 | ספירות בלשוניות הסטטוס (חובה בארכיטיפ A1) — השרת לא מחזיר (LI-27). | מספר רק בלשונית הפעילה (מ-`total`), ⓘ שמסביר. | "דורש נתונים" — endpoint ספירות הוא שינוי שרת | דורש נתונים |
| Q-L8 | לחיצה על שורה פותחת היום את ההזמנה; הארכיטיפ מבקש שורה מורחבת. | השורה עדיין פותחת (כמו היום); ההרחבה בכפתור חץ נפרד; מספר ההזמנה הוא כפתור נגיש במקלדת. | R8 + נגישות (היום השורה לא נגישה במקלדת) | UI בלבד |
| Q-L9 | פרטי הריחוף (אייקון ⓘ בשורה) עובדים רק בעכבר. | הוחלפו בשורה המורחבת (עובד במגע ובמקלדת). | C-1.12 | UI בלבד |
| Q-L10 | ת״ז לפני מחיקה (LI-45) לא עובדת מהרשימה היום. | אחרי תשובת 401 מהשרת נפתח חלון ת״ז ונשלחת אותה מחיקה עם `x-zeout` (אותו API). הצעה חלופית (בקשה מראש) דורשת שדה `hasZeout`. | אותו API; מתקן תקלה בלי שינוי שרת | UI בלבד |
| Q-L11 | תאריך לועזי ליד העברי — היום ברשימה רק עברי. | מוצג בשורה שנייה, ומוסתר כש-`hide_gregorian_calendar` דלוק. | `eventDate` כבר בתשובה; מכבד הגדרה קיימת | UI בלבד |
| Q-L12 | כפתור "שאלות סטטיסטיקה" לא מוסתר כש-AI כבוי (LI-25). | נשאר מוצג כמו היום. | R8; לבדוק אם זה מכוון | הכרעה |
| Q-L13 | אישור ייצוא מעל התקרה = סיסמה בלבד, בלי בחירת מאשר (`verify-pin` בלי `employeeId`). | חלון קוד עם סיסמה בלבד (`withPicker:false`), כמו היום. | לא להמציא בחירת מאשר שלא קיימת (לקח REVIEW-1 B-1) | הכרעה |
| Q-L14 | תאריך עברי: בהדמיה מחושב בדפדפן (Intl) ומוצג באותיות; הפורמט המדויק של `eventDateHebrew` ב-DB לא אומת. | במימוש — הערך מהשרת כמו שהוא. | אין גישה ל-DB בריצה | תיעוד |
| Q-L15 | פרופילי הארגונים בבקרה — ערכי דמה, לא אומתו מול DB. | לפי חוקה §ט.3 + ערכים סבירים. | אין גישה ל-DB | תיעוד |

## A3 — הזמנה חדשה
| # | שאלה | מה נבחר | למה | נוגע ב |
|---|---|---|---|---|
| Q-W1 | בורר התאריך העברי הקיים (`HebrewDatePicker`) הוא רכיב נפרד. | באב-הטיפוס שדה תאריך רגיל + התאריך העברי מתחתיו, עם הערה. במימוש — הבורר הקיים בשפת v3. | היקף | UI בלבד |
| Q-W2 | סיבת הנעילה של שלב 3 אומרת תמיד "יש למלא תאריכים" גם כשחסר לקוח (WZ-16). | הסיבה לפי מה שבאמת חסר (אותה בדיקה). | מלל מדויק | UI בלבד |
| Q-W3 | קפיצה בסטפר עוקפת את אישור הלקוח החסום ואת בדיקת המשלוח (WZ-146/148). | הקפיצה עוברת את אותן בדיקות של "המשך". | סוגר עקיפה; **שינוי התנהגות — לאישור** | התנהגות |
| Q-W4 | בחירה בלשונית "חיפוש בשם" מדלגת על בדיקות פרטים חסרים (WZ-147). | שני המסלולים (טלפון ושם) עוברים באותה בדיקה (`handleUseExistingCustomer`). | עקביות; **שינוי התנהגות — לאישור** | התנהגות |
| Q-W5 | חיוב אשראי שמכסה הכול שומר מיד ומדלג על בדיקת תאריך שעבר / טלפון / משלוח (WZ-149). | הבדיקות רצות **לפני** פתיחת חלון האשראי (כך אי אפשר לחייב ואז להיתקע). | **שינוי סדר — לאישור** | התנהגות |
| Q-W6 | תשלום שנרשם אחרי חיוב נשמר עם אופן התשלום שבתפריט (אפשר "מזומן") (WZ-150). | באב-הטיפוס נרשם "אשראי (דרך נדרים פלוס)" + מספר אישור. | תיקון נתון שגוי; **נוגע ב-payload — לאישור** | נתונים |
| Q-W7 | "רישום תשלום" במזומן לא מבקש אישור, ואז שער האישור לא מופעל כלל (WZ-151); "יציאה באישור מנהל" בלי הגדרת רמה = בלי אישור (WZ-153). | נשמר כמו היום (אישור רק לפי `PAYMENT_APPROVAL_LEVEL`, ורק בסיום). "רישום תשלום" מוסתר כשנבחרה "יציאה באישור מנהל". | R8 — מדיניות אישורים = הכרעה עסקית | הכרעה |
| Q-W8 | ערך הצומת "סיכום" נראה הפוך (WZ-160). | "מוכן לבדיקה" כשיש פריטים; ריק כשאין. | מלל | UI בלבד |
| Q-W9 | הסיכום לא מציג משלוח/סניף/הזמנה טלפונית ולא שורת מחיר משלוח, אבל הסה״כ כולל אותה (WZ-111/161). | מוצגים (בלוק "משלוח וסניף" + שורת משלוח), כך שהשורות מסתכמות לסה״כ. | `deliveryAmount` כבר מוחזר מהשרת | UI בלבד |
| Q-W10 | עריכת פריט מוציאה אותו מהסל מיד (WZ-163). | נשאר, עם פס "עריכת פריט" ו"ביטול העריכה" שמחזיר אותו. | מצב מקומי | UI בלבד |
| Q-W11 | שדה הערות מופיע פעמיים (שלב 2 ושלב 3) (WZ-104). | פעם אחת בשלב 2; בשלב 3 תצוגה + "עריכה". | פחות כפילות | UI בלבד |
| Q-W12 | כפתור המידה מציג תמיד את מספר הפנויות הרגיל גם כשיש ציפוף (WZ-170). | מוצג המספר האפקטיבי + "+N בציפוף". | הנתון כבר מחושב | UI בלבד |
| Q-W13 | "הוראות לתופרת" מסומן חובה אבל נכתב מעצמו (WZ-162). | מוצג "לא חובה" + רמז "אם ריק — נכתב מעצמו". | מלל שתואם להתנהגות | UI בלבד |
| Q-W14 | כרטיס "משלוח / סניף" מוצג היום גם כשהוא ריק (WZ-73). | מוצג רק כשיש בו שדה. | C-9.3 (אין חורים) | UI בלבד |
| Q-W15 | אחרי שמירה אין היום הודעת הצלחה (רק מעבר עמוד) (WZ-12). | NoticeBar עליון "הזמנה N נוצרה" עם פס זהב 15ש׳ + "פתיחת ההזמנה" (D-5). | חוקה §ד.7 | UI בלבד |
| Q-W16 | חסימת לקוח משתמשת ברמה קשיחה "הנהלה ראשית" ולא בפריט קטלוג (WZ-166). | חלון אישור עם בחירת מאשר, כמו היום (הרשימה מסוננת לפי מי שמורשה). | R8; לרשום לפרויקט ההרשאות | הכרעה |
| Q-W17 | בדיקות תבנית (טלפון/מייל/ת״ז) קורות היום רק בשרת (400) ומוצגות ב-alert (WZ-52). | מוצגות בשדה עצמו לפני השליחה (אותם כללים של `lib/customerValidation`). | אותם כללים; תצוגה | UI בלבד |
| Q-W18 | חזרה בדפדפן מוגנת ב-`window.confirm` (WZ-141/172); סגירת לשונית לא מוגנת. | יציאה מהכפתורים = חלון אישור; עם טיוטה — "היציאה לא מוחקת, הטיוטה נשארת". חזרת דפדפן במימוש דרך אותו חלון. | חוקה §ד.4 (`window.confirm` אסור) | UI בלבד |
| Q-W19 | הו״ק, סניף, מיקומי לקוחות, אופני תשלום — ערכי הדמה בפרופילים לא אומתו מול DB. | ערכי דמה. | אין גישה ל-DB | תיעוד |
