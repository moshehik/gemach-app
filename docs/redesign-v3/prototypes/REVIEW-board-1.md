# REVIEW-board-1 — סקירה עצמאית של אב-הטיפוס A4 (לוח / ציר זמן) על `/board`

> סוקר: סוכן עצמאי (לא הבונה), 2026-09-24, **סבב 1 מתוך 2** (CONSTITUTION §ח.2 S12).
> נסקר: `archetype-board.html` + `archetype-board.contract.md` בענף `redesign/v3-protos-review` @ `0962136`; במהלך הסקירה מוזגו תיקוני QA קטנים של הבונה (`85a82b1`: עטיפת `.d-head/.d-tools`, `nOrders()`, "· N פריטים" בכפתור אישור ההשכרה, ריפוד `pill-input`, `.tbl-wrap` יחסי). **הגרסה שנבדקה בסוף = `85a82b1` + התיקונים של הסקירה הזו.** במקום שהיה התנגשות נשארה גרסת הסקירה (`countPill`).
> מקור האמת לפונקציות: **`origin/main`** (פרודקשן), `app/board/page.js` וכל מה שהוא קורא. המלאי העצמאי (I-01…I-80, עם file:line) נבנה **לפני** קריאת החוזה של הבונה, ונבדק מול עותק קריאה-בלבד של `main`.
> השאלות של הבונה (`BOARD-DASHBOARD-QUESTIONS.md`, BDQ-B1…B16) נקראו ולא נערכו (הקובץ משותף). ברירות המחדל של הסקירה הזו רשומות כאן, בשאלות RB-Q.

## פסק דין (סבב 1): **FAIL** — 0 חוסמים, 5 מהותיים, 16 קלים

השפה החזותית, מערכת השכבות, ה-RTL והמצבים חזקים מאוד, והלקחים של REVIEW-1 (מאשר + סיסמה מלאה, gold-800 לטקסט, breakpoints מהסט, `persistToBell` כבוי) יושמו. הכישלון נובע מפריסה שבורה ב-1024–1439 **דווקא בפרופיל של נווה יעקב** (הדפסה דלוקה), מסטטוס שלא יוצא מ-`calculateOrderStatus`, מסינון מתקדם שמציג "מסנן" על שדות שלא מסננים, מיעדי מגע קטנים ומפוקוס שהולך לאיבוד. כל המהותיים תוקנו בסבב הזה ישירות באב-הטיפוס ובחוזה (ראו `REVIEW-board-2.md`).

### איך נבדק (מה רץ בפועל)
| בדיקה | כלי | תוצאה |
|---|---|---|
| בדיקות סטטיות: media, מאפיינים פיזיים, חצים בטקסט, `opacity:0` בסיסי, z-index מספרי, `alert/confirm`, ניגודיות tokens | `check.cjs` (Node) | media: כל הערכים בסט (640/639, 768/767, 1024/1023, 1440/1439, 480, 641); פיזי: רק `left` של toast (חריג מתועד); `opacity:0` רק במצבי יציאה ו-input קובץ; 2× `z-index:1` מקומי (תג "היום", כותרת יום בהיסטוריה — לא שכבה); אין `alert/confirm` |
| טעינה ב-360/768/1024/1440, גלישה אופקית, יעדי מגע, כפתורים בלי תווית | `check.cjs` (Playwright/Chromium headless) | אין `pageerror`; `scrollWidth ≤ clientWidth` בכל הרוחבות; **יעדי מגע קטנים** 36×36 ו-40×40 (M-4); 0 כפתורים בלי תווית |
| זרימות: תפריט הזמנה (מיקום ליד קצה המסך, Esc, פוקוס), כרטיס פרטים בריחוף, חלונית פרטים (כהה), חלון יום + סינון, תפריט מתוך חלון, כרטיס השכרה, אישור מנהל מעל השכרה (3 שכבות), Esc עליונה בלבד ×3, חיפוש מתקדם, NoticeBar + toast, ערכת נושא בהירה לחלונית קוד, מקלדת (Enter על כרטיס → תפריט), תצוגת שבוע, חיפוש גלובלי ריק | `flows.cjs` (Playwright) | ראו פירוט למטה; שגיאות: M-5 (פוקוס) ו-M-2 (סינון) |
| טלפון 360: ברירת מחדל "רשימה", מקרא מקופל, תפריט = גיליון תחתון, רשת חודש = מספר + מונה, הקשה על יום → חלון יום; פרופיל "קיצוני" + "עמוס" בשלוש התצוגות | `flows.cjs` | הכול תקין, אין גלישה |
| התנגשות טקסט בכותרת היום / חריגת תגית מהכרטיס (מדידה לפי `Range.getClientRects`) ב-1024/1280/1440 × org1/org2/קיצוני × רגיל/עמוס | `shot.cjs` | **org2 ב-1024: 14 ימים עם התנגשות**; גם org1 ב-1024 (3), org2 ב-1280 (4) וב-1440 ביום עמוס (1) — M-1 |
| גיאומטריית גליפים (RTL) | קריאת `path d` של `i-back`/`i-next` + מיקום הכפתורים | "החודש הקודם" (מימין, x=444) = `i-back` = חץ ימינה; "החודש הבא" (משמאל, x=179) = `i-next` = חץ שמאלה ✓ |
| ניגודיות לזוגות חשודים | חישוב WCAG ידני | ink-3 על gold-100 (תאריך לועזי ביום "היום") 5.17 ✓; plum על gold-100 5.25 ✓; **placeholder על gold-100 (מצב חכם) 4.09** (m-12); פסי קטגוריה על לבן 1.6–2.1 (לא טקסט; לצדם תמיד תגית עם אייקון+מילה — m-15) |
| השוואה לקוד `main` בנקודות שנויות במחלוקת | קריאת קוד | `getOrderCategory`, חישוב איחור, `calculateOrderStatus`, `getLateReturnInfo`+`addDaysSkippingWeekends`, סינון החגים, סגירת חלון היום ב-"כרטיס השכרה", אישורים ב-`RentalReturnModal` |
| axe / קורא מסך / Safari-iOS / מכשיר מגע אמיתי / Rubik | — | **לא רץ** (אין axe בסביבה; אין מכשיר; Google Fonts חסום ברשת — הצילומים בגופן חלופי) |
| `verify-board.cjs` של הבונה | — | **לא הורץ** (כותב צילומים לתיקייה `screenshots/board/` של הבונה, מחוץ לקבצים שמותר לי לגעת בהם) |

תוצאות זרימה עיקריות (סבב 1, לפני תיקון): תפריט בתוך המסך גם בעמודה השמאלית (165–405px); Esc סוגר תפריט ומחזיר פוקוס לכרטיס (`aria-expanded=false`); חלונית פרטים `dark`, `data-v3-lock`, סרגל עליון `inert`, ה-scrim מכסה את הסרגל העליון (`elementFromPoint` בגובה 20px), גלגלת לא מגלגלת (scrollY=0); חלון היום `light`; שלוש שכבות `light form / light form / dark code`; "0000" → "הסיסמה לא נכונה…"; Esc ×3 סוגר אחת-אחת; NoticeBar ב-72px (מתחת לסרגל ב-64), פס זהב נעצר בריחוף (`paused`); toast 26px משמאל, 24px מלמטה; קוד במצב בהיר `light`.

---

## ממצאים

### חוסם (blocker)
אין.

### מהותי (major)
**M-1 · כותרת היום מתנגשת ב-1024–1439 כשהדפסת ההכנה דלוקה (= הפרופיל של נווה יעקב).**
- ראיה: `screenshots/review-board/01-1024-org2-before-M1.png` — המונה "2 הזמנות" וכפתור ההדפסה עולים על האות העברית והתאריך הלועזי (`ה׳ 16/9` נחתך), והתגית "בהשכרה" יוצאת משטח הכרטיס. מדידה: org2 ב-1024 — 14 ימים; גם org1 ב-1024 (3 ימים עם 3+ הזמנות), org2 ב-1280 (4), יום עמוס ב-1440 (1).
- סיבה: `.d-num{min-width:0}` מתכווץ בתוך `.d-head` בלי עטיפה; `.oc .chip` ברוחב התוכן שלו בתוך עמודה של ~65px.
- תיקון (בוצע): `.d-head`/`.d-tools` עוטפים, `.d-num{flex:none}`; מתחת ל-1440 המונה = מספר בלבד (המילה ב-tooltip ובתווית היום); תגית בכרטיס עוטפת בין האייקון למילה (לא באמצע מילה), `max-width:100%`.

**M-2 · חיפוש מתקדם: "מתאריך / עד תאריך" מוצגים כמסננים, אבל לא מסננים — לא באב-הטיפוס ולא בפרודקשן; הבאג לא מתועד.**
- פרודקשן: `buildBoardMonthParams` (`app/lib/prefetchRoutes.js:185-196`) מצרף את התאריכים אחרי טווח החודש באותם מפתחות; השרת קורא `searchParams.get()` = הערך הראשון (`app/api/orders/route.js:41-42`) → טווח החודש גובר תמיד (מלאי I-67).
- אב-טיפוס: `visibleOrders()` מתעלם מהם (נכון), אבל שורת "מסונן:" מציגה "מתאריך: …" והשורה החיה אומרת "1 שדות מסננים את הלוח" — מדד (הורץ): 23 כרטיסים לפני ואחרי. החוזה (BD-39) לא מזכיר את הבאג → מי שיבנה את הפיילוט "יתקן" או ישכפל בלי לדעת.
- תיקון (בוצע): השדות נשארים (R7, אין הורדת פיצ'ר), עם שורת הסבר "היום לא משנה את הלוח (תקלה ידועה)"; הצ'יפ שלהם מקווקו זהב עם "· לא מסנן" ו-tooltip; השורה החיה סופרת רק שדות שמסננים באמת ומוסיפה משפט על התאריכים. BD-39 עודכן. התיקון עצמו (שינוי ב-`buildBoardMonthParams`) = שינוי לוגיקה → שאלה RB-Q1.

**M-3 · כרטיס ההשכרה ממציא סטטוס: הזמנה בלי פריטים = "מחוק".**
- אב-טיפוס (לפני): `status()` מחזיר "מחוק" כשאין פריטים לא-מחוקים — כולל הזמנה שאין לה פריטים **בכלל**. הורץ: הזמנה #48102 (0 פריטים, עבר) → "מחוק".
- מציאות: `calculateOrderStatus` (`lib/orderStatus.js:6-9`) מחזיר "מחוק" רק כשההזמנה מחוקה **או** כשיש פריטים וכולם מחוקים; 0 פריטים → "עבר"/"בקרוב". אותו סוג טעות כמו REVIEW-1 M-5 ("סטטוס — מקור אמת יחיד").
- תיקון (בוצע): אותה לוגיקה בדיוק (טיוטה = לפי `Order.status`, לא קיים בדמה). BD-51 עודכן.

**M-4 · יעדי מגע מתחת ל-44 (§ב.5, בדיקה §ח-17).**
- הורץ: "פתיחת כל ההזמנות של היום" ו"הדפסת ההזמנות של היום" 36×36 בתאי היום (1024/1440); "מעבר לחיפוש חכם" ו"שאלות סטטיסטיקה" 40×40 (כל הרוחבות); "עוד N" בגובה 36.
- תיקון (בוצע): כל אלה 44 (`--v3-tap`); בכותרת היום עם `margin-block:-4px` כדי לא להגדיל את השורה. (תיבות הסימון בבקרת ההדגמה — מחוץ לעיצוב.)

**M-5 · הפוקוס הולך לאיבוד אחרי פעולה מהתפריט (§ד.1 "הסגירה מחזירה פוקוס לטריגר").**
- הורץ: חלון יום → כרטיס → "כרטיס השכרה" → Esc: הפוקוס נחת על הלוגו בסרגל העליון (שכבר מאחורי scrim), לא על הכרטיס.
- סיבה: התפריט נסגר בלי להחזיר פוקוס, ולכן השכבה החדשה שמרה `body` כטריגר.
- תיקון (בוצע): `closeMenu(true)` לפני הפעולה — הכרטיס מקבל פוקוס ומשמש טריגר; אחרי הסגירה הפוקוס חוזר אליו.

### קל (minor)
| # | ממצא | ראיה | טיפול |
|---|---|---|---|
| m-1 | נתוני הדמה מציגים "ערב יום כיפור", "ערב סוכות", "ראש חודש …", "שמחת תורה" — הקוד מסנן ערבי חג וראש חודש, ובלוח ישראל יש "שמיני עצרת" בלבד | `page.js:497-507`; מלאי I-31 | **תוקן** (נתוני דמה + BD-17) |
| m-2 | "1 הזמנות", "1 שדות מסננים" | צילום 1440 | **תוקן** ("הזמנה אחת", "שדה אחד") |
| m-3 | באנר החיפוש החכם מפנה למשתמש לקוד פנימי "BDQ-B3" | html `aiBanner()` | **תוקן** (משפט רגיל) |
| m-4 | "ביטול ההחזרה" בכרטיס ההשכרה בלי אישור; היום יש אישור | `RentalReturnModal.js:387-388` | **תוקן** (Confirm, כהה/בהיר) + BD-57 |
| m-5 | בדיקת איחור בהחזרה נמדדת מיום האירוע; היום — מיום עסקים אחד אחריו (בלי שישי/שבת) | `lib/lateReturn.js:18-31` | **תוקן** בדמה + BD-56 |
| m-6 | "כרטיס השכרה" מתוך חלון היום נפתח מעליו (שכבה שנייה); היום חלון היום נסגר | `page.js:1096-1108` | נשאר (שיפור: חוזרים לרשימת היום); מתועד BD-66, שאלה RB-Q3 |
| m-7 | אשף ההדפסה המקוצר מציג רק "הכנה/לפי תאריך"; חסרים סוגי הדוחות האחרים ובחירת תאריך/טווח, והחוזה לא מזכיר שזה המסלול היחיד לדוחות תיקונים מהלוח | מלאי I-46, I-76 | **תוקן** בחוזה (BD-45) + רמז באשף; המימוש = הרכיב המשותף |
| m-8 | `role="grid"` בלי ניווט חצים, וכותרות הימים `role="presentation"` (אין columnheader) | html `grid()` | נשאר; בפיילוט: `role="table"`+`columnheader`, או ניווט grid מלא |
| m-9 | כפתורי AI/סטטיסטיקה מוסתרים כשאין גישה; היום הם גלויים ומחזירים 403 (I-77) | — | נשאר (שיפור UI, כמו BDQ-D3); מתועד BD-65 |
| m-10 | החוזה חסר עובדות: BD-28 שגוי (הרשימה נגללת בתוך התא, 150px), טיוטות/"שמור לחיוב" נכללות, חיתוך שקט ב-2000, שיוך לפי אזור הזמן של הדפדפן, קפיצה חוזרת לאותו תאריך, "לא ידוע" | מלאי I-13, I-39, I-70, I-71, I-79 | **תוקן** (BD-28, BD-60…67) |
| m-11 | כ-300 שורות CSS שהועתקו מ-A2 ולא בשימוש (רייל, טאבים, רשימת שינויים, מגירה, טבלאות); ערכים גולמיים (`#fff`, `rgba`) | html 216–530 | נשאר באב-טיפוס סטטי; בפיילוט רק `var(--v3-*)` |
| m-12 | placeholder במצב חכם (רקע gold-100) 4.09:1 | חישוב | **תוקן** (`::placeholder` = ink-3, 5.17:1) |
| m-13 | breakpoints: גם `max-width:640` וגם `639` לאותו "טלפון" — ב-640 בדיוק חלק מהכללים חלים וחלק לא | html | נשאר; בפיילוט custom-media אחד |
| m-14 | ריפוד פיזי `padding:0 6px 0 12px` ב-`.tb-user` (לא נתפס ב-lint כי קיצור) | html 136 | **תוקן** (`padding-inline`) |
| m-15 | פסי הקטגוריה (sky-300/400, rose-500) על לבן 1.6–2.1 | חישוב | נשאר: הפס קישוטי, לצדו תמיד תגית אייקון+מילה ("אחר" — בלי פס בולט ובלי תגית, כמו היום) |
| m-16 | אחרי אישור השכרה היום נשאלת "להדפיס את פרטי ההשכרה?"; לא מודגם | `RentalReturnModal.js:361` | נשאר (רכיב משותף, מקוצר); שייך לחוזה כרטיס ההשכרה |

### מה עבר (עיקרי)
- **שפה חזותית (סקיצה B):** navy/sky/gold/rose בלבד; זהב = כפתור ראשי אחד לאזור ("לחפש"), "היום" וטיימר; plum רק לאיחור ושגיאה; קטגוריות = פס + תגית עם אייקון ומילה (אין ירוק/אדום/כתום). Rubik, h1 28→23, גוף 16.
- **A4 "חייב להכיל":** ניווט תקופה עם חצים RTL נכונים (נבדק בגיאומטריה) ✓; רשת עם תא = כרטיסים מקופלים ✓; כרטיס פרטים בריחוף (עכבר) + חלונית במגע/מקלדת ✓; מקרא לא-בצבע-בלבד, כולל "בלי פריטים", "באיחור", "היום" ✓; חודש/שבוע/רשימה (+ חלון יום) ✓; טלפון = רשימה יומית עם קו "היום" ✓.
- **שכבות:** portal, scrim מלא על הסרגל העליון, נעילת גלילה, `inert`, לכידת Tab, Esc לעליונה בלבד, עד 3 שכבות, confirm/code/viewer כהה או בהיר לפי מתג, form (חלון יום, חיפוש מתקדם, השכרה) בהיר תמיד; תפריט = popover בדסקטופ וגיליון תחתון בטלפון.
- **הודעות:** NoticeBar מתחת לסרגל העליון, פס זהב יורד 15ש' שנעצר בריחוף; toast שמאל-תחתון; `persistToBell` לא קיים.
- **לוגיקה שהועתקה נכון:** קטגוריה (סדר עדיפות, "ריקה" סופרת מחוקים, תיקונים כוללים מחוקים), איחור (>2 ימים, רק פריטים לא מחוקים), כפתור הרחבה רק ביותר מ-2, הדפסה לפי `enable_batch_print_prep`, ציפוף לפי `hide_custom_spacing` (true = מוסתר), `hide_taken_orders_from_orders_list`, מצב AI שנשאר גם במעבר חודש (הבאג הקיים מוצג, לא מומצאות תוצאות), אישור מנהל = מאשר + סיסמה מלאה.
- **org variance:** 4 פרופילים; בלי הדפסה — אין חור בכותרת/תא/חלון; "קיצוני" + יום עם 14 הזמנות + שם 40+ תווים בלי גלישה ב-360/768/1024.

---

## מטריצת עקיבות — מלאי `main` (I-nn) → אב-טיפוס
סטטוס: ✔ קיים · ✔* קיים אחרי תיקון בסבב זה · ◐ שינוי מכוון, מתועד כשאלה · — מחוץ לתחום חזותי (שרת/ניווט/מימוש) · ✘ חסר.

| # | פריט (I-nn) | באב-טיפוס | סטטוס |
|---|---|---|---|
| 1 | I-01..04 שער `page:board`, סדר הכרעה, סגור כברירת מחדל | מצב "אין הרשאה" בבקרה | ✔ (השער עצמו — שרת) |
| 2 | I-05 קישור ניווט לפי אותו כלל | "לוח" בסרגל | — |
| 3 | I-06 `restrict_board_to_managers` לא נקרא | לא מוצג | ✔ (BDQ-B15) |
| 4 | I-07..08 הרשאות API נפרדות; AI → `checkAiAccess` | מתג "חיפוש חכם זמין" | ✔ / ◐ (m-9) |
| 5 | I-09 AbortController במעבר חודש | — | — (BD-5) |
| 6 | I-10..11 טווח חודש עברי ±14, `limit=2000`, חיפוש/מתקדם בשרת | סינון מקומי בדמה | — (BD-4, BD-61) |
| 7 | I-12 `hide_taken_orders_from_orders_list` | מתג בבקרה | ✔ |
| 8 | I-13 טיוטות ו"שמור לחיוב" נכללות | — | ✔* (BD-60, מתועד) |
| 9 | I-14 שדות הזמנה | כולם בכרטיס/פרטים | ✔ |
| 10 | I-15..17 `data.data`, מטמון SWR, prefetch | — | — |
| 11 | I-18 הגדרות: 3 מפתחות | פרופילים + מתגים | ✔ |
| 12 | I-19 חיפוש גלובלי, AI, סטטיסטיקה, השכרה, אשף | כולם | ✔ |
| 13 | I-20 חודש בלבד | חודש + שבוע + רשימה | ◐ (BDQ-B1) |
| 14 | I-21 נפתח על החודש הנוכחי | ✔ | ✔ |
| 15 | I-22 קודם/הבא לפי חודש עברי | ✔ (חצים RTL) | ✔ |
| 16 | I-23 שם חודש עברי בלבד | + טווח לועזי מתחת (נגזר) | ◐ (תוספת UI) |
| 17 | I-24 קפיצה לתאריך (בורר עברי) | חלונית יום+חודש; "במימוש: הבורר המשותף" | ✔ (BDQ-B11) |
| 18 | I-25 אין "היום" | כפתור "היום" | ◐ (BDQ-B9) |
| 19 | I-26 סימון היום | מסגרת זהב + תג "היום" | ✔ |
| 20 | I-27 ראשון→שבת, ראשון מימין | ✔ | ✔ |
| 21 | I-28 רשת 7 עמודות, תאים ריקים, רק ימי החודש | ✔ | ✔ |
| 22 | I-29 תא: יום באותיות, מונה, הרחבה (>2), הדפסה, לועזי | ✔ | ✔* (M-1, M-4) |
| 23 | I-30 פרשה בשבת | ✔ | ✔ |
| 24 | I-31 חגים (סינון) | תגיות | ✔* (m-1) |
| 25 | I-32 שיוך לפי `eventDate` בלבד, אזור זמן דפדפן | ✔ | ✔ (BD-62) |
| 26 | I-33 סדר בתוך יום | סדר הדמה | ✔ (BD-67) |
| 27 | I-34 כרטיס: שם, מספר, תגית (לא ל"אחר"), ⓘ, סימון איחור | ✔ | ✔ |
| 28 | I-35 כללי קטגוריה | זהה (`category()`) | ✔ |
| 29 | I-36 תוויות/צבעים | מילים חדשות, פלטת B | ◐ (BDQ-B8) |
| 30 | I-37 איחור (>2 ימים) בכרטיס ובתא | ✔ | ✔ |
| 31 | I-38 מקרא (בלי "ריקה", איחור, היום) | + "בלי פריטים", "באיחור", "היום" | ◐ (BDQ-B5) |
| 32 | I-39 גלילה בתוך התא (150px) | עד 3 + "עוד N" | ◐ (BDQ-B2; BD-28 ✔*) |
| 33 | I-41 תפריט: הזמנה / לקוח (רק עם מזהה) / השכרה | ✔ + מיקום בתוך המסך | ✔* (M-5) |
| 34 | I-42 פרטים בריחוף (בלי שם לקוח — באג) | ריחוף + חלונית; עם שם | ◐ (BDQ-B6, B7) |
| 35 | I-43, I-49(BD) השכרה → רענון הלוח | ✔ | ✔ |
| 36 | I-44 הדפסת יום (לשונית חדשה) | toast בדמה | ✔ |
| 37 | I-45 כפתור אשף בכותרת (לפי הגדרה) | ✔ | ✔ |
| 38 | I-46 אשף: סוגי דוחות, מצבי תאריך | מקוצר + רמז | ✔* (m-7) |
| 39 | I-47 חלון היום משתמש באותם כרטיסים | ✔ | ✔ |
| 40 | I-48 אין גרירה/בחירה מרובה/עריכה | לא נוספו | ✔ (BDQ-B14) |
| 41 | I-49 חיפוש רגיל + X | ✔ + שורת "מסונן:" | ✔ |
| 42 | I-50 מצב AI (שומר טקסט, מדכא טעינת חודש) | ✔ + באנר | ✔* (m-3) |
| 43 | I-51 סטטיסטיקה | חלונית מקוצרת | ✔ |
| 44 | I-52 חיפוש גלובלי; טקסט ריק → הודעה | ✔ | ✔ |
| 45 | I-53 חיפוש מתקדם חל בכל הקשה | ✔ + שורה חיה | ✔ (BDQ-B10) |
| 46 | I-54 אין פילטרים נוספים | לא נוספו | ✔ |
| 47 | I-55 חלונית מתקדם: 2 קבוצות, תיבת AI (מוסתרת ב-`hide_ai_features`), נקה/החל | ✔ | ✔* (M-2) |
| 48 | I-56 חלון יום: כותרת, סינון מקומי, הדפסה, סגירה (גם ב-scrim) | ✔ (form בהיר, scrim סוגר) | ✔ |
| 49 | I-57 תוצאות גלובליות: טוען/ריק/רשימה, "לחודש", "פתיחה" | ✔ + מצב שגיאה | ✔ |
| 50 | I-59 טעינה (ספינר; מטמון בלי ספינר) | שלד | ✔ |
| 51 | I-60 אין מצב ריק | הודעה מעל הלוח | ◐ (תוספת UI) |
| 52 | I-61, I-80 שגיאה נבלעת, נתוני החודש הקודם נשארים | מצב שגיאה + "לנסות שוב" | ◐ (BD-8) |
| 53 | I-62..63 מטריצת הגדרות/org | פרופילים org1/org2/מינימלי/קיצוני | ✔ |
| 54 | I-64 אין התאמה לטלפון | רשימה, רשת מקוצרת, גיליונות | ◐ (שיפור) |
| 55 | I-65 באג AI (טבלת לקוחות → לוח ריק) | מוצג כמו שהוא | ✔ (BDQ-B3) |
| 56 | I-66 "לקוח:" בלי ערך | עם ערך | ◐ (BDQ-B6) |
| 57 | I-67 תאריכי מתקדם לא משפיעים | מוצג ומסומן "לא מסנן" | ✔* (M-2, RB-Q1) |
| 58 | I-68..69 יציאה ממצב AI מסורבלת; אין חיווי סינון | באנר + צ'יפים (הבאג המהותי נשמר) | ◐ |
| 59 | I-70 קפיצה חוזרת לאותו תאריך | — | ✔* (BD-63, מתועד) |
| 60 | I-71 ריפוד ±14 לא מוצג; חיתוך 2000 | — | ✔* (BD-61) |
| 61 | I-72 קוד מת (`highlightedDate`, `router`) | "hl" אחרי "לחודש הזה"/קפיצה | ◐ (שימוש חדש במצב קיים) |
| 62 | I-73 קטגוריה כפולה מ-`orderStatus` | נשמרה מקומית | ✔ (החוזה: לא להחליף) |
| 63 | I-75 "הוחזר"/"שולם" באותו צבע | צבעים ואייקונים שונים | ✔ |
| 64 | I-77 AI גלוי למרות `hide_ai_features` | מוסתר כשאין גישה | ◐ (m-9, BD-65) |
| 65 | I-78 תיאורי debug/howTo לא מדויקים | — | — (לא UI) |
| 66 | I-79 אזור זמן | — | ✔* (BD-62) |
| 67 | BD-50..59 כרטיס השכרה (רכיב משותף) | מקוצר: סריקה, השכרה/החזרה, מחסן → מאשר+סיסמה, בחוץ בהזמנה אחרת, איחור → "לא תקין", סגירה עם סריקות ממתינות, ביטולים | ✔* (M-3, m-4, m-5); הדפסה אחרי אישור — m-16 |

**חסר לגמרי (✘) אחרי התיקונים: אין.** כל שינוי מכוון (◐) מתועד בשאלות BDQ של הבונה או ב-RB-Q למטה.

## פריטים שהאב-טיפוס הציג ואינם מגיעים מהנתונים / מהקוד (invented)
| פריט | מצב |
|---|---|
| "מחוק" להזמנה בלי פריטים בכרטיס ההשכרה | M-3 — תוקן |
| "שדות מסננים" על תאריכים שלא מסננים | M-2 — תוקן |
| ערבי חג, ראש חודש, "שמחת תורה" כתגיות | m-1 — תוקן |
| טווח לועזי מתחת לשם החודש | **נגזר** מימי החודש (UI בלבד) — מותר |
| "באיחור" כתגית/מילה | **נגזר** מאותו כלל של היום (BD-21) — מותר |
| "נמצאו 6 הזמנות להדפסה", שמות שמלות, שמות סניפים | דמה מוצהר ("נתוני דמה בלבד") |
| שם הלקוח בכרטיס הפרטים | `customerName` קיים בתשובה — מותר (BDQ-B6) |

## שאלות פתוחות (לא חוסמות; ברירת המחדל שנבחרה)
- **RB-Q1** תאריכי החיפוש המתקדם לא מסננים (I-67). **ברירת מחדל:** בפיילוט השדות נשארים עם הסימון "לא מסנן" (R8 — לא משנים מה נשלח לשרת); מומלץ לתקן בנפרד ב-`buildBoardMonthParams` (להחליף את טווח החודש כשהמשתמש מילא תאריך) — החלטת בעלים.
- **RB-Q2** מונה ההזמנות בתא מתחת ל-1440 = מספר בלבד. **ברירת מחדל:** כך (המילה ב-tooltip ובתווית הנגישה של היום); אם הבעלים רוצה מילה — לוותר על כפתור ההרחבה בתא (יש "עוד N").
- **RB-Q3** "כרטיס השכרה" מתוך חלון היום: היום סוגר את חלון היום; באב-הטיפוס נפתח מעליו. **ברירת מחדל:** לשמור את הערימה (חוזרים לרשימת היום אחרי הסריקה), בכפוף למגבלת 3 שכבות — אם יש בכרטיס ההשכרה חלונית שלישית שדורשת רביעית, לסגור את חלון היום כמו היום.
- **RB-Q4** כפתורי AI כשאין גישה: היום גלויים ונכשלים ב-403. **ברירת מחדל:** להסתיר (אותו כלל של `checkAiAccess` שכבר מחושב ב-layout) — כמו BDQ-D3 בבית.
- **RB-Q5** `role="grid"` מול `role="table"`. **ברירת מחדל:** בפיילוט `table` עם `columnheader` (אין ניווט חצים בלוח היום, ולכן grid מבטיח משהו שלא קיים).
