# REVIEW-dashboard-2 — סבב שני (אימות התיקונים)

> סוקר עצמאי, 2026-09-24. בסיס: `85a82b1` (תיקוני QA של הבונה, מוזגו ע"י הרכז) + התיקונים שלי מ-REVIEW-dashboard-1 בעץ העבודה (לא עשיתי commit — הרכז מבצע).
> מה רץ: `scratchpad/check.cjs` (סטטי + 360/768/1024/1440) ו-`scratchpad/dashboard/flow.cjs` (Playwright/Chromium headless) על הקובץ הסופי. צילומים: `screenshots/review-dashboard/` (`r1-*` = לפני, `r2-*` = אחרי; 12 קבצים).
> לא רץ: axe, קורא מסך, מכשיר אמיתי, `verify-dashboard.cjs` של הבונה (הוא כותב ל-`screenshots/dashboard/`, מחוץ לקבצים שמותר לי לגעת בהם).

## פסק דין: **PASS-with-minors**
אין חוסמים ואין מהותיים פתוחים. נשארים 4 קלים (m-9, m-10, m-12, m-13) — כולם מתועדים כשאלות עם ברירת מחדל. אפשר לעבור לפיילוט של A5 אחרי אישור הבעלים ל-BDQ-D1 (דורש טיפול) ול-OQ-D2 (פרטיות ב-storage).

## אימות כל ממצא מ-REVIEW-dashboard-1
| # | בדיקה (הורץ) | תוצאה |
|---|---|---|
| M-1 | `scrollWidth − clientWidth` ב-360/1024 × 4 פרופילים × (פתיחה/תוצאות/שיחה) | ✔ 0 בכל 24 הצירופים (היה 84 בשיחה ב-360). `r2-home-ai-360.png` |
| M-2 | `getComputedStyle(.kv)` בשורה המורחבת ובכרטיס פרטי האריח | ✔ `flex/column` (היה `block/row`); תווית מעל ערך. `r2-chat-1440.png`, `r2-kpi-viewer-dark-1440.png` |
| M-3 | "סיכום: שגיאת שרת" ו"כשל בפעולות שרת" ב-`/dashboard` | ✔ כרטיס "לא הצלחנו לטעון את הסיכום" + "לנסות שוב" (`role=alert`). `r2-dash-error-1440.png` |
| M-4 | "סיכום: טעינה הדרגתית": מיד / אחרי 1.3ש' | ✔ מיד: 8 שלדים באריחים + 2 בגרפים; אחרי: 0 באריחים (המספרים מופיעים), הגרפים עדיין בשלד. `r2-dash-loading-1440.png` |
| M-5 | קריאת החוזה | ✔ סעיף 7א (DB-62…DB-70), DB-1/42/57 הורחבו, 5 שורות חדשות ב"UI בלבד מול דורש נתונים". בשיחה: בועת HOWTO עם כפתור "פתיחת עמוד ההגדרות" (לתפקידים שאינם מנהלים) |
| M-6 | תפקיד "לא מחוברת" בארגון א׳ — `/` ו-`/dashboard`; תפקיד 1 → הגדרה → שמירה | ✔ "צריך להתחבר כדי להמשיך" בשני הנתיבים (בפרופיל "מינימלי" — בלי `require_login` — האורח רואה את הבית, כמו היום); רשימת מאשרים = 2 (הנהלה ראשית בלבד), "אישור" מושבת עד בחירה. `r2-approver-dark-1440.png` |
| m-1 | חפיפת כפתור ההעתקה עם הטקסט ב-4 הבועות | ✔ אין חפיפה (הייתה בבועת העוזר) |
| m-2 | grep לשורטהנד פיזי | ✔ `.pill-input`/`.tb-user` לוגיים; טיימר ה-NoticeBar מסומן `v3-physical`; `check.cjs`: פיזי = רק `.toasts left` (חריג) |
| m-3 | תגית "שולם" | ✔ מוגדרת: טקסט navy-800 ומסגרת navy-500 בעובי 1.5 (הרקע sky-100 כמו ברירת המחדל; ההבדל הוא המסגרת, האייקון והמילה) |
| m-4 | מבנה הבועות | ✔ `[[טבלה,—,—],[—,הגדרה,—]]` למנהלת; טלפונים רק בתאי הטבלה (`052-4418210`, `054-1102233`) |
| m-5 | הביטוי | ✔ זהה ל-`COPYABLE_RE` |
| m-6 | גובה תווית ציר ב-360/768/1024/1440 | ✔ 14px בכל הרוחבות (היה ~5px ב-360); "3K" נראה. `r2-dash-360.png` |
| m-7 | Enter על "30 ימים" ואז ← | ✔ עובר ל-"12 שבועות" ומסמן אותו |
| m-8 | טקסט הפרטיות | ✔ placeholder ניטרלי, בלי התחייבויות |
| m-11 | `a.btn` | ✔ בלי קו תחתון |
| רגרסיה | `check.cjs` על הקובץ הסופי | ✔ `badMedia` ריק, אין `pageerror`, 0 כפתורים בלי תווית, `scrollWidth ≤ clientWidth` ב-4 הרוחבות. שכבות: Esc עליונה בלבד, נעילה משתחררת, פוקוס חוזר לאריח/לכפתור, code כהה/בהיר, form בהיר; toast left 24/bottom 24; NoticeBar מתחת לסרגל, נעצר בריחוף, 15s תחת הפחתת תנועה |

## קלים שנשארו (לא חוסמים)
- **m-9** העמודה הזהובה = התקופה הפעילה האחרונה (OQ-D3).
- **m-10** CSS מת מ-A2 בקובץ העצמאי (כולל `z-index:1` ו-`style.zIndex` מספרי). בפיילוט — רכיבי v3 ו-tokens בלבד.
- **m-12** טבלת AI בטלפון: פעולה/הרחבה בגלילה אופקית פנימית (OQ-D5).
- **m-13** קישור "לקוח X" לא מודגם, כי במציאות הוא כמעט תמיד לא נוצר או שבור (DB-65).
- **n-1 (חדש)** `check.cjs` לא מזהה shorthand פיזי (`margin/padding` עם 4 ערכים). כדאי להוסיף לבדיקת §ח-8.
- **n-2 (חדש)** `.tbl-wrap{position:relative}` מופיע פעמיים (שלי + של `85a82b1`). אין לזה השפעה, אבל כדאי לאחד כשמעבירים לפיילוט.

## שאלות פתוחות לבעלים (ברירת המחדל שבה הפיילוט ייבנה)
| # | שאלה | ברירת מחדל |
|---|---|---|
| BDQ-D1 | "דורש טיפול" בבית | כבוי; מקום שמור בלי נתונים עד החלטה |
| OQ-D1 | `[OPEN_LINK]`/`[FILTER]` גולמיים | OPEN_LINK = כפתור, FILTER מוסר (UI בלבד) |
| OQ-D2 | שיחה/אחרונים ב-storage של הדפדפן עוברים בין עובדים | להציג כמו עכשיו; לדווח כפער פרטיות |
| OQ-D3 | זהב = "תקופה אחרונה פעילה" | להשאיר עם המקרא הנוכחי |
| OQ-D4 | חלוקת ימים ב-UTC | לא לשנות (R8); לדווח |
| OQ-D5 | טבלת AI בטלפון | 2 עמודות + הרחבה; הפעולה בשורה המורחבת |
| OQ-D6 | פערי הרשאה/אבטחה ב-main (DB-69, BDQ-D2) | מחוץ לתחום; משימה נפרדת |
