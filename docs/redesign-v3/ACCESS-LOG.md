# ACCESS-LOG — יומן גישה לאימות חי (D-12, 2026-09-24)

**append בלבד.** לפני כל אימות חי מול אחד האתרים מוסיפים רשומה בסוף הקובץ. אסור לערוך/למחוק רשומות קודמות. **לעולם לא נרשם ערך של סוד** — רק שם משתנה הסביבה.

היקף מאושר: **קריאה בלבד**; סודות רק במשתני סביבה מקומיים (`AGENT_LOGIN_SECRET` או מקביל); לא בקבצים, לא בקומיטים, לא בדוחות.

## פורמט רשומה
```
### <YYYY-MM-DD HH:MM (Israel)> · <session/agent id> · <org1|org2|preview>
- env var used: <NAME בלבד - לא הערך>
- access type: read-only
- what was read: <דפים/נתיבי API/שאילתות>
- result: <בקצרה; ללא נתונים אישיים>
```

## רשומות
(אין עדיין - לא בוצע אימות חי.)
