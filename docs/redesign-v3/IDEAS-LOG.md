# יומן רעיונות והחלטות
פורמט: `#n · תאריך · מי · נושא` ← מה ← למה ← סטטוס (החלטה/הצעה/פתוח). כל סוכן מוסיף שורות בסוף הקובץ (append בלבד).

#1 · 2026-09-24 · claude-main · פלטות משתמש מול פלטה קבועה
- המערכת הקיימת מאפשרת פלטה לכל עובד (`Employee.themeColor`, `app/lib/customPalette`). סקיצה B קבועה: navy/sky/gold.
- **הצעה:** ה-tokens של v3 הם ברירת המחדל; מערכת הפלטות מוזרמת כ-override של אותם משתנים (`--v3-brand` וכו') כך שההעדפה האישית לא נשברת. להחליט בשלב 2 אחרי מיפוי המשתנים. → שאלה Q2.
#2 · 2026-09-24 · claude-main · הגנה על מכסת deploy
- ענף redesign ידלג על build ב-Vercel (ignore-build). `[force-deploy]` בהודעת קומיט לפריוויו. ← החלטה.
#3 · 2026-09-24 · claude-main · הקדמת `v3-` ו-`data-v3`
- מאפשר מעבר הדרגתי עמוד-אחר-עמוד בלי לשבור את האתר החי. ← החלטה.
#4 · 2026-09-24 · claude-main · שיטת א'/ב' (רעיון 12)
- נבחרה, עם ההבחנה: ב' לפי *ממד* (RTL/מלל/איקונים/חלוניות/התאמה) ולא לפי עמוד. ראו ARCHITECTURE §2. ← החלטה.
#5 · 2026-09-24 · user · הבהרת מקורות
- הסקיצה הקנונית = `Desktop\גמח שמלות חדש\order-card-sketch-B.html` (עותק ב-`sketch/`). מדפיס-בוט (`print-center`) משמש **רק** לדפוס תהליכי-שלבים ספציפיים — לא כמקור עיצוב כללי. ← החלטה.
#6 · 2026-09-24 · user · דף הכניסה בתוך ההיקף
- מעצבים גם את מסך הכניסה (`app/components/LoginScreen.js`, כולל בורר מוסד/עובד/PIN אם קיים). השראה: דף הכניסה של מדפיס-בוט (`print-center`, "לאיזה מוסד מתחברים?") **בשילוב** בסיס העיצוב של סקיצה B. ← החלטה. (מתקן את #5: מדפיס-בוט = שלבים + כניסה.)
#7 · 2026-09-24 · user · אישור ברירות מחדל ל-Q1–Q5
- Q1 ניהול (/admin,/management) מחוץ לשלב א'; employees/profile/display-settings בפנים. Q2 פלטה קבועה + hook. Q3 עמודים בהירים בלבד, חלוניות אישור/קוד כהה+בהיר. Q4 הקיוסק בפנים, סגנון v3 מותאם למגע, מנגנון הנעילה ללא שינוי. Q5 הדפסות ניטרליות. ← החלטה סופית.

#7 · 2026-09-24 · claude-agent · דפוסי שלבים (STEPPER-PATTERNS.md)
- סטפר v3 אחד משותף (`V3Stepper`/`V3StepFooter`/`V3StepBody`) במקום שלושה סטפרים נפרדים (הזמנה חדשה, אשף דגם, אשף הרשאות). לוקחים מ-B את צמתי `.tx` (done=navy, cur=זהב+טבעת), ממדפיס-בוט: עוגן "שלב X מתוך N", כניסת fade-up לכל שלב, פוטר חזרה/המשך קבוע, סיכום עם "עריכה" שחוזר לסיכום, פס מקטעים במובייל.
- החלפת `alert()` בחסימת שלב (`orders/new/page.js:1444`) ב-tooltip צף (R11) - שינוי הצגה בלבד, אותה לוגיקת `canNavigateToStep`.
- לא כל דבר הוא סטפר: החזרת השכרה = רשימת בדיקה + פס התקדמות; "אשף הדפסה" = טופס אחד; כרטיס הזמנה = ציר `.tlx` של B.

#8 · 2026-09-24 · claude-agent · מסך כניסה (LOGIN-DESIGN.md)
- מיזוג: מעטפת דו-עמודית של מדפיס-בוט (פס מותג + כרטיס + אייד "הירו" עם כותרת זהב ו-3 יתרונות) בשפת B (navy/sky/gold, מסגרות, איקונים מונפשים); ללא הפונט הכתב-יד. מוק: `sketch/login-mock.html`.
- בכניסה הנוכחית **אין בורר מוסד** (גמח אחד לכל אתר) - "לאיזה מוסד" לא מיושם; ההשראה עוברת לכרטיסי בחירת עובד. הוספת בורר גמח = פונקציונליות חדשה, נרשם ב-QUESTIONS אם יידרש.
- רשימת התאמה 100% של 43 אלמנטים ב-LOGIN-DESIGN §3; דגשים: הגנת autofill (name אקראי ב-useEffect), `finishLogin` = reload מלא, PIN ברירת מחדל במחשב מהימן, דיאלוג סיסמה חדשה לא ניתן לסגירה, קישור `/punch-clock` בעמוד מלא בלבד.
#9 · 2026-09-24 · user · ענן כברירת מחדל
- אם סוכן הענן (ניסוי: ICON-INVENTORY) מצליח — כל עבודה שאינה דורשת שרת פיתוח/דפדפן מקומי עוברת לענן. סוכני ענן כותבים לענף-דוחות נפרד (`redesign/site-v3-reports-*`) ומאוחדים לענף הראשי על ידי הסוכן הראשי. ← החלטה מותנית בהצלחת הניסוי.
#7 · 2026-09-24 · agent-dialogs · Provider אחד + override גם ל-window.confirm/prompt
- `PopupProvider` דורס רק `window.alert`; 16 `window.confirm` ו-3 `window.prompt` נשארים native (≈9 ללא fallback). ← **הצעה:** DialogProvider v3 שומר את חתימות `window.customConfirm/customPrompt/customAuthPrompt/customThreeWayConfirm/alert` ומוסיף override ל-`confirm`/`prompt` — סוגר את כל ה-native בלי לגעת ב-~100 קריאות. ← DIALOG-INVENTORY §E. הצעה.
#8 · 2026-09-24 · agent-dialogs · alert() במסך הכניסה הוא native
- `LoginScreen` נטען מחוץ ל-`PopupProvider` (`layout.js:626`) — שם `alert()` הוא native אמיתי. ← ב-v3 ה-Toast/Dialog provider צריך לעטוף גם את מסך הכניסה. הצעה.
#9 · 2026-09-24 · agent-dialogs · CSS ישן חשוד כמיותר
- `globals.css`: `.modal-overlay`, `.popup-overlay`, `.global-popover`, `.popup-toast-container` — לבדוק שימוש ולמחוק בשלב 7. פתוח.
#10 · 2026-09-24 · agent-notif · הערות שמירה בפעמון = `Notification.category='activity_note'` (בלי שינוי סכמה)
- דורש ענף קטן ב-`POST /api/notifications` (לעצמי, בלי מייל, dedupe 60s) + סינון ב-`/messages`. ← NOTIFICATIONS-DESIGN §2.4. שאלות Q-N1..N4. הצעה.
#11 · 2026-09-24 · agent-notif · פערי משוב שנמצאו
- יציאה מכרטיס הזמנה אחרי שמירה, יצירת הזמנה/לקוח/עובד/דגם, ומחיקת הזמנה — **אין שום הודעה** היום. `pendingBalance toast` לא קיים בקוד. אין פופאפ למטה-שמאל להודעות פנימיות (רק חלונית מרכז ב-`ShiftMessageWatcher`) — R21 הוא רכיב חדש. ← NOTIFICATIONS-DESIGN §1,§4. עובדה.
#12 · 2026-09-24 · agent-notif · הפעמון צריך אירוע רענון
- הפעמון מתרענן רק בשינוי pathname / poll 120s; אחרי POST של הערה יש לשלוח `v3:bell-refresh`. הצעה.
#13 · 2026-09-24 · claude-main · ניסוי ענן הצליח
- סוכן `isolation: remote` קרא את הענף מ-GitHub, כתב דוח ודחף לענף-דוחות נפרד (`redesign/site-v3-reports-<נושא>`); מוזג לענף הראשי ב-`git checkout origin/<branch> -- <file>`. מעכשיו: כל עבודת קריאה/ביקורת/אימות → ענן. בנייה בפועל + שרת פיתוח → מקומי. ← החלטה.

#18 · 2026-09-24 · history-agent · מתאם היסטוריה בשכבת הקריאה
- כל ההתאמה (קטגוריה/משפט/סכום/replay של from) בקריאה בלבד; אפס שינוי בכתיבת AuditLog. ראו HISTORY-DESIGN.md + reports/history-adapter-skeleton.js. ← הצעה.
#19 · 2026-09-24 · history-agent · הפיד בהזמנה חייב לאחד 5 ישויות
- היום טאב ההיסטוריה מציג רק entityType=Order; פריטים/תשלומים/חיובים/זיכויים לא מופיעים. פתרון ללא שינוי שרת: 5 קריאות מקבילות ל-/api/audit. ← הצעה.
#20 · 2026-09-24 · history-agent · דחיית רשומות רעש + קיבוץ ביטול הזמנה
- UPDATE אוטומטי של הזמנה שולח כמעט כל שדה בכל שמירה (בלי from); הצעה: replay + דחיית זהים, וקיבוץ CANCEL_ORDER (N+2 שורות) לרשומה אחת. דורש אישור (משנה ספירות). ← פתוח.
#21 · 2026-09-24 · history-agent · redo בפיד
- ה-redo בסקיצה הוא מחסנית עגלה לפני שמירה, לא היסטוריה. בפיד: eligible=false, למעט חלונות ביטול-מיידי קיימים (זיכוי 15 דק׳). שאלה למשתמש. ← פתוח.
#22 · 2026-09-24 · history-agent · איחוד מילוני תרגום
- FIELD_TRANSLATIONS/ACTION_TRANSLATIONS מיובאים ב-5 קבצים ומשוכפל ACTION_BADGE_CLASS ב-4 טאבים; מילון אחד (ENTITIES/ACTIONS/FIELDS) במקום. ← הצעה.
#7 · 2026-09-24 · claude-design-agent · שמות טוקנים אמיתיים במקום שמות הסקיצה
- בסקיצה `--gold-50/100` הם אפרסק-ורוד ו-`--gold-700` הוא שזיף. ב-`tokens.css` שינינו שמות ל-`rose-*` ו-`plum`. ← החלטה. (מונע טעות "זהב=ורוד" אצל סוכנים.)
#8 · 2026-09-24 · claude-design-agent · reduced-motion אמיתי
- הסקיצה מכבה את `prefers-reduced-motion` (`min-width:99999px`) להדגמה. ב-`components.css` הוא פעיל: לולאות/נצנוץ כבויים, סימוני draw במצב סיום. ← החלטה (R12).
#9 · 2026-09-24 · claude-design-agent · משך טוסט לפי הקשר
- 3 טוקנים: רגיל 6.5s, מידע 2.6s, `--v3-toast-duration-nav` 15s (R20, אחרי שמירה+מעבר עמוד). המונה הוא CSS (פס זהב) + timeout ב-JS באותו ערך. ← הצעה ליישום בשלב 3.
#10 · 2026-09-24 · claude-design-agent · מצב חלונית ב-attribute ולא ב-body
- בסקיצה `body.dlg-dark` (גלובלי). ב-v3: `data-v3-mode="dark|light"` על החלונית עצמה + `.v3-dialog--form` (בהיר בלבד) — מאפשר חלונית אישור כהה לצד חלונית מייל בהירה בו-זמנית וללא כתיבה על body. ← החלטה.
#11 · 2026-09-24 · claude-design-agent · כפתור "שקט" כרכיב כללי
- בסקיצה ה-ghost בחלונית בלבד (טקסט + קו זהב נמתח). הוצא ל-`v3-btn--quiet` לשימוש בכל האתר ("חזרה/ביטול"). ← הצעה.
#12 · 2026-09-24 · claude-design-agent · מסגרת "זהב עתיק" + טבעת נוסעת כרכיב גנרי
- `.v3-shine` (טבעת conic נוסעת בריחוף) זמין לכל כרטיס; ציר הזמן משתמש בגרסה קבועה. ← הצעה: להשתמש כסימן "פעיל" במקום צל.
#13 · 2026-09-24 · claude-design-agent · רעיונות להכללה
- ראו DESIGN-LANGUAGE §11 (20 רעיונות). בנוסף: (א) `--fill` על מסילת ציר הזמן כדי להציג התקדמות אמיתית בין תאריכים; (ב) קומפוננטת `<V3Toast>` יחידה עם תור (max 1 גלוי + ספירה) כמו קבוצת הבאנרים; (ג) `IconButton` שמייצר `aria-label`+`data-tip` מתוך מילון אייקונים אחד; (ד) קוד 4 ספרות כרכיב `<CodeInput>` משותף (אישור מנהל, PIN עובד, נקודת שעון).
#14 · 2026-09-24 · claude-design-agent · פערים / סתירות בסקיצה שנדרש להחליט עליהם
- (א) ה-ghost ברייל שקוף-לבן, ב-page הסופי זהה למשני — הגדרנו `--quiet` (טקסט) ו-`--on-dark`. (ב) רדיוס חלונית: 26→20→24 (נבחר 24). (ג) גובה שדה חיפוש 48 ושדה רגיל 44 — נשארו כך (48 = חיפוש/פילטר/חלונית). (ד) הטוסט מעוגן `left` פיזית; אם ירצו "start" בסביבה LTR — לשנות. ← פתוח.
#23 · 2026-09-24 · icons-agent · ספריית איקונים v3
- נוצרו app/v3/icons.css, ui/Icon.js, ui/IconSpriteV3.js (12 סמלים חסרים כולל i-unlock), ui/index.js. אנימציות טרנספורם-בלבד לפי ריחוף/מיקוד הורה. ← בוצע.
- draw אמיתי (stroke-dashoffset) דורש pathLength והוא לא עובר דרך <use>; חלופה: לשכפל סמלי check כ-path עם pathLength=1 בתוך Icon. ← רעיון.
- codemod להחלפת <svg className="icon"><use/></svg> (~1,020 מופעים) ב-<Icon/> והסרת lucide-react. ← הצעה.

#23 · 2026-09-24 · notify-agent · קיבוץ הערות פעמון
- קיבוץ הערות activity_note רצופות מאותה ישות (הזמנה נשמרה 3 פעמים) לשורה אחת בפעמון, וניקוי אחרי 30 יום ב-cron קיים. ← הצעה.
#24 · 2026-09-24 · notify-agent · מונה נשארו
- ב-prefers-reduced-motion הפס יורד בצעדים; אפשר להוסיף "נשארו N שניות" לקוראי מסך. ← הצעה.
