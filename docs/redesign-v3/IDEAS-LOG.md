# יומן רעיונות והחלטות
פורמט: `#n · תאריך · מי · נושא` ← מה ← למה ← סטטוס (החלטה/הצעה/פתוח). כל סוכן מוסיף שורות בסוף הקובץ (append בלבד).

#1 · 2026-09-24 · claude-main · פלטות משתמש מול פלטה קבועה
- המערכת הקיימת מאפשרת פלטה לכל עובד (`Employee.themeColor`, `app/lib/customPalette`). סקיצה B קבועה: navy/sky/gold.
- **הצעה:** ה-tokens של v3 הם ברירת המחדל; מערכת הפלטות מוזרמת כ-override של אותם משתנים (`--v3-brand` וכו') כך שההעדפה האישית לא נשברת. להחליט בשלב 2 אחרי מיפוי המשתנים. → שאלה Q2.
#2 · 2026-09-24 · claude-main · הגנה על מכסת deploy
- ענף redesign ידלג על build ב-Vercel (ignore-build). `[force-deploy]` בהודעת קומיט לפריוויו. ← החלטה.
#3 · 2026-09-24 · claude-main · הקדמת `v3-` ו-`data-v3`
- מאפשר מעבר הדרגתי עמוד-אחר-עמוד בלי לשבור את האתר החי. ← החלטה.
#4 · 2026-09-24 · claude-main · שיטת א'/ב' (רעיון 12)
- נבחרה, עם ההבחנה: ב' לפי *ממד* (RTL/מלל/איקונים/חלוניות/התאמה) ולא לפי עמוד. ראו ARCHITECTURE §2. ← החלטה.
#5 · 2026-09-24 · user · הבהרת מקורות
- הסקיצה הקנונית = `Desktop\גמח שמלות חדש\order-card-sketch-B.html` (עותק ב-`sketch/`). מדפיס-בוט (`print-center`) משמש **רק** לדפוס תהליכי-שלבים ספציפיים — לא כמקור עיצוב כללי. ← החלטה.
#6 · 2026-09-24 · user · דף הכניסה בתוך ההיקף
- מעצבים גם את מסך הכניסה (`app/components/LoginScreen.js`, כולל בורר מוסד/עובד/PIN אם קיים). השראה: דף הכניסה של מדפיס-בוט (`print-center`, "לאיזה מוסד מתחברים?") **בשילוב** בסיס העיצוב של סקיצה B. ← החלטה. (מתקן את #5: מדפיס-בוט = שלבים + כניסה.)
#7 · 2026-09-24 · user · אישור ברירות מחדל ל-Q1–Q5
- Q1 ניהול (/admin,/management) מחוץ לשלב א'; employees/profile/display-settings בפנים. Q2 פלטה קבועה + hook. Q3 עמודים בהירים בלבד, חלוניות אישור/קוד כהה+בהיר. Q4 הקיוסק בפנים, סגנון v3 מותאם למגע, מנגנון הנעילה ללא שינוי. Q5 הדפסות ניטרליות. ← החלטה סופית.
