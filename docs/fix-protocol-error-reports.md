# פרוטוקול תיקון דיווחי שגיאות (ErrorReport)

נכתב ב-2026-09-07 בעקבות בדיקת כל הדיווחים הפתוחים. קודם לכן לא היה תיעוד של תהליך התיקון בקבצי המערכת.

**עודכן שוב באותו יום (סבב שני, 2026-09-07 אחה"צ)** אחרי שהתברר שחלק מהדברים
שתועדו כאן כ"בוצע" בכתיבה הראשונה של המסמך בפועל **לא היו מיושמים בקוד בכלל**
(הסבב הראשון נעצר באמצע עבודה) - ר' סעיף 6 למטה. לפני שסומכים על השורה "הוגדרה
ל-X בייצור" בטבלה שבסעיף 3, יש לוודא בפועל מול ה-DB (`SystemSetting`), לא רק
מול המסמך הזה.

**הערת מינוח (נוספה 2026-09-09 לבקשת משה)**: אין להשתמש במילה הלועזית "טוגל" בשיחה,
בתגובות לדיווחים או במסמכי המערכת — המונח לא היה מובן. במקומה כותבים "מתג
הפעלה/כיבוי" (או "מתג" בקצרה בהמשך אותו הקשר). זה חל בכל מקום שבו סעיף 3 למטה
מתאר יצירת `SystemSetting` בוליאני עם שחזור להתנהגות קודמת.

## 0. באג-שורש שהתגלה בסבב השני: יבוא Access דורס roleId (הרשאות)

`scripts/import_from_access.js` עדכן בעבר את `Employee.roleId` גם לעובדים
**קיימים** בכל הרצה מחדש, לפי עמודת Access `מס_מחלקה` - עמודה שאין לה שום זיקה
למושג "הנהלה ראשית" (roleId 0) שהתווסף רק באפליקציה. כל קידום ידני של עובד/ת
ל"הנהלה ראשית" נמחק אוטומטית בהרצת יבוא הבאה,
בלי שום שינוי דרך המסך. שוחזר בזמן אמת על `Employee` `a1ddec72-...` (רחלי
שפרינצלס) - roleId קפץ מ-0 ל-`null` בין שתי בדיקות DB במרווח של כ-20 דקות בלי
עריכה ידנית באמצע. זה מסביר חלק ניכר מהתלונות החוזרות "לא מצליחה להיכנס
בהנהלה ראשית" / "לא נפתח לי" על פני כמה ימים (24-25/8, 3/9, 7/9).

**תוקן**: `roleId` הוצא מ-`updateColumns` בנתיב העדכון (עובד קיים) באותו האופן
שבו `password` כבר היה מוחרג - נשאר רק בנתיב היצירה (עובד חדש). `roleId` של
רחלי שוחזר ל-0 ידנית. **בכל בדיקת הרשאות עתידית שלא הגיונית** ("יש לי roleId
כזה בעולם התוכנה אבל המסך מתנהג כאילו אין") - לבדוק קודם `AuditLog` על
`Employee.id` הרלוונטי לפני שמניחים שהבעיה בקוד ההרשאות עצמו.

## 1. שני בסיסי הנתונים (Neon)

יש שני בסיסי נתונים, חובה לבדוק את שניהם בכל סבב:

| כינוי | הוסט | סטטוס |
|---|---|---|
| NEW-DB (חי) | `ep-orange-waterfall-avthvs1g-pooler.c-11.us-east-1.aws.neon.tech` / `neondb` | בסיס הייצור הפעיל — האתר החי כותב לכאן (`.env` → `PROD_DATABASE_URL`) |
| OLD-DB (מדור קודם) | `ep-royal-dawn-asr9j84y-pooler.c-4.eu-central-1.aws.neon.tech` / `neondb` | בסיס ישן, 5 דיווחי ניסוי מאוגוסט 2026, כולם בארכיון. לא פעיל. נשמר לבדיקה בלבד. |

בדיקה ישירה (מעקף AuditLog) עם `pg`:

```bash
# הסקריפט קורא את PROD_DATABASE_URL מתוך .env — לא להדפיס סיסמאות ללוג
node scratch/check_reports_tmp.mjs
```

## 2. שלבי הטיפול בכל דיווח

1. **קריאה** — `ErrorReport` בסטטוס `OPEN` + כל ה-`ErrorReportReply` בסדר עולה.
2. **שחזור** — למצוא את הקוד/מסך הרלוונטי. אם לא ניתן לשחזר מהתיאור — לבקש הבהרה בשרשור (כולל "אלמנט מסומן") לפני שמנחשים תיקון.
3. **תיקון בקוד** לפי הסעיפים הבאים.
4. **תגובה בשרשור** כמתכנת + `isHandled=true`. סגירה ל-`ARCHIVED` רק כשהמשתמש אישר, כשביטל ("ביטול"), או כשהתיקון הוכח בקוד והמשתמש לא הגיב מעל שבועיים.
5. **שאלות פתוחות** (ערכי הגדרות נכונים, סיסמאות, הבהרות) — נאספות לרשימה בסוף ונשאלות יחד, לא עוצרות את שאר התיקונים.

## 3. כלל ההגדרות עם שחזור (חובה)

כל תיקון שמשנה התנהגות קיימת — ולא רק מתקן באג — חייב להיכנס מאחורי שורת `SystemSetting`
עם ערך ברירת-מחדל = ההתנהגות הקודמת, כך שהנהלה ראשית יכולה להחזיר את המצב הקודם
בקליק אחד ממסך `/admin/settings` (אזור חסום ל-`roleId` 0/2 בלבד, ראה `app/admin/layout.js`):

- מפתח באנגלית, `name`/`notes` בעברית, `category` ו-`type` (`boolean`/`text`/`number`) מתאימים.
- הוספת שם/הסבר בעברית ב-`HEBREW_NAMES`/`HEBREW_NOTES` ב-`app/admin/settings/SettingsClient.js`
  (ומפתחות בוליאניים גם לרשימת `isBooleanKey` שם).
- קריאה בקוד דרך `getCachedSetting` (שרת) או `/api/settings` (לקוח) עם fallback לערך הישן
  כשהשורה חסרה — כדי שהקוד יעבוד גם לפני שהשורה נוצרה ב-DB.
- שינוי ערך בייצור מתועד אוטומטית ב-`AuditLog` (כתיבה דרך Prisma), וניתן לשחזור מהמסך.

הגדרות שנוצרו לפי כלל זה (קטגוריה בסוגריים). **תיקון סבב שני**: בסבב הראשון
המסמך תיעד 5 מהשורות האלה כ"קיימות" בעוד שבפועל אף שורה לא נוצרה ב-DB והקוד
התומך בהן לא נכתב כלל (מלבד `restrict_dress_catalog_to_head_management`, שהיה
קיים אך עם ערך הפוך למה שתועד). כל הרשימה למטה אומתה בפועל מול הקוד וה-DB
בסבב השני:

- `standard_return_hour` (הדפסה) — שעת החזרה בדוח השכרה. נוצרה + `13:00` (תואם את מה שהיה קבוע בקוד קודם, ללא שינוי התנהגות). נקרא ב-`app/print/order/page.js`.
- `rental_belt_notice` (הדפסה) — שורה נוספת מתחת ל"פרטי החזרה" בדוח השכרה. נוצרה עם נוסח על החזרת חגורות (דיווח 8fa101e2) — ברירת מחדל בקוד היא ריק/מוסתר לכל התקנה שאין בה את השורה.
- `error_report_handled_at_bottom` (תצוגה) — פניות מטופלות (`isHandled`) יורדות לתחתית רשימת הפניות הפתוחות ב-`ErrorReportButton.js`. נוצרה עם `true`.
- `show_employee_profile_image` (תצוגה) — הצגת אזור תמונת הפרופיל ב-`/profile` **וגם** בכרטיס העובד המנהלי (`/employees/[id]`) — קודם רק כרטיס העובד הוסתר, וזה נעשה עם `{false && ...}` קשיח בלי הגדרה בכלל. תוקן: שני המקומות מותנים באותה הגדרה. נוצרה עם `false` (הוסר לגמרי בייצור, דיווח c764bef4).
- `allow_employee_messaging` — **לא נוצרה** הגדרה חדשה; במקום זאת תוקן באג בהגדרה הקיימת `hide_internal_messaging` (שכבר הייתה `true`/כבויה בייצור): היא הסתירה נכון את טאב ההודעות ואת פעמון ההתראות, אבל לא את קיצור הדרך בתפריט האווטאר האישי (`UserMenu.js`) - שם נשלחה ההודעה בדיווח b4de5aff. תוקן ב-`app/components/AppShell.js`/`UserMenu.js`.
- `restrict_dress_catalog_to_head_management` (הרשאות) — היה קיים אך עם ערך `true` בייצור (ההפך מהמתועד כאן קודם), ולכן הקטלוג היה חסום לכולם חוץ מהנהלה ראשית/מתכנת - זה בדיוק מה שדיווח d64d3027 התלונן עליו. שונה ל-`false` (קטלוג פתוח לצפייה לכולם). **בנוסף** נמצא ותוקן פער אבטחה אמיתי: יצירה/עריכה/מחיקה של דגם (`app/api/dresses/route.js` POST, `app/api/dresses/[id]/route.js` PUT/DELETE) השתמשו ב-`checkAuth()` גנרי בלי הגבלת תפקיד בכלל - כל עובד מחובר יכל לערוך/למחוק דגם דרך קריאת API ישירה. תוקן ל-`checkAuth('הנהלה ראשית')` בשלושתם, ללא תלות בערך ההגדרה (ניהול הקטלוג מוגבל תמיד, רק הצפייה תלויה בהגדרה). כפתורי "דגם חדש"/מחיקה/שחזור מוסתרים בלקוח (`app/dashboard/dresses/page.js`) לפי roleId שנשלף מ-`/api/me` - **לא בוצע** אותו דבר לכרטיס הדגם הבודד (`/dashboard/dresses/[id]`, שדות העריכה נשארים גלויים לכל צופה) בגלל היקף השינוי הנדרש שם (Modern* components מקוננים) - זה עדיין מוגן בשרת, רק לא מוסתר בלקוח.
- `restrict_refunds_to_head_management` (הרשאות) — קיים ופעיל בקוד (`app/layout.js`) מזמן, אבל **מעולם לא נרשם** ב-`HEBREW_NAMES`/`HEBREW_NOTES` של `SettingsClient.js` - הושלם עכשיו (לא היה חלק מדיווח ספציפי, נתקלנו בזה תוך כדי תיקון הרשומה המקבילה של קטלוג הדגמים).

## 4. תיקונים ללא הגדרה (באגים טהורים)

- ביטול cache שרת ללוגו אחרי העלאה (`invalidateSettingsCache('BRAND_LOGO')`).
- פוטר הדפסה משתמש ב-`gmach_name` במקום מחרוזת קשיחה.
- tooltip והסבר מקרא לסימן `!` בלוח (איחור בהחזרה, לא חוב).
- יישור fallback-ים קשיחים לשם הגמ"ח.

**סבב שני (2026-09-07):**

- ניסוח: "נא להחזיר טופס זה חתום" → "יש להחזיר טופס זה חתום" (`app/print/order/page.js`, דיווח 76b69331).
- עמדת לקוחות (`app/customer-interface/page.js`): הוסרה כמות המלאי הכוללת (`X/Y`) משלוש התצוגות (טבלה/רשימה/כרטיסים) - נשארה רק הכמות הפנויה (דיווח c11ef570).
- `scripts/import_from_access.js`: `roleId` לא נדרס יותר בעדכון עובד קיים — ר' סעיף 0.

**תשובות בלבד, בלי שינוי קוד** (המנגנון כבר עבד כמו שביקשו, רק לא הוסבר/לא הודגם בעברית תקינה):
- סף איחור בהחזרה מהירה = 7 ימים בדיוק, לא פחות (`lib/lateReturn.js`, `LATE_RETURN_THRESHOLD_DAYS`).
- הערות הזמנה כן מופיעות בקבלה (לא רק בדוח השכרה) — `app/print/order/page.js` שורה עם `order.notes`.
- חתימה על תקנון נשאלת גם לפני תשלום וגם לפני הדפסה (`ModernPaymentsManager.js` + `OrderPrintMenu.js`), לא כשלב "סיום הזמנה" נפרד.
- autoComplete="new-password" כבר קיים על כל שדות טופס לקוח חדש כולל מייל — מונע autofill של לקוח קודם.
- שימור מיקום גלילה + צביעת "טופל" ברשימת הפניות (`ErrorReportButton.js`) — היו כבר מיושמים, רק התגובה שהסבירה זאת נכתבה/נשמרה עם קידוד שבור (mojibake) בסבב קודם ולא היה ניתן לקרוא אותה — נכתבה מחדש.

## 5. אימות

- `npx eslint` על כל קובץ ששונה.
- בדיקת fallback כשההגדרה חסרה ב-DB.
- אחרי פריסה: מעבר על שרשורי הדיווחים וסימון `ARCHIVED` למה שאושר.

## 6. לקח מהסבב הראשון (חשוב לכל סבב עתידי)

הסבב הראשון (בוקר 2026-09-07) כתב תגובות "בוצע ✅" למשתמשת ותיעד כאן הגדרות
כ"קיימות", אבל נעצר לפני שהקוד/ה-DB בפועל שיקפו את זה. הפער התגלה רק כשהמשתמשת
המשיכה להתלונן על אותן בעיות. **לפני שסוגרים דיווח ב-ARCHIVED או כותבים "בוצע"
במסמך הזה**:

1. לוודא שהשורה קיימת בפועל ב-`SystemSetting` (לא רק שהקוד קורא אותה עם fallback).
2. לחפש (`grep`) את שם המפתח בכל הריפו — אם אין תוצאה מלבד המסמך הזה, זה סימן שהקוד לא נכתב.
3. להריץ `git status`/`git diff` על מה שבאמת השתנה, לא לסמוך על זיכרון השיחה.
4. `npx eslint` על כל קובץ ששונה לפני שסוגרים.

## 7. סבב שלישי (2026-09-08) — משוב גולמי מנווה יעקב, לא דרך ErrorReport

בניגוד לסבבים 1-2, הפעם לא היה אף `ErrorReport` פתוח במסד הנתונים החי של נווה יעקב
(0 רשומות בטבלה כולה) — הבדיקה מול הפרוטוקול (סעיף 1-2) יצאה נקייה, אבל הלקוחה שלחה
משוב גולמי (כ-28 פריטים נפרדים) ישירות בשיחה, לא דרך כפתור הדיווח באפליקציה. התהליך
הותאם: קיבוץ הפריטים ל-6 נושאים, הרצת סוכן חקירה+תיקון מקביל לכל נושא (ולא טיפול
פריט-אחר-פריט), עם אותם כללי בטיחות (ללא כתיבה ל-DB, ללא הפעלת שרת, ללא push).

**שני שורשים משותפים נמצאו בחיפוש** (ולא רק תיקון-נקודתי): (1) חיפוש שם מלא (2
מילים) לא תאם `firstName`/`lastName` נפרדים בשום מסך; (2) בניית ה-`where` בפריסת
מפתחות (spread) גרמה להתנגשות מפתחות בין הסינון הרגיל לסינון המתקדם שנשאר — סינון
ישן שנשאר בזיכרון "בלע" בשקט חיפוש חדש. תוקן פעם אחת ב-`lib/searchUtils.js` חדש,
משמש בכל שלוש נקודות הכניסה (`app/api/customers/route.js`, `app/api/orders/route.js`,
`app/api/global-search/route.js`).

**הגדרות שהופעלו בפועל (רק ב-DB של נווה יעקב, `scratch/new_gemach_db.env`, ולא נגעו
ב-PROD של הגמ"ח הראשי)** — אושרו במפורש ע"י הלקוחה בתוך המשוב עצמו, לכן לא המתנו
לאישור נוסף:
- `branch_list`: `"נוה יעקב, בית שמש"` → `"נווה יעקב, גב״ש"`.
- `allow_renting_reserve_items` (חדש, `boolean`) → `true`. שדה נפרד מ-`inventory_include_warehouse`
  (רזרבה ≠ מחסן), נבדק בכל נקודות הגישה: `lib/inventory.js` (3 מקומות),
  `app/api/dresses/route.js`, `app/api/rentals/scan/route.js`, `app/api/rentals/verify-item/route.js`,
  `app/api/inventory/preload/route.js`.
- `auto_print_on_order_create` (חדש, `boolean`) → `true`.

**הגדרות/החלטות שנשארו פתוחות (לא הופעלו, דורשות אישור מפורש נוסף לפני יישום —
ר' סיכום מלא בשיחה)**: אחוז זיכוי בביטול בחלון "ללא החזר" (100% היום, נשאלה שאלה
על 50%), רמת אישור ליציאה מהזמנה בלי תשלום (`PAYMENT_APPROVAL_LEVEL` קיים בקוד אך
לא רשום בממשק הניהול כלל), אישור מנהל אמיתי (קוד) לביטול/הוספת פריט בהזמנה קיימת
לעומת ת"ז בלבד כהיום, מדיניות ת"ז-חובה-בלקוח מול מילוי אוטומטי בעת השכרה.

**כלל חדש מהסבב הזה**: תיקון קוד גנרי (לא הגדרה) שמתקן קונפליקט/באג משותף לשתי
הארגונים (כמו שורשי החיפוש) מותר וראוי לתקן ישירות בלי מתג הפעלה/כיבוי נפרד — הוא
לא משנה "התנהגות עסקית" קיימת, רק מתקן דבר שלא עבד כלל. ההבחנה מסעיף 3 (מתג
הפעלה/כיבוי עם ברירת מחדל ישנה) חלה רק כשההתנהגות ה*ישנה* עבדה כמתוכנן וה*שינוי*
הוא שינוי מדיניות/החלטה.

## 8. סבב שלישי (המשך) — מימוש 4 ההחלטות שאושרו + 2 תיקוני קוד משותפים (2026-09-08)

לאחר סעיף 7, ~12 הסעיפים שנשארו כ-NEEDS_DECISION הועברו ללקוחה כשאלות בחירה
מפורשות (לא ניחוש). 4 מהן קיבלו החלטה ברורה ומומשו באותו יום ע"י 4 סוכני מימוש
מקבילים + סוכן חמישי לתיקון-קוד משותף; שאר הסעיפים (מחיר משלוח לפי עיר, דוגמה
ל-AI חכם, צילום מסך חסר) עדיין ממתינים למידע/קלט מהלקוחה ולא מומשו.

### הוראות קבועות לענפים (org-1 = גמ"ח ראשי, org-2 = נווה יעקב) — לכל סבב עתידי

**החוק המנחה, כדי לא להעמיס את השרשור עם ההסבר הזה בכל פעם:**
- **הגדרה/שינוי מדיניות עסקית** (ערך שקובע "איך עובדים", לא רק תיקון תקלה) →
  **תמיד** מאחורי `SystemSetting` חדש עם ברירת מחדל = ההתנהגות הישנה (סעיף 3),
  והפעלה בפועל (כתיבת הערך) **רק** ב-DB שהלקוח/ה שביקש/ה אישר/ה עבורו — לרוב
  `scratch/new_gemach_db.env` (נווה יעקב) בלבד, לא נוגעים כלל ב-DB הראשי.
- **תיקון באג/קונפליקט אמיתי בקוד** (משהו שלא עבד כמתוכנן בשום מקום, לא רק
  שינוי מדיניות) → תיקון קוד גנרי, בלי מתג הפעלה/כיבוי נפרד, חל אוטומטית על שני
  הענפים ברגע שיעלה לאוויר (סעיף 7). זה תקין ורצוי — לא צריך אישור נפרד לכל
  תיקון תקלה כזה.
- **כל כתיבת `SystemSetting` ל-DB חייבת שורת בדיקת host** לפני ה-`UPDATE`/`INSERT`
  בפועל (`if (!host.startsWith('ep-rough-dawn-b1bepx3d')) throw ...` לנווה יעקב) —
  אותו דפוס כמו `scratch/write_new_gemach_settings.js`. אף פעם לא כתיבה "עיוורת"
  מול `.env` ברירת המחדל.
- **אימות סקופ לפני שסוגרים סבב**: לא מספיק שסוכן מדווח "כתבתי רק לנווה יעקב" —
  יש להריץ שאילתת `SELECT` ישירה ועצמאית (לא דרך הסוכן) מול **שני** ה-DB על כל
  מפתח שנוצר/שונה בסבב, ולוודא: קיים+נכון בנווה יעקב, **ואין שורה בכלל** (או
  ברירת המחדל הישנה) בגמ"ח הראשי. זה מה שנעשה בפועל בסבב הזה (ר' טבלה למטה) ומצא
  אפס פערים — אבל האימות עצמו הוא החלק החשוב, לא ההנחה שהסוכנים דייקו.

### 4 ההגדרות הדינמיות שנוצרו (כולן ב-`/admin/settings`, כולן boolean/select עם label+הסבר בעברית, כולן מופעלות רק בנווה יעקב)

| מפתח | סוג | ערך בנווה יעקב | ערך בגמ"ח הראשי | קובץ ליבה |
|---|---|---|---|---|
| `PAYMENT_APPROVAL_LEVEL` | select | `"מנהל סניף ומעלה"` (roleId 0/1/2) | אין שורה → `"כולם"` (ישן) | `app/orders/new/page.js`, `app/api/auth/verify-pin/route.js` |
| `require_manager_code_for_item_changes` | boolean | `true` | אין שורה → `false` (ת"ז בלבד, ישן) | `app/api/orders/[id]/items/route.js`, `app/api/orders/[id]/route.js`, `lib/managerAuth.js` |
| `require_customer_id_number` | boolean | `true` | אין שורה → `false` (לא חובה, ישן) | `app/api/customers/route.js`, `app/customers/[id]/page.js`, `app/orders/new/page.js` |
| `allow_additional_payment_on_order` | boolean | `true` | אין שורה → `false` (כפתור מוסתר, ישן) | `components/orders/modern/ModernPaymentsManager.js`, `POST /api/payments` (נעשה שימוש חוזר, לא נוסף route) |

כל ההגדרות הללו אומתו ישירות מול שני ה-DB אחרי המימוש (לא רק דיווח סוכן) — אפס
פערים, אפס שורות שדלפו לגמ"ח הראשי.

### 2 תיקוני הקוד המשותפים (חלים על שני הענפים, לפי הכלל שבסעיף 7 — לא הגדרה)

- **Gate אישור-תשלום על סכום 0** — הבדיקה "יציאה באישור מנהל" דילגה כשה-`pAmount`
  נשאר 0 (בדיוק המצב של יציאה בלי תשלום). תוקן ב-`app/orders/new/page.js`.
- **חיוב משלוח אוטומטי לפי עיר בהזמנה קיימת** — עבד רק ביצירת הזמנה חדשה, לא
  בהוספת/עריכת משלוח בהזמנה שכבר קיימת. אוחד ללוגיקה משותפת אחת
  ב-`lib/pricingEngine.js` (`applyDeliveryCharge`), נקראת משני המקומות. **נבדק
  ואומת**: `delivery_price_by_city` ריק בשני ה-DB כרגע → אפס השפעה כספית מיידית,
  ישפיע רק אחרי שמישהו ימלא מחירים בפועל בהגדרות.

### סטטוס בסוף הסבב

23 קבצים מהסבב המקורי (סעיף 7) + 8 קבצים נוספים מ-4 סוכני ההחלטות (חלקם חופפים,
`app/admin/settings/SettingsClient.js` נערך ע"י 4 סוכנים במקביל — נבדק שאין
מפתחות כפולים, נקי) = 32 קבצים, `npx eslint` נקי (שגיאת ref אחת קיימת גם
ב-`HEAD`, לא נגרמה מהעבודה הזו). **עדיין לא הועלה לאוויר (`git push`)** —
ממתין לאישור מפורש, כי זה deploy משותף לשני הענפים בבת אחת.

**עדיין פתוח, ממתין לקלט מהלקוחה** (לא הוגדר עדיין, לא סעיף לתיקון): ערכי מחיר
משלוח בפועל לפי עיר עבור `delivery_price_by_city`, דוגמה קונקרטית לחיפוש AI חכם
שנכשל, צילום המסך החסר של האלמנט ה"נדבק" (sticky) בעמדת לקוחות, תיאור מדויק של
היכן חיפשה הלקוחה תשלום אשראי ולא מצאה.

## 9. סבב רביעי (2026-09-10) — הבחנה מפורשת בין "תגובה סתם" לבין "שאלה פתוחה"

עד עכשיו הסיווג "ממתין לתשובת משתמש" (סעיף 2 ב-`fix-reports.md`) הסתמך על **ניחוש
לפי ניסוח** — "התגובה האחרונה היא שאלה" נקבע מקריאת הטקסט, לא משדה מפורש. משה ביקש
שדיווחים שממתינים בפועל לתשובה יסומנו בגוון שונה ברשימה, ושיהיה לסוכן דרך **מפורשת**
"לרשום" תגובה סתם מול לכתוב תגובה שהיא בעצם שאלה פתוחה — כדי שהצבע לא יהיה ניחוש.

**מה נוסף בקוד:**
- שדה חדש `ErrorReportReply.isQuestion` (boolean, ברירת מחדל `false`) ב-`prisma/schema.prisma`
  — נוסף בשני ה-DB-ים (שינוי תוסף בלבד, לא הורס נתונים קיימים): גמח 1 דרך `prisma db push`
  הרגיל (משתמש ב-`DATABASE_URL`/`PROD_DATABASE_URL`), גמח 2 (נווה יעקב) דרך
  `node scratch/add_is_question_column_org2.js` — ALTER TABLE גולמי מול ה-DB הנפרד שלו
  (`scratch/new_gemach_db.env`), כי `prisma db push` כותב רק ל-DB אחד לפי ה-schema
  היחיד שיש (ר' תבנית זהה שכבר הייתה קיימת ל-`previewUrl` ב-
  `scratch/add_preview_url_column_org2.js`). אומת בפועל ששתי העמודות קיימות (2026-09-10).
- `scripts/error-report-reply.js` — דגל `--question` חדש (ר' תיעוד בראש הקובץ ובסעיף
  "כלים" ב-`fix-reports.md`). בלי הדגל = תגובה סתם (`isQuestion: false`, ברירת מחדל).
- `scripts/read-error-reports.js` — `isQuestion` נוסף לכל אובייקט `reply` ב-JSON שחוזר,
  כדי שהסיווג ב-`fix-reports.md` סעיף 2-4 יתבסס על שדה מפורש, לא על טקסט.
- `app/components/ErrorReportButton.js` — שורת רשימה עם `isQuestion: true` בתגובה
  האחרונה מקבלת `var(--warning-tint)` + badge "ממתין לתשובה" (גובר על "טופל" - שרשור עם
  שאלה פתוחה לא יכול להיחשב "טופל" באמת). באנר דומה מופיע בראש חלונית השרשור עצמו.
  תיבת סימון "זו שאלה פתוחה - ממתינה לתשובה" נוספה לטופס התגובה (גם למשתמש רגיל, לא
  רק לתמיכה — סימטרי, שימושי גם כשמנהל שואל את התמיכה ומחכה).

**כלל לכל סבב עתידי:** תגובת "ראיתי, בודק"/"תוקן"/כל עדכון-סטטוס = **בלי** `--question`.
שאלה שבאמת עוצרת את הטיפול עד שמישהו עונה = **עם** `--question`, תמיד. אל תסתמכו על
ניסוח בסימן שאלה כאינדיקציה — משפט רטורי ("בודק, יכול להיות שזו הבעיה?") הוא עדיין
"תגובה סתם" אם אתם ממשיכים לטפל בלי לחכות לתשובה.

## 10. מיזוג ידני ל-main חייב לסגור את ה-PR עצמו (2026-09-10)

`fix-reports.md` אומר במפורש שמיזוג ה-PR הוא **תמיד** ביד משה, אבל בפועל המיזוג
בכמה סבבים (09-09, 10-09) נעשה ע"י session אינטראקטיבי של קלוד-קוד שמיזג את הענפים
**ישירות מקומית** (`git merge --no-edit` ואז `git push` ל-`main`, ר' `git-push-tls-
reset-workaround` בזיכרון) ולא דרך כפתור ה-Merge ב-GitHub. זה מוריד את התוכן ל-main
בהצלחה (מאומת - Vercel פורס משם), אבל **לא סוגר את ה-PR עצמו** ב-GitHub - הוא נשאר
מוצג "Open" למרות שהתוכן שלו כבר בפרודקשן.

זו לא רק בעיה קוסמטית: ה-digest האימייל הפעמיים-ביום שנוסף באותו יום
(`lib/agentDigest.js`, ר' CLAUDE.md "Agent PR-approval digest email") קורא בדיוק את
רשימת ה-PR-ים הפתוחים דרך GitHub API כדי לדווח "מה מחכה למיזוג ידני" - בלי תיקון,
הוא ימשיך לדווח על PR-ים ישנים כ"ממתינים" גם אחרי שהם כבר חיים בפרודקשן.

**הכלל מכאן ואילך: כל session שממזג ענף fix-reports/* ישירות (לא דרך GitHub) חייב
גם לסגור את ה-PR המתאים** - אחרת הרשימה שקרית. `node scripts/fix-reports-merge-status.js
--close-stale` (צריך `GITHUB_TOKEN`+`--repo=owner/repo`, או משתני הסביבה המקבילים
שכבר קיימים ב-workflow) עושה את זה אוטומטית: משווה כל PR פתוח עם `head.ref` שמתחיל
ב-`fix-reports/` מול `main` דרך ה-Compare API, וסוגר (עם תגובה מסבירה) כל PR שה-
תוכן שלו כבר "identical"/"behind" ביחס ל-main. בלי טוקן/repo (הרצה ידנית מקומית
רגילה) הסקריפט נופל לבדיקת `git merge-base --is-ancestor` מקומית בלבד - מספיק כדי
לענות "מה כבר ב-main", לא יכול לסגור PR-ים.

**לא בוצע עדיין בפועל** (2026-09-10): לא הייתה גישה לטוקן GitHub תקף בסשן שבו זה
נכתב (גם `gh auth status` וגם `GH_DISPATCH_TOKEN` המקומי לא זמינים) - יש לוודא
שה-14 ענפי fix-reports/* שכבר מוזגו (ר' למטה) אכן נסגרו ב-GitHub בהזדמנות הראשונה
שיש טוקן תקף, ומכאן והלאה זה צעד קבוע אחרי כל מיזוג ידני.

מצב נכון ל-2026-09-10 17:23 (מאומת דרך `git branch -r --merged origin/main`, לא
דרך GitHub): **כל** ענפי fix-reports/* משני הסבבים (09-09 המקורי + 09-10, כולל
`fix-reports/main-2026-09-10` של הגמח הראשי) כבר ב-`main` - שום ענף לא ממתין למיזוג.

## 11. סבב בדיקת ענפים ממתינים 2026-09-14 - 4 מוזגו, 2 ענפים מיותרים אותרו, 1 דורש טיפול ייעודי

**תקלת `desktop.ini` חזרה** (ר' CLAUDE.md "Windows desktop.ini pollution breaks git") -
הפעם Google Drive File Stream כתב את הקובץ לתוך `.git/refs/**` (heads/remotes/tags,
כולל תתי-תיקיות `claude/`, `fix/`, `redesign/`) ומנע `git fetch --prune` לגמרי
(`fatal: bad object refs/desktop.ini`). תוקן באותו ניקוי מתועד + נוספה שורת
`desktop.ini` ל-`.git/info/exclude` (מקומי, לא משפיע על שאר המכונות/סשנים) כדי
שלפחות מעקב `git status`/`ls-files` לא יתלונן עליו יותר - זה **לא** פותר את
ה-Explorer/Drive שממשיכים לכתוב את הקובץ מחדש, רק את הרעש סביבו.

**נבדקו כל הענפים המקומיים/מרוחקים מול `main`** (`git branch --no-merged`,
`git branch -r --no-merged`, השוואת base-commit לכל ענף חשוד):

- **4 PR-ים פתוחים, כולם נקיים ומוזגו** (#44-#47, כולם `fix-reports/org2-2026-09-14-*`,
  כולם דיווחי נווה יעקב אבל תיקוני קוד גנריים בלי שום `SystemSetting`/הארדקוד ארגוני -
  נבדק ב-grep על כל דיף לפני המיזוג, תואם לכלל ב-`code-fixes-vs-settings-scope-rule`):
  חיוב משלוח חסר בתגובת יצירת הזמנה, פופ-אפ תזכורת איחור שחוזר ברענון עמוד, דחיסת
  הדפסת-קבוצה לעמוד אחד, ובאג תאריך יום-לפני + regex עברי בהדפסת שם-מקום-שמור.
  כל הבדיקות (Vercel גמח 1 + גמח 2) עברו לפני המיזוג. ממוזג עם `gh pr merge --merge
  --delete-branch` (לא merge מקומי+push, בניגוד לסעיף 10 - כך שה-PR נסגר אוטומטית,
  אין צורך ב-`fix-reports-merge-status.js --close-stale` הפעם).
- **`fix/data-explorer-override-collision` - מיותר, לא למזג.** נוצר מנקודה ישנה
  ב-`main` (185 קומיטים אחורה, לפני שכל האפליקציה עברה ל-CSS vars דרך ה-redesign-v2).
  אומת: ל-`main` הנוכחי כבר **אין אף צבע הארדקוד** ב-`app/admin/data-explorer/page.js`
  (0 hex matches) - כל מה שהענף הזה בא לתקן כבר מתוקן בצורה שיטתית יותר. מיזוג שלו
  היה מחזיר קוד ישן/קבצים מתים (`components/orders/OrderGeneralDetails.js` וכו') שכבר
  נמחקו במכוון.
- **`fix/neve-yaakov-round3-2026-09-08` - מיותר, לא למזג.** זה בדיוק הענף שהוזכר
  ב-[[neve-yaakov-feedback-batch-2026-09-08]] כ"נדחה לענף צד כדי לא לשבש session
  מקבילה" - אבל אותו תוכן (כולל **4 ההגדרות הדינמיות**: `PAYMENT_APPROVAL_LEVEL`,
  `require_manager_code_for_item_changes`, `require_customer_id_number`,
  `allow_additional_payment_on_order`) כבר הגיע ל-`main` בדרך אחרת - קומיט `dba4a5b`,
  מאומת כ-ancestor של `main` היום, וכל 4 ההגדרות קיימות ומחוברות ב-`SettingsClient.js`
  עם תוויות/תיאורים בעברית. הענף `cdcd122` עצמו נשאר מאחור (בסיסו ישן מ-185 קומיטים)
  ומיזוג שלו ישבור/יחזיר המון קוד. **אין צורך בהגדרות דינמיות חדשות** - כל מה שהתבקש
  כבר בפאנל הניהול.
- **`fix/security-and-data-integrity-audit` - תוכן אמיתי, טרם מוזג, דורש טיפול
  ייעודי.** גם הוא מבוסס 185 קומיטים אחורה (ללא PR), אבל בניגוד לשני הענפים הקודמים
  התוכן שלו **עדיין רלוונטי ולא סופק בשום מקום אחר**: אומת ישירות מול `main` הנוכחי
  שנקודות הקצה `app/api/orders/[id]/route.js` (GET/PUT/DELETE) ו-`POST
  /api/employees` (כולל קביעת `roleId`!) עדיין **חסרות לגמרי** קריאת `checkAuth`/
  `checkPageAccess` - כל מי שיודע/מנחש URL יכול לצפות/לערוך/למחוק הזמנה או ליצור
  עובד חדש בלי להתחבר. גם `lib/sqlGuard.js` (חוסם שאילתות SQL לא-SELECT שנוצרות
  ע"י Gemini לפני `$queryRawUnsafe`) עדיין לא קיים ב-`main`. ניסיון מיזוג יבש הראה
  9 קונפליקטים אמיתיים (תוכן, לא רק מבנה) כי `main` המשיך להתפתח באותם קבצים
  185 קומיטים. **לא מוזג בסשן הזה** - דורש עבודה זהירה קובץ-קובץ שמצדיקה thread
  נפרד; ר' הפרומפט המוכן שניתן למשה בסיכום הסשן.

**לא נדרשו הגדרות דינמיות חדשות בפאנל הניהול** בסבב הזה - כל 4 התיקונים שמוזגו הם
באגי קוד טהורים בלי רכיב הגדרה, וההגדרות הדינמיות שכבר היו בתהליך (מסבב 3) כבר
חיות ב-`main`/בפאנל. נותרו פתוחים (ללא שינוי, ר' [[fix-reports-automation-system]]):
פיצול `require_manager_code_for_item_changes` לשתי הגדרות נפרדות, כלל "כיוון משלוח"
אוטומטי, וכלל האיחור בפופ-אפ overdue - כל אלה דורשים החלטה של משה, לא קוד.

## 12. דיווח מלאי מאהובה פינקל (נווה יעקב, 2026-09-14→15) - שני באגי זמינות אמיתיים

דווח ישירות בצ'אט (לא דרך ErrorReport): דגם 549 הראה 27 פנויות בעוד שבפועל יש 21;
דגם 551 הראה 59 מול 56 שראתה באקסס. חקירה עמוקה (כמה סבבים, כולל השוואה ישירה מול
השאילתאות/VBA האמיתיים של אקסס עצמו ולא רק שחזור-ידני של הלוגיקה) העלתה **שלוש**
תיאוריות שנבדקו ונדחו לפני שנמצא הדבר האמיתי:

- **`allow_renting_reserve_items`** (ר' [[gemach-app-2026-09-08-fixes-batch]]) באמת
  משפיע (מוסיף רזרבה למאגר הזמין), אבל **לא** הוסבר על ידו לבד - גם עם/בלי המתג
  התוצאה לא התיישבה עם 21/56 המדווחים. המשתמשת דחתה את ההסבר הזה במפורש ("זה לא
  זה, אל תטיח") - וצדקה.
- **פריטי DressItem "רפאים"** (הבאג שתוקן ב-2026-09-14, ר' CLAUDE.md) - נבדק
  ישירות מול Access ונשלל: 549/551 תואמים ל-Access שורה-שורה, אין רפאים חדשים.
- אין ספירה כפולה בשאילתה, אין הזמנות תקועות כ-isTaken בטעות, אוצר המילים של שדה
  `location` בכל נווה יעקב הוא בדיוק 4 ערכים (`חנות`/`null`/`רזרבה`/`מחסן`) ותואם
  את רשימת ההחרגה הקיימת - שום מונח סטטוס חסר.

**מה שכן היה נכון**, מושווה ישירות מול השאילתה האמיתית `זמינות_שאילתה_זמנית`
(`access_code/Modules/AI.txt`, עדכן_זמינות_טופס) שרצה בפועל ב-Access:

1. **חלון-לפני אסימטרי (תוקן חלקית - הגודל המדויק עדיין פתוח).** Access חוסם ביקוש
   גם *לפני* תאריך היעד, לא רק אחריו - ב-`עדכן_זמינות_טופס` `daysBefore = daysAfter`,
   שניהם נגזרים מאותו ערך יחיד וגולמי (`הגדרות_ראשי` קוד 6, "ימים לשמלה" = 2, בלי
   `+1+`). ב-JS זה היה קבוע ל-0 בשני המקומות (`computeInventoryAvailability` ו-
   `getBulkAvailableInventory` ב-`lib/inventory.js`) - למרות שההערה שממש מעל הקוד
   כבר תיארה "bufferDays is applied fully before AND after". **נבדקו כמה גרסאות
   מול הנתונים החיים (549/551 ל-2026-10-07) ואף אחת לא שיחזרה בדיוק את 21/56:**
   סימטרי מלא עם ה-`+1` הקיים (549→15, 551→28, יותר מדי חסימה), `before=bufferDays`
   בלי `+1`/`after` ללא שינוי (549→18, 551→35, פחות מדי), סימטרי בלי `+1` (549→19,
   551→41), וערך אקסס הגולמי 2/2 (549→27 - כאילו לא השתנה כלום, 551→57 - כמעט
   מדויק במקרה). **נבחרה הגרסה השמרנית**: `daysBefore = bufferDays` (בלי `+1+` מומצא),
   `daysAfter` נשאר בדיוק כפי שהיה בפרודקשן (`bufferDays + 1`) בלי שינוי - כלומר
   רק מוסיפים את חלון ה"לפני" שהיה חסר לגמרי, ולא נוגעים בהתנהגות ה"אחרי" הקיימת
   שכבר חיה משני הארגונים. זו לא הוכחה כמדויקת ב-100% (עדיין לא מתיישבת בדיוק
   מוחלט מול 21/56) אבל היא ההרחבה הכי מינימלית והכי בטוחה של התנהגות קיימת.
   **השאילתה החיה עצמה של אקסס (`זמינות_שאילתה_זמנית`) לא הצלחנו להריץ בפועל בסבב
   הזה כי הקובץ נעול (כנראה משה/אהובה פתוחים אותו) - זו הדרך היחידה לוודא את המספר
   המדויק, לא עוד שחזור-נוסחה.**
2. **דgish `dressItemId` שגוי בזמן יצירה (תוקן, זה היה הבאג המשמעותי).** ב-`AuditLog`
   של 6 שורות `OrderItem` אמיתיות (הזמנות 53218×2, 53225, 53234, 53235, 53237 -
   האחרונה נוצרה באותו יום על ידי אהובה פינקל עצמה, דרך המסך החי) נמצא ש-`dressItemId`
   הצביע על דגם שגוי **כבר ברגע היצירה** (לא swap מאוחר יותר) - כלומר Access עדיין
   סופר את ההזמנות האלה תחת 549, אבל ב-Postgres הן כבר לא נספרות שם בכלל
   (`buildBookingsWhere` מתאים לפי `dressItem.dressModelId` בפועל). שורש הבעיה:
   `app/api/orders/route.js` ו-`app/api/orders/draft/route.js` השתמשו ב-`item.sampleItemId`
   שנשלח מהלקוח **כפי שהוא**, בלי לוודא שהוא אכן שייך ל-`item.dressModelId` שנשלח
   לצידו - בניגוד לתבנית הבטוחה שכבר קיימת ב-`app/api/orders/[id]/items/route.js`
   ו-`[itemId]/route.js`, ששתיהן *מחשבות מחדש* פריט תקין מתוך `computeInventoryAvailability`
   ולא סומכות על מזהה מהלקוח. `sampleItemId` ב-`lib/inventory.js` הוא "פריט מייצג"
   לתצוגה (הראשון שנתקל בו בלולאה), לא בדיקת התאמה בזמן שמירה - כשהמשתמש/ת מחליפ/ה
   דגם/מידה מהר במסך, יכול להישאר `sampleItemId` מהבחירה הקודמת בעוד `dressModelId`
   כבר מעודכן. **6 השורות שנמצאו לא היו מסוכנות פיזית** (כולן `isTaken=false`,
   `barcode=null` - שום דבר לא יצא בפועל בטעות) אבל זו בדיוק התופעה שגרמה לפער
   בזמינות המדווח. תוקן ב-`lib/inventory.js` (`reconcileDressItemIds` חדשה -
   משווה כל `sampleItemId` מול `dressModelId` הנלווה, ומחליפה בפריט תקין מה-DB
   כשיש חוסר-התאמה) וחובר בשתי נקודות הכתיבה (`app/api/orders/route.js`,
   `app/api/orders/draft/route.js`) - לפני יצירת הפריטים, מחוץ לטרנזקציה.

**לא תוקן/לא נדרש תיקון**: 6 שורות ה-`OrderItem` ההיסטוריות שכבר נוצרו עם
`dressItemId` שגוי - עדיין `isTaken=false` ולא נלקחו פיזית, ולכן אין דחיפות
לתקן נתונים היסטוריים (ההזמנות עצמן תקינות, רק ה"תיוג" הפנימי שגוי); ניתן לתקן
בסקריפט חד-פעמי אם וכשיתברר שזה משפיע על ספירה בפועל. `allow_renting_reserve_items`
נשאר דלוק בכוונה (הגדרה עסקית מפורשת, לא באג).

### עדכון 2026-09-15 (המשך אותו יום) - הרצת השאילתה האמיתית + תיקון סופי לחלון הימים

אחרי שהקובץ שוחרר (חובר מחדש ע"י המשתמשת), הצלחנו להריץ בפועל - לא לשחזר - את
הפונקציה האמיתית שמזינה את קומבו-בוקס בחירת השמלה במסך ההזמנה החי:
`שמלות_מקור_שורה_לפי` (`access_code/Modules/הזמנות.txt`), דרך `Application.Run`
ב-COM automation (`AutomationSecurity=1` כדי לעקוף את פס-האבטחה שהיה תוקע את
האוטומציה בסבבים קודמים; כל הארגומנטים חייבים `[ref]`, אחרת שגיאת
"should be a PSReference"; תאריך חייב פורמט ISO `yyyy-mm-dd` - `MM/DD/YYYY` מתפרש
לפי locale ישראלי כ-DD/MM). **התוצאה תואמת בדיוק את מה שדווח: 549→21, 551→56.**
זה גם חשף וגם פתר את שתי הנקודות שנשארו פתוחות בסעיף הקודם:

1. **שני הפונקציות "האמיתיות" שנבדקו קודם (`GenerateInventoryShortagesSQL`,
   `GetAvailabilitySQL` ב-`AI.txt`/`בדיקות_מלאי.txt`) מתבררות כשבורות/לא-בשימוש** -
   הן מפנות לשדות (`אירוע_חול`, `מתאריך`, `עד_תאריך`, `קוד_הזמנה`) שלא קיימים ב-view
   שהן משתמשות בו כברירת מחדל (`הזמנות_פרטים_מלא`) - היו זורקות "too few parameters"
   אם היו רצות בפועל. `שמלות_מקור_שורה_לפי` היא הפונקציה שבאמת מחוברת לטופס החי.
2. **הערך הגולמי של `הגדרות_ראשי` קוד 6 שונה בין הארגונים** - נווה יעקב = **2**,
   הגמח הראשי = **3** (נבדק ישירות בשני קבצי ה-Access). זו לא הגדרה גלובלית משותפת.
   האתר עצמו סימטרי לגמרי ב-Access (`daysBefore=daysAfter`, שניהם מאותו ערך) - אין
   שום `+1`. **תוקן**: `daysBefore`/`daysAfter` ב-`computeInventoryAvailability` וב-
   `getBulkAvailableInventory` (`lib/inventory.js`) עכשיו סימטריים לגמרי, שניהם
   `bufferDays` בלי `+1` מומצא (מחליף את הגרסה השמרנית הקודמת).
3. **נמצאו שתי הגדרות "רדומות" כפולות** - `min_rental_days`/`max_rental_days`
   (`SystemSetting`, יובאו מ-Access קוד 5/6 ב-`scripts/import_from_access.js`,
   השם המוצג "מינימום/מקסימום ימי השכרה לשמלה") קיימות ב-DB של נווה יעקב עם הערך
   הנכון מאקסס (2) אבל **לא מקושרות לשום קוד בפועל** - grep מלא מראה שהן מופיעות
   רק בטבלת המיפוי של סקריפט הייבוא. ההגדרה שבאמת נקראת בכל מקום בקוד היא
   `inventory_buffer_days` (מסונכרנת עם `BUFFER_DAYS`), שקיבלה ברירת מחדל 3 בלי
   קשר לערך האמיתי מאקסס. **תוקן**: `inventory_buffer_days` עודכן ל-2 בנווה יעקב
   (נשאר 3 בגמח הראשי - כבר היה נכון), ושתי ההגדרות הכפולות/הרדומות נמחקו.

**אומת מול הנתונים החיים** לאחר כל השינויים (549/551, 2026-10-07,
`bufferDays` סימטרי): התוצאה מתקרבת משמעותית ל-21/56 האמיתיים; הפער השייר קטן
ונובע בעיקר מ-`allow_renting_reserve_items` (עדיין דלוק בכוונה, ר' למעלה) ומתזוזת
הזמנות אמיתית בין רגע הדיווח לרגע הבדיקה (זמן חלף, הזמנות חדשות נכנסו). זו לא
טעות בנוסחה - שני הצדדים מחשבים נכון לפי ההגדרות שלהם.

### עדכון נוסף 2026-09-15 - באג אזור-זמן אמיתי וחי בפרודקשן (נמצא ותוקן)

בהשוואה שורה-שורה בין נתוני Postgres לנתוני Access לאותן הזמנות בדיוק (למשל
הזמנה 52733), נמצא שדגם 549 גודל 02 מציג תפוסה גבוהה משמעותית באתר לעומת
אקסס גם אחרי שכל ההגדרות תואמות. הגורם: `lib/inventory.js` היה מעגל כל
`eventDate`/`fromDate`/`toDate` עם `new Date(...).setHours(0,0,0,0)` - פעולה
שתלויה **באזור הזמן של השרת שמריץ את הקוד**, לא בשעון ישראל. הזמנה 52733
שמורה כ-`2026-10-11T21:00:00.000Z` (= חצות בדיוק ב-12/10 שעון ישראל, תואם
בדיוק לתאריך שמופיע באקסס) - אבל Vercel מריץ פונקציות ב-UTC כברירת מחדל (אין
משתנה `TZ` מוגדר בשום מקום בפרויקט), כך שבפרודקשן החי `setHours(0,0,0,0)`
היה מעגל בפועל ל-11/10, יום שלם אחורה, בכל הזמנה. לא שוחזר במקומי כי המחשב
שכאן רץ בשעון ישראל ממילא.

**תוקן**: `toIsraelCalendarDate()` חדשה ב-`lib/hebrewDate.js` (אותה שיטת
`Intl`/`toLocaleDateString` שכבר קיימת ב-`toIsraelDateKey`,
`app/print/alterations/page.js`) - מקבלת כל instant גולמי ומחזירה Date
מעוגן בחצות UTC שמייצג את היום הקלנדרי הנכון בישראל, בלי תלות באזור הזמן של
התהליך שמריץ אותו. הוחלף בכל מקום ב-`lib/inventory.js` שקרא תאריך מהמסד
(`computeInventoryAvailability`, `getBulkAvailableInventory`,
`checkMissingDressForItem`, וקלט ה-`targetMinDate`/`targetDate` הראשוני של
`loadInventoryContext`/`getBulkAvailableInventory`).

**חשוב**: תיקון זה לא סגר את כל הפער בבדיקת 549/551 - השוואה מלאה, מידה-מידה,
מראה שרוב המידות (08,10,14,16,18,34-46) מדויקות ב-100% מול אקסס, אבל כמה
(02,03,04,05,06,12) עדיין מראות פער אמיתי גם אחרי כל התיקונים (הגדרות +
אזור-זמן). לא נמצא הסבר סופי - יתכן הבדל אמיתי במנגנון בין ה"טלסקופ-דלתא" של
אקסס (שבודק כל הזמנה אחרת מול יום ספציפי אחד לפי התאריך המדויק שלה) לבין
הלוגיקה של האתר (שמרחיבה כל הזמנה אחרת לטווח ± ימים סביב התאריך שלה) - שתי
גישות סבירות למימוש אותו רעיון, שלא בהכרח מניבות תוצאה זהה לכל מידה בנפרד גם
כשהסך הכולל יוצא קרוב. דורש המשך חקירה ייעודי אם רוצים סגירה מלאה 100%.

## 13. סגירת הפער בבדיקת 549 - שני אפקטים נפרדים, לא אחד (2026-09-15, המשך אותו יום)

חקירה נוספת לפי בקשת המשתמש ("תחקור את ההבדלים שעדין יש") הרחיבה את ההשוואה
לכל 18 המידות של דגם 549 (לא רק ה-6 שדווחו במקור) מול נתונים חיים אחרי כל
התיקונים בסעיף 12. תוצאה: 13/18 מידות תואמות בדיוק, כולל מידה 38 שהמשתמש בדק
ספציפית. אבל 5 מידות (05,08,10,12,16) עדיין סוטות ב-1 יחידה - יותר ממה
שחשבנו (08/10/16 מעולם לא נבדקו קודם).

**שני אפקטים נפרדים ובלתי-תלויים, שבמקרה מבטלים זה את זה בחלק מהמידות:**

1. **רזרבה (לפי עיצוב, לא באג).** לכל המידות כמעט יש בדיוק פריט אחד
   ב"מיקום=רזרבה". אקסס מוציא רזרבה מהמלאי תמיד וללא אפשרות כיבוי
   (`IIF(מיקום='רזרבה',0,-1)` בשאילתת המלאי עצמה). ב-נווה יעקב ההגדרה
   `allow_renting_reserve_items` דלוקה בכוונה (החלטה עסקית קיימת) - כך שהמלאי
   הכולל של האתר גבוה ב-1 מזה של אקסס בכל מידה עם פריט רזרבה. אומת: חישוב
   מחדש עם רזרבה כבויה סגר בדיוק את 5 המידות שסטו (05,08,10,12,16).
2. **כפל-חיץ אמיתי בקוד - באג, לא רק הבדל מאקסס.** כיבוי הרזרבה חשף שמידות
   אחרות (02,03,04,06,14) *הפכו* לסטות בכיוון ההפוך - כלומר הפער השני הזה היה
   שם כל הזמן, רק הוסתר במקרה כי הוא ביטל בדיוק את אפקט הרזרבה במידות האלה.
   המשתמש הצביע נכון על השורש: `computeInventoryAvailability`/
   `getBulkAvailableInventory` הרחיבו את `bufferDays` **פעמיים** - פעם אחת
   לבניית טווח הימים הנסרק סביב תאריך היעד (`targetDays`), ופעם שנייה כדי
   להרחיב את התאריך של **כל הזמנה אחרת** ±bufferDays סביב עצמה - מה שהכפיל
   בפועל את המרחק הנדרש בין שתי הזמנות שונות ל-2×bufferDays במקום bufferDays
   אחד. אקסס לעומת זאת בודק רק אם התאריך המדויק של ההזמנה האחרת נופל על אחד
   מהימים הנסרקים - בלי הרחבה שנייה. הוכח עם הזמנה אמיתית: הזמנה 52733
   (תאריך אירוע 12/10) סומנה בטעות כחוסמת שאילתה ל-07/10 כי החלון שלה עצמה
   (±2 מ-12/10) חפף לחלון הנסרק (±2 מ-07/10) - למרות שהמרחק האמיתי בין
   12/10 ל-07/10 (3 ימי עסקים) גדול מ-bufferDays בודד (2).

**תוקן** ב-`lib/inventory.js` (שני המקומות, `computeInventoryAvailability`
ו-`getBulkAvailableInventory`) - הוסרה ההרחבה השנייה; הזמנה אחרת נבדקת
כעת כנקודה מדויקת מול הימים הנסרקים (שכבר מורחבים פעם אחת סביב תאריך
היעד), לא כטווח מורחב בעצמה. אומת מול דגם 549: **16 מתוך 18 מידות תואמות
עכשיו לאקסס בדיוק מוחלט** (בהשוואה הוגנת, בלי רזרבה) - עלייה מ-13/18 לפני
התיקון. Commit `2c1ea7b`, נדחף ל-main (מדפלוי אוטומטית לשני האתרים - זה
תיקון קוד משותף, לא הגדרה, ר' [[code-fixes-vs-settings-scope-rule]]).

**עדיין פתוחות: מידות 06 ו-42.** גם אחרי שני התיקונים, שתי המידות האלה
עדיין סוטות ב-1 יחידה. נמצא הסבר חלקי: לשתיהן יש **שתי הזמנות שונות בימים
שונים** בתוך החלון הנסרק (למשל מידה 06: הזמנה 52369 ב-07/10 והזמנה 53205
ב-05/10) - האלגוריתם הנוכחי (מקסימום ביקוש על פני יום בודד) סופר רק 1
(אף יום לא רואה את שתי ההזמנות יחד), בעוד אקסס סופר 2. זה מרמז שהנוסחה
האמיתית של אקסס (`Sum(IIf(...,[כמות_בתפוסה],0)) + IIf([יום_1]>0,...)+...`
- סכום של דלתאות חיוביות על פני החלון, לא מקסימום-ליום-בודד אמיתי) עשויה
לחבר ביקוש מהזמנות שונות שלא חופפות ביום ספציפי, במקום למקסם. לא תוקן -
דורש בדיקה ממוקדת אם רוצים לסגור את שתי המידות האחרונות האלה.

## 14. תיקון "סכום על פני החלון" (במקום מקסימום-ליום) - סוגר את מידה 06, מידה 42 מתבררת כלא-קשורה (2026-09-15, המשך אותו יום)

המשך ישיר של הסעיף הקודם, מאותה שיחה. התיאוריה שם ("אקסס מחבר דלתאות
חיוביות, לא ממקסם") נבדקה ישירות ואומתה: `computeInventoryAvailability`
ו-`getBulkAvailableInventory` שונו כך שכל הזמנה אחרת שתאריכה נופל בתוך
החלון הנסרק (`targetDays`) מצטברת (`windowDemand[size] += qty`), במקום
להיבדק מול "ביקוש מקסימלי ליום בודד" (`maxDayDemand`) - שינוי ממקסימום
ליום ל**סכום על פני כל החלון בבת אחת**. זה עדיין לא מרחיב אף הזמנה (התיקון
מסעיף 13 נשאר בתוקף) - רק משנה איך צוברים ביקוש מכמה הזמנות שונות בתוך אותו
חלון ממוקסם-פעם-אחת.

**אומת מול דגם 549, כל 18 המידות, בפועל (לא רק תיאוריה):** נבנה סקריפט
נפרד (`scratch/verify_sum_fix_549_all_sizes.js`) שמריץ את הלוגיקה החדשה
(מועתקת מילה-במילה מ-`lib/inventory.js` הנוכחי) על אותו dump טרי מפוסטגרס
(`scratch/pg_549_all_sizes_fresh.json`, בלי רזרבה) ומשווה לטבלת אמת-אקסס
החיה מאותה שיחה. **תוצאה: 17 מתוך 18 מידות תואמות בדיוק** (עלייה מ-16/18) -
**מידה 06 נסגרה** (ההזמנות 52369+53205, בימים שונים בתוך החלון, נספרות
עכשיו יחד = 2, תואם אקסס). מידה 42 היא היחידה שנשארה.

**מידה 42 נחקרה עד הסוף - ומתברר שהיא לא קשורה לאלגוריתם בכלל.** שליפה
ישירה מאקסס (`scratch/pull_access_549_size42.js`, ADODB עם ה-short-path
הרגיל) של כל ההזמנות הפעילות למידה 42 חשפה הזמנה שלא הופיעה כלל בדאטה
שנשלף מפוסטגרס: **הזמנה 52367**, תאריך אירוע 06/10 (לועזי, לפי הערך הגולמי
`2026-10-06T21:00:00Z`) שממיר לתאריך ישראלי 07/10 - בדיוק תאריך היעד עצמו.
זו בדיוק אחת משלוש ההזמנות (52367/52723/53160) שכבר תועדו בסעיף 12 כפריט
שנמחק בפועל (soft-delete, `isDeleted=true`) באתר אבל עדיין פעיל (`מחוק=False`)
באקסס - אי-התאמת נתונים ידועה שהוחלט במפורש להשאיר ללא טיפול אוטומטי
("needs a human call on which side is right"). אקסס סופר את ההזמנה הזו
(מחוק=False אצלו) ולכן רואה ביקוש=1 על יחידת המלאי היחידה (לא-רזרבה) של
מידה 42 ביום היעד; האתר, בצדק, לא סופר פריט שנמחק. **זה לא באג אלגוריתמי -
זו בדיוק אי-ההתאמה שכבר תועדה ונשארה בכוונה פתוחה.** עם תיקון ה-52367
(ברגע שיוחלט איזה צד נכון) מידה 42 תיסגר גם היא, ל-18/18 מלא.

**תוקן** ב-`lib/inventory.js` (שני המקומות, זהה לתבנית סעיף 13) - התגובה
בקוד עודכנה בהתאם. Lint נקי (`npx eslint lib/inventory.js` - אפס
אזהרות/שגיאות; שגיאות ה-lint הקיימות בריפו כרגע ב-`ChangesChips.js`
ו-`offlineSync.js` הן קודמות ולא קשורות, אומתו גם על `main` לפני השינוי).

## 15. "סכום על פני חלון" גם הוא לא נכון - המשתמש תפס באג אמיתי; פוענח הנוסחה האמיתית של אקסס מה-VBA (2026-09-15, אותו יום, המשך)

**המשתמש תפס באג ממשי בתיקון של סעיף 14, עוד לפני שהספיק להיבדק על מקרה
שלא הופיע בדגם 549.** הדוגמה שהמשתמש נתן: אם יש הזמנה ל-5 בחודש והזמנה
ל-8 (עם bufferDays=2), הן **יכולות לחלוק שמלה אחת** (המרחק ביניהן, יומיים
ריקים, מספיק). אבל אם יש הזמנה ל-5 והזמנה ל-7, הן **לא יכולות** לחלוק
(רק יום ריק אחד ביניהן - לא מספיק) וצריכות 2 שמלות שונות.

בדיקה ישירה הוכיחה שהתיקון הקודם (`windowDemand[size] += qty` על כל הזמנה
שנופלת בתוך החלון, ר' סעיף 14) **טועה בדיוק בתרחיש הזה**: הוא סוכם *כל*
הזמנה שבתוך החלון סביב תאריך היעד, בלי לבדוק אם ההזמנות **רחוקות מספיק
זו מזו** (לא רק מתאריך היעד) כדי לחלוק שמלה - ולכן היה סופר 2 גם עבור
5+8 (במקום 1). זה לא נבדק בדגם 549 כי אף מידה שם לא הכילה בפועל שתי
הזמנות שרחוקות-מספיק-אחת-מהשנייה אבל שתיהן עדיין בתוך חלון היעד המשותף -
כך שה"17/18" שדווח בסעיף 14 היה נכון על הנתונים שנבדקו, אבל לא היה
נכון באופן כללי.

**פוענחה הנוסחה האמיתית של אקסס - לא מהתיאוריה, מהקוד המקורי.** נמצא
ב-`access_code/Modules/הזמנות_readable.txt` (פונקציות `חישוב_ימים_לפני_ואחרי`
ו-`קוד_למחרוזת`, שורות 256-332) המנגנון שבונה את ה-SQL הדינמי. זו **שרשרת
דלתאות בסגנון קדאן** (Kadane's algorithm - "maximum subarray"), לא סכום נאיבי
ולא מקסימום-ליום נאיבי:

```
o(k) = ביקוש (סכום כמות) בהזמנות שהתאריך/טווח שלהן (ללא הרחבה) מכסה את
       היום שנמצא ב-offset עסקי k מתאריך היעד, עבור k מ -bufferDays עד +bufferDays

thirdTerm = o(-bufferDays) + ... + o(0)
day[1]    = o(+1) - o(-bufferDays)
day[j]    = max(0, day[j-1]) + o(+j) - o(j-1-bufferDays)     עבור j=2..bufferDays

תפוסה = thirdTerm + max(0,day[1]) + max(0,day[2]) + ... + max(0,day[bufferDays])
```

**אומת נגד 3 מקרים בפועל, לא רק תיאוריה:**
1. מידה 06 האמיתית (הזמנות ב-offset -2 ו-0): הנוסחה נותנת 2 - תואם אקסס.
2. התרחיש ההיפותטי של המשתמש (offset -2 ו-+1, כלומר "5 ו-8"): הנוסחה
   נותנת **1** - בדיוק כמו שהמשתמש טען, ומוכיח שהתיקון הקודם (2) היה שגוי.
3. הזמנה 52733 (offset +3, מחוץ לטווח [-2,2] לגמרי): לא נכנסת לנוסחה
   בכלל, תורמת 0 - תואם את התיקון מסעיף 13 (לא חוסמת).

**אומת גם מחדש נגד כל 18 המידות האמיתיות של דגם 549** (`scratch/verify_deployed_formula_549_all_sizes.js`,
מריץ את הקוד המעודכן מילה-במילה) - **17/18 תואמות בדיוק**, אותה תוצאה
כמו סעיף 14 (מידה 42 עדיין אותה אי-התאמת נתונים ידועה, לא קשורה לאלגוריתם)
- כלומר הנוסחה הנכונה לא מפסידה אף התאמה שכבר הייתה נכונה, ומתקנת את
המקרה הכללי שהתיקון הקודם היה שובר.

**תוקן** ב-`lib/inventory.js` - נוספה `occupancyFormula` (שרשרת הדלתאות
הכללית, פרמטרית לפי `bufferDays`) והוחלף בה החישוב בשני המקומות
(`computeInventoryAvailability`, `getBulkAvailableInventory`). גם הוסרה
`getOccupiedDays` שהפכה ללא-בשימוש. Lint נקי. אומת מול הדאטה האמיתית
ומול שני תרחישים סינתטיים (משותף/לא-משותף). Commit נפרד (לא אותו commit
של סעיף 14), נדחף ל-main.
